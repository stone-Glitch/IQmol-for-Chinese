# 菜单中文化穷举审查（2026-09-24）

> 审查方式：穷举法。扫描全仓 `src/` 所有 `addMenu` / `addAction` / `new QMenu` / `addSeparator` 菜单调用，
> 提取菜单项文本，区分 `tr()` 包裹与否，并对所有 `tr()` 字面菜单文本逐一与 `translations/zh_CN.ts` 比对译文状态。
> 责任人：@IQmol 汉化组
> 状态：**完成（发现并已修复 1 处真漏翻）**

## 一、结论速览

| 审查项 | 结果 |
|---|---|
| 全仓 `tr()` 字面菜单文本数 | 33 条 |
| 其中已译文（`.ts` 有非空译文） | 33 条（**100%**） |
| 包 `tr()` 但缺译文（missing） | 0 |
| 包 `tr()` 但译文为空（unfinished） | 0 |
| 裸字符串（未包 `tr()`）菜单项 | 5 条（全部在 `InputDialog.C` 的 Job/Font 子菜单） |
| 历史快照目录 `src/Process/save/` | 不参与编译（CMake/configure 不含），其 3 个裸字符串为旧副本，不计 |

**唯一真漏翻**：`src/Qui/InputDialog.C` 中 Q-Chem 输入编辑器菜单的 **5 个 `Job`/`Font` 子菜单项使用了裸字符串**，
lupdate 永远无法提取，运行时恒为英文。已在本次审查中修复（包 `tr()` + 补译文）。

## 二、各菜单逐项核对

### 1. 主窗口菜单栏（`src/Main/MainWindow.C`，91 处调用）
| 顶级菜单 | 子项（节选） | 状态 |
|---|---|---|
| File（文件） | New Molecule、Open…、Open Directory…、Recent Files、Close Viewer、Save All、Save As、Save Picture、Quit | 全 `tr()`，译文齐 |
| Edit（编辑） | Undo、Redo、Cut、Copy、Paste、Select All、Clear Selection、Invert Selection、Reindex Atoms、Preferences | 全 `tr()`，译文齐 |
| Display（显示） | Full Screen、Reset View、Camera、Configure Appearance、Atom Labels（Element/Index/Mass/NMR/Charge/Spin/Label） | 全 `tr()`，译文齐 |
| Build（构建） | Insert Molecule by ID、Fill Valencies With Hydrogens、Set Geometric Constraint、Freeze Atom Positions、Minimize Energy、Force Field（MMFF94 等力场名，合理不译）、Translate To Center、Symmetrize 等 | 全 `tr()`，译文齐（力场名 `ff` 变量为非译项） |
| Calculation（计算） | Q-Chem Setup、Job Monitor、Edit Servers、Gromacs Setup、Edit Amber Config 等 | 全 `tr()`，译文齐 |
| Help（帮助） | Show Help | 全 `tr()`，译文齐 |

> 注：力场名（`MMFF94` 等）由变量 `ff` 注入，属化学方法名/明确不译项。

### 2. Q-Chem 输入编辑器菜单（`src/Qui/InputDialog.C`，17 处调用）
| 顶级菜单 | 子项 | 状态 |
|---|---|---|
| File（文件） | Save As、Close | `tr()`，译文齐 |
| Edit（编辑） | Copy、Paste、Cut | `tr()`，译文齐 |
| Job（任务） | **New Job Section**、**Reset Input** | ⚠️ 裸字符串 → **已修复**（`tr("新建作业段")` / `tr("重置输入")`） |
| Job | Submit | `tr()`，译文齐 |
| Font（字体） | **Bigger**、**Smaller**、**Set Font** | ⚠️ 裸字符串 → **已修复**（`tr("放大")` / `tr("缩小")` / `tr("设置字体")`） |

> 说明：`InputDialog : public QMainWindow` 且带 `Q_OBJECT`，`tr()` 生效正常。
> 菜单栏标题（File/Edit/Job/Font）本身已 `tr()` 且有译文（文件/编辑/任务/字体）；
> 若此前版本见英文标题，系旧构建包，拉取最新 `main` 重编即恢复。

### 3. 右键 / 上下文菜单
| 文件 | 菜单内容 | 状态 |
|---|---|---|
| `src/Layer/MoleculeLayer.C`（3D 视图右键） | 电荷模型（Gasteiger、Mulliken、Multipole Derived、CHELPG、Hirshfeld、Lowdin、Natural、Merz-Kollman ESP/RESP 等） | 全 `tr()`，译文齐（方法名为术语，保留西文但已纳入翻译框架） |
| `src/Process/JobMonitor.C`（任务监视器右键） | Reconnect Servers、Remove All Processes、Close、Delete Job From Queue、Cancel Copy、Kill Job、Remove Job、Query Job、View Output File、Open Results、Copy Results From Server | 全 `tr()`，译文齐 |
| `src/Grid/GridInfoDialog.C` | Delete、Export Cube File、Export Cube File (Switch Phase) | 全 `tr()`，译文齐 |
| `src/Qui/GeometryConstraint.C` | Delete | `tr()`，译文齐 |
| `src/Plot/CustomPlot.C` | Save Image As | `tr()`，译文齐 |

### 4. 排除项
- `src/Process/save/JobMonitor.C` 中 `Delete Job From Queue`/`Cancel Copy`/`Kill Job` 为裸字符串，但该目录为**历史快照、不参与编译**，其对应实际版本（`src/Process/JobMonitor.C`）已 `tr()` 且有译文。
- 下拉框取值（`Energy`、`HF`、`6-31G`、`DIIS`、`SAD`、`MMFF94` 等）为输入关键字/基组/方法名，按规范保留英文，非菜单漏翻。

## 三、本轮修复

| 文件 | 改动 |
|---|---|
| `src/Qui/InputDialog.C` | 5 处 `name = "..."` → `name = tr("...")`（New Job Section、Reset Input、Bigger、Smaller、Set Font） |
| `translations/zh_CN.ts` | 新增 5 条译文（新建作业段 / 重置输入 / 放大 / 缩小 / 设置字体），`lupdate` 收敛、`lrelease` 重生成 |
| `translations/zh_CN.qm` | 重新生成：2111 条 / 153 context / 0 unfinished（较上轮 2106 +5） |
| 文档口径 | 全仓翻译统计 2106 → 2111 同步更新 |

## 四、复检命令（供 CI / 自查）

```bash
# 穷举扫描裸字符串菜单项
grep -rnE '(addMenu|addAction|new QMenu)\s*\(\s*"[A-Za-z]' src/ --include="*.C" --include="*.h" | grep -v "/save/"

# 译文完整性（应为 0 unfinished）
lrelease translations/zh_CN.ts -qm /tmp/check.qm 2>&1 | tail -1
```
