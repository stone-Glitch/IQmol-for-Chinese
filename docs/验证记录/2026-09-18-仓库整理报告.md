# 仓库整理报告（2026-09-18）

**类型**：整理记录
**范围**：文档一致性维护（未改动任何代码/译文）
**基线**：`master` @ `012af66`（采纳汉化反馈包后）
**整理前状态**：工作区干净，无临时文件残留，无未跟踪文件

---

> ⚠️ **数据更新提示**：本文为 2026-09-18 历史整理报告，当时译文为 1563 条。当前主分支译文为 **2111 条 / 153 个 context，0 unfinished**。软件版本为 **v3.2.3**（与 `src/version.h` 及上游官方一致）。

## 一、整理动机

采纳第三方审计反馈包（4 issue + 术语修正 + 语言切换）后，`translations/zh_CN.ts`
由 1519 条扩至 **1563 条**，并新增了界面语言切换特性与 2 份验证记录，但若干文档
仍停留在旧口径，存在**文档与实际脱节**与**索引缺失**两类问题。

---

## 二、发现的问题与处置

### 1. 翻译条数口径不一致（4 处）

`.ts` 实际为 1563 条（`grep -c '<message'` 实测），但以下文档仍写 1519：

| 文件 | 位置 | 处置 |
|---|---|---|
| `README.md` | 汉化段落「界面翻译」 | 改为 1563 条，去掉过时的「111 个上下文」 |
| `仓库结构说明.md` | §3 `translations/` | 改为 1563 条，并补「条数口径 + 校验命令」说明块 |
| `doc/构建说明-中文版.md` | 开头成果行 + §翻译文件说明 | 两处均改为 1563 条 |
| `doc/编译-照着敲.md` | 构建日志示例 | **保留 1519 原文**（日志实录），加注说明现行条数 |

### 2. 验证记录索引缺失 2 份文档

`docs/验证记录/README.md` 的索引表漏收：

- `2026-09-13-OpenBabel-Windows静态链接undefined-reference诊断.md`
- `2026-09-18-第三方评测Qui.h完整性核查-已证伪.md`

已补入清单，并将 `编译与运行验证.md` 的条数标注为「1519 条（后扩至 1563 条）」。

### 3. 索引断链 2 处

`docs/验证记录/README.md` §四 的相对链接层级写错：

| 原链接 | 实际位置 | 修正为 |
|---|---|---|
| `../汉化对照表/` | `doc/汉化对照表/` | `../../doc/汉化对照表/` |
| `../界面英文残留清单.md` | `doc/界面英文残留清单.md` | `../../doc/界面英文残留清单.md` |

### 4. 新增特性未入文档

反馈包 issue4 引入的「界面语言切换」未在任何说明文档中体现，已补：

- `仓库结构说明.md`：新增 §五「界面语言切换」（选项表 + 实现方式 + 静态生效说明）；
  §四 关键文件索引补入 `Preferences.{h,C}`、`PreferencesBrowser.{ui,C}`、`IQmolApplication.C`。
- `README.md`：汉化段落补「界面语言切换」条目。

### 5. 关键修复未记录（issue1/issue2）

- `仓库结构说明.md` §三 `update_translations.sh` 一行，补注 `-extensions C,ui`
  的必要性（否则 292 个 `.C` 源文件 `tr()` 零提取）。
- `README.md` 翻译维护段落，补同款注释。
- `仓库结构说明.md` §5 验证记录表补入 Qui.h 澄清文档。

---

## 三、链接校验结果

对 `仓库结构说明.md` 与 `docs/验证记录/README.md` 中全部相对链接做存在性校验：

```
仓库结构说明.md       3/3 OK
docs/验证记录/README.md  10/10 OK（修正前 2 处 MISS）
```

---

## 四、未处置项（有意保留）

| 项 | 原因 |
|---|---|
| `doc/GitHub上传方案.md` 归类 | 属沙箱/发布类内部文档，与用户手册混放略欠清爽；但移动会破坏既有引用，收益小于风险，暂留原处 |
| `doc/编译-照着敲.md` 日志中的 1519 | 系构建日志实录，改动反而不实，仅加注说明 |
| `submodules-package` 引用 | 指另一分支内容，非本分支断链，属正常 |

---

## 五、改动清单

| 文件 | 改动 |
|---|---|
| `README.md` | 条数修正 + 语言切换条目 + lupdate 扩展名注 |
| `仓库结构说明.md` | 条数修正 + 语言切换章节 + 索引补全 + 关键修复注 |
| `doc/构建说明-中文版.md` | 条数修正 2 处 |
| `doc/编译-照着敲.md` | 日志示例加注 |
| `docs/验证记录/README.md` | 索引补 2 条 + 断链修正 2 处 + 条数标注 |
| `docs/验证记录/2026-09-18-仓库整理报告.md` | 本报告（新增） |

> 本次整理**未改动任何源码、译文（.ts/.qm）或构建脚本**，仅维护文档一致性与链接有效性。
