# IQmol 汉化项目 — 程序全量检查记录表

> **检查依据**：通用程序检查目录（26 大类），按本项目实际形态（C++/Qt5 桌面应用的汉化工程）裁剪适配。
> **检查日期**：2025-08-28　**检查对象**：`/workspace/IQmol3`（含 3 次汉化提交）
> **结果标记**：✅ 通过　❌ 不通过（已修复）　⚠️ 待确认　➖ 不适用

## 一、检查结果总览

| 结论 | 数量 |
|------|------|
| 通过 | 22 |
| 不通过 → 已修复 | 3 |
| 待确认 → 已解决/待决策 | 1 已解决（.ui 直改改回 tr()）、1 待决策（截图工具入库） |
| 不适用（裁剪掉） | 9 大类 |

---

## 二、检查结果记录表

### A. 构建系统与配置（裁剪自"构建/打包/CI"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 1 | CMake 配置完整可运行 | ✅ 通过 | 修复后重新配置成功，Build files 正常生成 |
| 2 | `find_program(LRELEASE_EXECUTABLE)` 缺失时的防护 | ❌→✅ 已修复 | 原先无 fallback，lrelease 缺失时 POST_BUILD 直接失败；现加 `if()` 防护 + WARNING 提示，缺失时跳过 qm 生成、界面回退英文，构建继续 |
| 3 | POST_BUILD 自动生成 `zh_CN.qm` 并随目标输出 | ✅ 通过 | CMakeLists.txt:191-210，输出至可执行文件同级 translations/ |
| 4 | 文档构建脚本健壮性 | ✅ 通过 | `build_docs.sh` 具备 `set -e`、`command -v xelatex` 依赖检查、`--clean` 清理；实测编译成功（34 页） |
| 5 | 翻译流程脚本健壮性 | ✅ 通过 | `update_translations.sh` 一体化 lupdate→防退化（移除误标 vanished）→lrelease，支持 `--sync` |

### B. 版本控制卫生（裁剪自"仓库管理"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 6 | `.gitignore` 规则作用域 | ❌→✅ 已修复 | 原全局 `*.out` 误伤 `samples/*.out`（Q-Chem 样本数据，git 已跟踪、未来新增会被忽略）；现限定为 `/doc/*.out` 等，`git check-ignore` 验证 samples 不再被忽略 |
| 7 | 中间工作文件已清理 | ✅ 通过 | `zh_CN_full.ts`、`zh_CN_new.ts` 已在 57a45a7 删除，仅保留正式 zh_CN.ts |
| 8 | 提交历史规范 | ✅ 通过 | 3 次提交（90282d3 全量汉化 / 57a45a7 清理 / ed5eb5a 检查修复），中文规范 message |

### C. 翻译资产完整性（裁剪自"数据/资源文件"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 9 | `zh_CN.ts` 翻译完整度 | ✅ 通过 | 1519 条 / 111 个 context；未完成 0、vanished 0、空翻译 0（tr() 方案还原后清理 3 条冗余，1522→1519） |
| 10 | `zh_CN.qm` 与 `.ts` 同步 | ✅ 通过 | 重新 lrelease 后一致，1519 条全部 finished |
| 11 | lupdate 解析局限的兜底 | ✅ 通过 | 4 个 context（ViewerModel / Layer::Component / MolecularSurfaces / Molecule）手工补条目；脚本固化防退化流程 |
| 12 | 翻译文件入库状态 | ✅ 通过 | translations/zh_CN.ts、zh_CN.qm 均被 git 跟踪 |

### D. 源码国际化质量（裁剪自"代码质量/字符串处理"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 13 | `tr()` 改造覆盖（菜单/对话框/图层名） | ✅ 通过 | 20+ 源文件改造；运行时验证主窗口菜单全中文 |
| 14 | 非注释代码中的中文硬编码 | ✅ 通过 | 曾有 3 处 .ui 直改（CPU 数/Ω/α），已按负责人决策改回 tr() 方案：.ui 还原英文原文、译文移入 zh_CN.ts、清理 3 条冗余条目（提交 912af41），恢复英文回退能力 |
| 15 | 双语言兼容逻辑 | ✅ 通过 | MoleculeLayer.C:2009 对 `DefaultMoleculeName` 与 `tr("Untitled")` 双判断，避免比较失效 |
| 16 | tr() 调用时机正确性 | ✅ 通过 | ViewerModel.C 的 `m_global(tr("Global"),…)` 位于构造初始化列表，翻译时机合法 |

### E. 术语一致性（裁剪自"翻译质量"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 17 | 术语统一（Job=任务） | ✅ 通过 | 全文检索"作业"0 残留；"Job Info→任务信息" |
| 18 | 专业术语遵循全国科技名词委规范 | ✅ 通过 | 前次 8 维度检查已核（分子轨道/表面/振动频率等） |
| 19 | 上下文命名空间规范 | ✅ 通过 | `IQmol::ViewerModel`、`IQmol::Layer::Molecule` 等 Qt context 规则正确 |

### F. 文档与手册（裁剪自"文档"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 20 | 手册配图完整性 | ✅ 通过 | tex 引用 71 张全部存在（含 gallery/ 子目录 24+7 张）；未被引用的 12 个为原版冗余，无影响 |
| 21 | 中文配图占比 | ✅ 通过 | 15 个对话框/主窗口图为中文界面实拍；原版英文图保留于图标类素材 |
| 22 | 中文重叠/排版问题 | ✅ 通过 | `\scshape` 已全部改 `\textbf`，ctex/xelatex 下渲染正常 |
| 23 | 3 个空章节补全 | ✅ 通过 | 反应路径 / 制作电影 / 配置 已撰写并细化，编译后 34 页 |
| 24 | README 更新 | ⚠️→✅ 已修复 | 原版 README 无任何汉化说明；已追加"简体中文本地化"章节（翻译资产/构建脚本/手册/截图说明） |

### G. 法律合规（裁剪自"许可证合规"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 25 | LICENSE 文件存在 | ✅ 通过 | GPLv3 完整保留 |
| 26 | 源文件 GPL 头部声明 | ✅ 通过 | main.C 等文件版权/许可声明未被改动 |
| 27 | 汉化改动的许可一致性 | ✅ 通过 | 翻译/脚本/文档均衍生自 GPL 工程，随同 GPLv3 分发合规 |

### H. 资产入库与可复现性（裁剪自"发布物/可追溯"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 28 | dialog_screenshots 入库 | ✅ 通过 | 中文对话框截图已 git 跟踪 |
| 29 | doc/figures 中文配图入库 | ✅ 通过 | 已跟踪 |
| 30 | 截图驱动工具入库 | ⚠️ 待确认 | `/tmp/dlg_loader.cpp`、`/tmp/filedlg_shot.cpp`（QUiLoader 静态渲染驱动）未入库；截图成果已入库，工具属一次性验证用途，如需可复现截图可后续移入 scripts/ |

### I. 原项目固有代码质量（异常处理/资源管理/并发/测试/日志——本次检查裁剪说明）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 31 | 异常处理 / 资源管理 / 并发安全 / 日志 / 单元测试 | ➖ 不适用 | 汉化改动全部为字符串层（tr() 包裹、.ui 文本、.ts/.qm），不触及控制流、内存管理、线程模型与错误语义；原项目这些维度的固有质量不属于本次汉化任务的验收范围 |

### J. 运行时验证（裁剪自"功能验证"类）

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 32 | 主窗口运行时全中文 | ✅ 通过 | 前次验证：文件/编辑/显示/构建/计算/帮助菜单、模型视图/全局/未命名/历史记录、欢迎界面均中文 |
| 33 | 干净环境启动无异常弹窗 | ✅ 通过 | 已在 /tmp/iqmol_clean 干净目录重截验证 |

---

## 三、待确认项状态

1. ~~**3 处 .ui 直改的英文回退**（序号 14）~~ → ✅ **已解决**（2025-08-28 负责人决策"改回 tr() 方案"）：
   3 个 `.ui` 已还原英文原文（CPUs / Omega / Alpha），译文 `CPU 数` / `Ω（衰减参数）` / `不透明度 (α)` 移入 `zh_CN.ts` 对应 context（QueueResourcesDialog / AttenuationParameterTab / ColorDialog），并清理直改期间产生的 3 条冗余中文 source 条目（条目总数 1522 → 1519，全部完成）。静态渲染验证 3 个对话框运行时中文显示正常（提交 912af41）。
2. **截图工具是否入库**（序号 30）：⏳ 待决策。一次性驱动程序目前仅在 /tmp，不影响构建与使用；如希望任何人可复现截图，建议移入 `scripts/` 并补注释。

## 四、检查执行的修复（4 项）

| 文件 | 修复内容 |
|---|---|
| `CMakeLists.txt` | lrelease 查找失败时发出 WARNING 并跳过 POST_BUILD qm 生成，不再阻断整机构建 |
| `.gitignore` | LaTeX 中间文件规则限定到 `/doc/`，解除对 `samples/*.out` 的误伤 |
| `README.md` | 追加"简体中文本地化"章节，说明翻译资产、构建脚本、手册与截图 |
| 3 个 `.ui` + `zh_CN.ts` | 按负责人决策将 3 处直改中文还原为 tr() 方案（详见"三、待确认项状态"第 1 条） |

## 五、裁剪掉的原清单大类（不适用于本项目）

数据库类（表结构/迁移/索引/SQL 注入）、Web/API 类（REST/鉴权/CORS/限流）、移动端类（权限/推送/适配）、微服务与中间件类（消息队列/服务发现/熔断）、前端框架类（浏览器兼容/SEO/PWA）、容器编排类（K8s 探针/HPA）、国际化以外的本地化格式（RTL 等）——本项目为本地 C++/Qt 桌面应用汉化工程，均不涉及。
