# Q-Chem 关键词说明（tooltip）汉化对照表

> 来源：`share/qchem_option.db` → `options.Description`（Qt RichText HTML）
> 规则：关键词名**不译**（输入文件语法）；只译 `<body>` 内的可见文本；`{KEY}` 交叉引用占位符原样保留。

共 504 条；表中多个文本段以「 / 」分隔（对应同一说明里的多段文字）。

| 关键词 | 原文 | 译文 |
|---|---|---|
| `AAAA` |  |  |
| `ADC_CVS` | Activate the core-valence separation approximation in ADC calculations to compute core-excited states. In addition the parameter CC_REST_OCC has to be set to define the core orbitals. / Recommendation: / This keyword is deprecated. Use METHOD instead. | 在 ADC 计算中启用芯-价分离近似，以计算芯激发态。此外必须设置参数 CC_REST_OCC 来定义芯轨道。 / 建议： / 该关键词已弃用。请改用 METHOD。 |
| `ADC_C_C` | Scaling factor / c / C / for spin-opposite scaled ADC calculations. The parameter value is devided by 1000 to obtain the proper scaling factor. / Recommendation: / Use default. | 标度因子 / c / C / 用于自旋反向标度 ADC 计算。参数值需除以 1000 才得到正确的标度因子。 / 建议： / 使用默认值。 |
| `ADC_C_T` | Scaling factor / c / T / for MP(2) T amplitudes in spin-opposite scaled ADC calculations. The parameter value is devided by 1000 to obtain the proper scaling factor. / Recommendation: / Use default. | 标度因子 / c / T / 用于自旋反向标度 ADC 计算中的 MP(2) T 振幅。参数值需除以 1000 才得到正确的标度因子。 / 建议： / 使用默认值。 |
| `ADC_C_X` | Scaling factor / c / X / in spin-opposite scaled ADC calculations. The parameter value is devided by 1000 to obtain the proper scaling factor. / Recommendation: / Use default. | 标度因子 / c / X / 用于自旋反向标度 ADC 计算。参数值需除以 1000 才得到正确的标度因子。 / 建议： / 使用默认值。 |
| `ADC_DAVIDSON_CONV` | Convergence criterion on the RMS difference between successive sets of ADC amplitudes during the Davidson diagonalization [10 / -n / ]. / Recommendation: / Use default. | Davidson 对角化过程中相继两组 ADC 振幅之间均方根差值的收敛判据 [10 / -n / ]。 / 建议： / 使用默认值。 |
| `ADC_DAVIDSON_MAXITER` | Maximum number of iterations to determine the eigenstates in an ADC calculation using the Davidson algorithm. | 使用 Davidson 算法确定 ADC 计算中本征态的最大迭代次数。 |
| `ADC_DAVIDSON_MAXSUBSPACE` | Maximum number of ADC amplitudes in the subspace for the Davidson diagonalization. / Recommendation: / Larger values increase disk storage but accelerate and stabilize convergence. | Davidson 对角化子空间中的最大 ADC 振幅数目。 / 建议： / 较大的取值会增加磁盘存储量，但可加速并稳定收敛。 |
| `ADC_DAVIDSON_THRESH` | Numerical threshold to suppress noise in the ADC amplitudes during the Davidson diagonalization [10 / -n / ]. / Recommendation: / Use default. | Davidson 对角化过程中抑制 ADC 振幅噪声的数值阈值 [10 / -n / ]。 / 建议： / 使用默认值。 |
| `ADC_DIIS_ECONV` | Convergence criterion on the RMS difference between successive sets of ADC amplitudes during the Davidson diagonalization [10 / -n / ]. / Recommendation: / Use default. | Davidson 对角化过程中相继两组 ADC 振幅之间均方根差值的收敛判据 [10 / -n / ]。 / 建议： / 使用默认值。 |
| `ADC_DIIS_MAXITER` | Maximum number of iterations to determine the eigenstates in an ADC calculation using the DIIS algorithm. | 使用 DIIS 算法确定 ADC 计算中本征态的最大迭代次数。 |
| `ADC_DIIS_RCONV` | Convergence criterion on the RMS difference between ADC amplitudes in successive steps of the DIIS solver [10 / -n / ]. / Recommendation: / Use default. | DIIS 求解器相继步骤之间 ADC 振幅均方根差值的收敛判据 [10 / -n / ]。 / 建议： / 使用默认值。 |
| `ADC_DIIS_SIZE` | Specifies the maximum size of the DIIS space in an ADC(2) calculation. / Recommendation: / Larger values involve larger amounts of disk storage. | 指定 ADC(2) 计算中 DIIS 空间的最大大小。 / 建议： / 较大的取值需要更多的磁盘存储量。 |
| `ADC_DIIS_START` | Iteration number when the DIIS steps are turned on in an ADC(2)  calculation. Set to a large number to disable DIIS and use the Jacobi algorithm. / Recommendation: / Use default. | ADC(2) 计算中启用 DIIS 步骤的迭代序号。设为很大的数可禁用 DIIS，改用 Jacobi 算法。 / 建议： / 使用默认值。 |
| `ADC_DO_DIIS` | Compute the ADC(2) eigenvalues and eigenvectors by solving non-linear equation systems using the DIIS algorithm. / Recommendation: / Use only with extreme care! | 使用 DIIS 算法求解非线性方程组，从而计算 ADC(2) 的本征值与本征矢量。 / 建议： / 使用时务必极其谨慎！ |
| `ADC_ECORR` | Activate the computation of higher-order energy corrections for ADC excitation energies. [EXPERIMENTAL] | 启用 ADC 激发能的高阶能量校正计算。[实验性功能] |
| `ADC_EXTENDED` | Use the extended second order ADC variant ADC(2)-x. / Recommendation: / This keyword is deprecated. Use METHOD instead. | 使用扩展的二阶 ADC 变体 ADC(2)-x。 / 建议： / 该关键词已弃用。请改用 METHOD。 |
| `ADC_NGUESS_DOUBLES` | Set the number of guesses from the double excitation manifold. / Recommendation: / Use default. | 设置取自双激发流形的初始猜测数目。 / 建议： / 使用默认值。 |
| `ADC_NGUESS_SINGLES` | Set the number of guesses from the single excitation manifold. / Recommendation: / Use default ( = number of states to requested). | 设置取自单激发流形的初始猜测数目。 / 建议： / 使用默认值（等于所请求的态数目）。 |
| `ADC_ORDER` | Activate an ADC calculation and set the order of ADC to use. / Recommendation: / This keyword is deprecated. Use METHOD instead. | 启用 ADC 计算并设置所使用的 ADC 阶数。 / 建议： / 该关键词已弃用。请改用 METHOD。 |
| `ADC_PRINT` | Set the print level in the ADC part of the calculation. / Recommendation: / Use default. | 设置计算中 ADC 部分的输出级别。 / 建议： / 使用默认值。 |
| `ADC_PROP_ES` | Activate the calculation of excited state properties in an ADC calculation (one-particle transition properties from the ground state are activated by default). | 启用 ADC 计算中的激发态性质计算（默认情况下会启用基态的单粒子跃迁性质）。 |
| `ADC_PROP_ES2ES` | Activate the calculation of state-to-state transition properties in an ADC calculation. | 启用 ADC 计算中态-态跃迁性质的计算。 |
| `ADC_PROP_TPA` | Activate the calculation of two-photon absorption cross-sections for the excited states in an ADC calculation. | 启用 ADC 计算中激发态双光子吸收截面的计算。 |
| `ADC_SOS` | Activate the spin-opposite scaled variant of the second order ADC methods. / Recommendation: / This keyword is deprecated. Use METHOD instead. | 启用二阶 ADC 方法的自旋反向标度变体。 / 建议： / 该关键词已弃用。请改用 METHOD。 |
| `AIMD_FICTITIOUS_MASS` | Specifies the value of the fictious electronic mass is atomic units.  This quantity has dimensions (energy)x(time) / 2 / . / Recommendation: / Values in the range 50-200 a.u. have been employed. | 指定虚拟电子质量的原子单位数值。该量的量纲为 (能量)x(时间) / 2 / 。 / 建议： / 已有研究采用 50-200 a.u. 范围内的取值。 |
| `AIMD_FOCK_EXTRAPOLATION_ORDER` | Specifies the polynomial order / N / for Fock matrix extrapolation. | 指定 Fock 矩阵外推所用的多项式阶次 / N / 。 |
| `AIMD_FOCK_EXTRAPOLATION_POINTS` | Specifies the number M of old Fock matrices that are retained for use in extrapolation. / Recommendation: / Higher-order extrapolations with more saved Fock matrices are faster and conserve energy better than low-order extrapolations, up to a point. In many cases, the scheme ( / N / = 6, / M / = 12) | 指定保留用于外推的旧 Fock 矩阵数目 M。 / 建议： / 保留更多 Fock 矩阵的高阶外推在一定范围内比低阶外推更快、能量守恒也更好。在许多情况下，方案（ / N / = 6， / M / = 12）与 SCF_CONVERGENCE = 6 配合使用，可在保持能量守恒的同时节省约 50% 的计算开销。 |
| `AIMD_INITIAL_VELOCITIES` | Specifies the method for selecting initial nuclear velocities. / Recommendation: / This variable need only be specified in the event that velocities are not specified explicitly in a $velocity  section. | 指定选取初始核速度的方法。 / 建议： / 只有在 $velocity 段中没有显式指定速度时，才需要设置该变量。 |
| `AIMD_METHOD` | Selects an / ab initio / molecular dynamics algorithm. / Recommendation: / Born-oppenheimer MD (BOMD) yields exact classical molecular dynamics, provided that the energy is tolerably conserved. Curvy-steps extended Lagrangian MD (Curvy) is an approximation to exact classical dynamics whose validity  | 选择一种 / ab initio / 分子动力学算法。 / 建议： / 只要能量守恒在可接受的范围内，Born-Oppenheimer 分子动力学（BOMD）就能给出精确的经典分子动力学。Curvy-steps 扩展拉格朗日量分子动力学（Curvy）是对精确经典动力学的一种近似，应针对所关心的性质检验其适用性。 |
| `AIMD_MOMENTS` | Specifies the order of multipole moments that are output at each time step.  Setting this to 0 disables the printing of moments. | 指定每个时间步输出的多极矩阶次。设为 0 将不再输出多极矩。 |
| `AIMD_STEPS` | Specifies the requested number of molecular dynamics steps. | 指定所请求的分子动力学步数。 |
| `AIMD_TEMPERATURE` | Specifies a temperature (in Kelvin) for Maxwell-Boltzmann velocity sampling. / Recommendation: / This variable is only useful in conjunction with AIMD velocit initialisation set to Thermal. Note that the simulations are run at constant energy, rather than constant temperature, so the mean nuclear ki | 指定用于 Maxwell-Boltzmann 速度采样的温度（单位 Kelvin）。 / 建议： / 该变量仅在 AIMD 速度初始化方式设为 Thermal 时才有作用。注意模拟是在恒定能量而非恒定温度下运行的，因此平均核动能会在模拟过程中发生涨落。 |
| `AIMD_TIME_STEP` | Specifies the molecular dynamics time step, in atomic units (1 a.u. = 0.0242 fs). / Recommendation: / Smaller time steps lead to better energy conservation; too large a time step may cause the job to fail entirely. Make the time step as large as possible, consistent with tolerable energy conservatio | 指定分子动力学时间步长，采用原子单位（1 a.u. = 0.0242 fs）。 / 建议： / 时间步长越小，能量守恒越好；步长过大可能导致任务完全失败。应在能量守恒可接受的范围内尽量取较大的时间步长。 |
| `ANALYTIC_DERIVATIVE_ORDER` | Controls the order of derivatives that are evaluated analytically. The user is not normally required to specify a value, unless numerical derivatives are desired. The derivatives will be evaluated numerically if this option is set lower than the type of job requires. / Recommendation: / Usually set  | 控制解析求导的阶次。除非需要数值导数，用户通常无需指定该值。若该选项设置得低于任务类型所要求的阶次，导数将以数值方式求取。 / 建议： / 为提高效率通常设为最大可能值。注意，若所请求阶次的解析导数不可用，该选项会被自动调低。 |
| `ANHAR` | Selects whether or not to perform various nuclear vibrational theory (TOSH, VPT2, VCI) calculations to obtain vibrational anharmonic frequencies. / Recommendation: / Since this calculation involves the third and fourth derivatives at the minimum of the potential energy surface, it is recommended tha | 选择是否执行各类核振动理论（TOSH、VPT2、VCI）计算，以获得振动非谐频率。 / 建议： / 由于该计算涉及势能面极小点处的三阶和四阶导数，建议将几何优化容差（位移、梯度和能量）设置得更严格。注意，若体系存在偶然简并共振，VPT2 计算可能失败。关于提高非谐计算精度的更多细节，请参阅 VCI 这一 $rem 变量。 |
| `ANHARMONIC` | Selects whether or not to perform various nuclear vibrational theory (TOSH, VPT2, VCI) calculations to obtain vibrational anharmonic frequencies. / Recommendation: / Since this calculation involves the third and fourth derivatives at the minimum of the potential energy surface, it is recommended tha | 选择是否执行各类核振动理论（TOSH、VPT2、VCI）计算，以获得振动非谐频率。 / 建议： / 由于该计算涉及势能面极小点处的三阶和四阶导数，建议将几何优化容差（位移、梯度和能量）设置得更严格。注意，若体系存在偶然简并共振，VPT2 计算可能失败。关于提高非谐计算精度的更多细节，请参阅 VCI 这一 $rem 变量。 |
| `AO2MO_DISK` | Sets the amount of disk space (in megabytes) available for MP2 calculations. / Recommendation: / Should be set as large as possible, as discussed in the manual. | 设置可供 MP2 计算使用的磁盘空间大小（单位 MB）。 / 建议： / 如手册中所述，应尽可能设大。 |
| `AUXILIARY_BASIS` | Specifies the auxiliary basis to be used in a RI-MP2 calculation | 指定 RI-MP2 计算中使用的辅助基组 |
| `BASIS` | Specifies the basis sets to be used. / Recommendation: / Consult literature and reviews to aid your selection. | 指定所使用的基组。 / 建议： / 查阅文献与综述以辅助选择。 |
| `BASIS2` | Specifies the basis sets to be used. / Recommendation: / Consult literature and reviews to aid your selection. | 指定所使用的基组。 / 建议： / 查阅文献与综述以辅助选择。 |
| `BASIS2_SAVE` | Selects either a small basis set to use in basis set projection for the initial guess, or a subset basis for dual basis set calculations. | 选择用于初始猜测的基组投影小基组，或者用于双基组计算的子集基组。 |
| `BASIS_LINEAR_DEPENDENCE_THRESH` | Sets the threshold for determining linear dependence in the basis set.  The threshold is set to 10 / -n / . / Recommendation: / Set to 5 or smaller if you have a poorly behaved SCF and you suspect linear dependence in you basis set. Lower values (larger thresholds) may affect the accuracy of the cal | 设置判定基组线性相关的阈值。该阈值设为 10 / -n / 。 / 建议： / 若 SCF 收敛表现不佳且怀疑基组存在线性相关，可设为 5 或更小。较小的取值（即较大的阈值）可能影响计算精度。 |
| `BASIS_PROJECTION_TYPE` | Determines which method to use when projecting the density matrix for the basis set projection guess. | 决定在基组投影初始猜测中投影密度矩阵所采用的方法。 |
| `CCMAN2` |  |  |
| `CC_AMPLITUDE_RESPONSE` | If set to TRUE, adds amplitude response terms to one-particle and two-particle CCSD density matrices before calculation of properties. CC_PROP must be set to TRUE. / Recommendation: / The cost is always about the cost of an analytic gradient calculation, independent of whether or not the two-particl | 若设为 TRUE，则在计算性质之前把振幅响应项加入 CCSD 的单粒子和双粒子密度矩阵。CC_PROP 必须设为 TRUE。 / 建议： / 其开销始终约为一次解析梯度计算的开销，与是否请求双粒子性质无关。此外，只加入振幅响应项而不加入轨道响应项，几乎不可能改善性质的质量。不过它可用于调试目的。 |
| `CC_BLOCK_TENSOR_BUFFER_SIZE` | Specifies the maximum size, in Mb, of the buffers for in-core storage of block-tensors. / Recommendation: / Larger values can give better I/O performance and are recommended for systems with large memory (add to your / .qchemrc / file) | 指定块张量内存存储的缓冲区最大大小（单位 Mb）。 / 建议： / 较大的取值可获得更好的 I/O 性能，推荐用于内存较大的机器（可添加到你的 / .qchemrc / 文件中） |
| `CC_CANONIZE` | Whether to semi-canonicalize orbitals at the start of the calculation ( / i.e. / Fock matrix is diagonalized in each orbital subspace) / Recommendation: / Should not normally have to be altered. | 是否在计算开始时对轨道做半正则化（ / 即 / 在每个轨道子空间中对 Fock 矩阵对角化） / 建议： / 通常无需改动。 |
| `CC_CANONIZE_FINAL` | Whether to semi-canonicalize orbitals at the end of the ground state calculation. / Recommendation: / Should not normally have to be altered. | 是否在基态计算结束时对轨道做半正则化。 / 建议： / 通常无需改动。 |
| `CC_CANONIZE_FREQUENCY` | The orbitals will be semi-canonicalized every / n / theta resets. The thetas (orbital rotation angles) are reset every CC_RESET_THETA iterations. The counting of iterations differs for active space (VOD, VQCCD) calculations, where the orbitals are always canonicalized at the first theta-reset. / Rec | 轨道将每经过 / n / 次 theta 重置做一次半正则化。theta（轨道旋转角）每 CC_RESET_THETA 次迭代重置一次。对于活性空间（VOD、VQCCD）计算，迭代的计数方式不同，轨道总是在第一次 theta 重置时被正则化。 / 建议： / 对于不收敛的情形，可尝试较小的取值。 |
| `CC_CONVERGENCE` | Overall convergence criterion for the coupled-cluster codes. This is designed to ensure at least / n / significant digits in the calculated energy, and automatically sets the other convergence-related variables (CC_E_CONV, CC_T_CONV, CC_THETA_CONV, CC_THETA_GRAD_CONV, CC_Z_CONV) [10 / -n / ]. | 耦合簇程序的总体收敛判据。其设计目标是确保所计算的能量中至少有 / n / 位有效数字，并自动设置其他与收敛相关的变量（CC_E_CONV、CC_T_CONV、CC_THETA_CONV、CC_THETA_GRAD_CONV、CC_Z_CONV）[10 / -n / ]。 |
| `CC_CONVERGENCE_AMPLITUDES` | Convergence criterion on the RMS difference between successive sets of coupled-cluster doubles amplitudes [10 / -n / ] / Recommendation: / Use default | 相继两组耦合簇双振幅之间均方根差值的收敛判据 [10 / -n / ] / 建议： / 使用默认值 |
| `CC_CONVERGENCE_ENERGY` | Convergence desired on the change in total energy, between iterations. | 相邻迭代之间总能量变化所要求的收敛判据。 |
| `CC_CONVERGENCE_ZVECTOR` | Convergence criterion on the RMS difference between successive doubles / Z / -vector amplitudes [10 / -n / ]. / Recommendation: / Use Default | 相继双激发 / Z / 矢量振幅之间均方根差值的收敛判据 [10 / -n / ]。 / 建议： / 使用默认值 |
| `CC_DCONVERGENCE` | Convergence criterion for the RMS residuals of excited state vectors / Recommendation: / Use default. Should normally be set to the same value as CC_DTHRESHOLD. | 激发态矢量均方根残差的收敛判据 / 建议： / 使用默认值。通常应设为与 CC_DTHRESHOLD 相同的值。 |
| `CC_DIIS` | Specify the version of Pulay's Direct Inversion of the Iterative Subspace (DIIS) convergence accelerator to be used in the coupled{cluster code. / 0 Activates procedure 2 initially, and procedure 1 when gradients are smaller / than DIIS12 SWITCH. / 1 Uses error vectors dened as dierences between p | 指定耦合簇程序中使用的 Pulay 迭代子空间直接求逆（DIIS）收敛加速算法的版本。 / 0 初始启用方案 2，当梯度 / 小于 DIIS12 SWITCH 时启用方案 1。 / 1 误差矢量定义为相邻迭代参数矢量之间的差值， / 在接近收敛时最为高效。 / 2 误差矢量定义为用近似对角海森矩阵的平方根标度后的梯度， / 在远离收敛时最为高效。 / 建议： / DIIS1 可能更稳定。若在计算的早期阶段（梯度较大时）遇到 DIIS 问题，可尝试使用 DIIS 1。 |
| `CC_DIIS12_SWITCH` | When to switch from DIIS 2 to DIIS 1 procedure, or when DIIS 2 procedure is required to generate DIIS guesses less frequently. Total value of DIIS error vector must be less than 10 / -n / , where / n / is the value of this option. | 何时从 DIIS 2 方案切换到 DIIS 1 方案，或者 DIIS 2 方案何时需要以更低的频率生成 DIIS 猜测。DIIS 误差矢量的总值必须小于 10 / -n / ，其中 / n / 为该选项的取值。 |
| `CC_DIIS_EXTRAPOLATION_FREQUENCY` | DIIS extrapolation will be attempted every n iterations. However, DIIS2 will be attempted every iteration while total error vector exceeds CC_DIIS12_SWITCH. DIIS1 cannot generate guesses more frequently than every 2 iterations. | 每 n 次迭代尝试一次 DIIS 外推。不过，当误差矢量总值超过 CC_DIIS12_SWITCH 时，每一次迭代都会尝试 DIIS2。DIIS1 生成猜测的频率不能高于每 2 次迭代一次。 |
| `CC_DIIS_MAXIMUM_OVERLAP` |  |  |
| `CC_DIIS_MINIMUM_OVERLAP` | The DIIS procedure will be halted when the square root of smallest element of the error overlap matrix is less than 10 / -n / , where / n / is the value of this option. Small values of the B matrix mean it will become near-singular, making the DIIS equations difficult to solve. | 当误差重叠矩阵最小元素的平方根小于 10 / -n / ，其中 / n / 为该选项的取值时，DIIS 过程将被中止。B 矩阵取值过小意味着它将接近奇异，使 DIIS 方程难以求解。 |
| `CC_DIIS_SIZE` | Specifies the maximum size of the DIIS space. / Recommendation: / Larger values involve larger amounts of disk storage. | 指定 DIIS 空间的最大大小。 / 建议： / 较大的取值需要更多的磁盘存储量。 |
| `CC_DIIS_START` | Iteration number when DIIS is turned on. Set to a large number to disable DIIS. / Recommendation: / Occasionally DIIS can cause optimized orbital coupled-cluster calculations to diverge through large orbital changes. If this is seen, DIIS should be disabled. | 启用 DIIS 的迭代序号。设为很大的数可禁用 DIIS。 / 建议： / 有时 DIIS 会因轨道变化过大而导致优化轨道的耦合簇计算发散。若出现这种情况，应禁用 DIIS。 |
| `CC_DIP` | Initializes a EOM-DIP-CCSD calculation | 启动 EOM-DIP-CCSD 计算 |
| `CC_DMAXITER` | Maximum number of iteration allowed for Davidson diagonalization procedure. / Recommendation: / Default is usually sufficient | Davidson 对角化过程允许的最大迭代次数。 / 建议： / 默认值通常已足够 |
| `CC_DMAXVECTORS` | Specifies maximum number of vectors in the subspace for the Davidson diagonalization. / Recommendation: / Larger values increase disk storage but accelerate and stabilize convergence. | 指定 Davidson 对角化子空间中的最大矢量数目。 / 建议： / 较大的取值会增加磁盘存储量，但可加速并稳定收敛。 |
| `CC_DOV_THRESH` | Specifies the minimum allowed values for the coupled-cluster energy denominators.  Smaller values are replaced by this constant during the early iterations only, so the final results are unaffected, but initial convergence is improved when the guess is poor. / OPTIONS: abcde Integer code is mapped t | 指定耦合簇能量分母所允许的最小值。仅在早期迭代中，较小的取值会被该常数替代，因此最终结果不受影响，但在初始猜测较差时可改善初期收敛。 / 选项：abcde 整数代码映射为 abc x 10^-de / 建议： / 建议： / 对于不收敛的耦合簇计算，可增大到 0.5 或 0.75。 |
| `CC_DO_CISDT` | Controls the calculation of full CISDT | 控制完整 CISDT 的计算 |
| `CC_DO_DISCONNECTED` | Determines whether disconnected terms included in the EOM-OD equations / Recommendation: / Inclusion of disconnected terms has very small effects and is not necessary. | 决定 EOM-OD 方程中是否包含非连通项 / 建议： / 包含非连通项的影响极小，并无必要。 |
| `CC_DO_DYSON` | Whether ground state Dyson orbitals will be calculated for EOM-IP/EA-CCSD calculations. / Recommendation: / none | 是否在 EOM-IP/EA-CCSD 计算中计算基态 Dyson 轨道。 / 建议： / 无 |
| `CC_DO_DYSON_EE` | Whether excited state Dyson orbitals will be calculated for EOM-IP/EA-CCSD calculations. / Recommendation: / none | 是否在 EOM-IP/EA-CCSD 计算中计算激发态 Dyson 轨道。 / 建议： / 无 |
| `CC_DO_TRIPLES` | This keyword initializes a EOM-CC(2,3) calculation. If {CC_IP_PROPER} is set then EOM-IP-CC(2,3) states are calculated. | 该关键词启动 EOM-CC(2,3) 计算。若设置了 {CC_IP_PROPER}，则计算 EOM-IP-CC(2,3) 态。 |
| `CC_DTHRESHOLD` | Specifies threshold for including a new expansion vector in the iterative Davidson diagonalization. Their norm must be above this threshold. / Recommendation: / Use default unless converge problems are encountered. Should normally be set to the same values as CC_DCONVERGENCE, if convergence problems | 指定在迭代 Davidson 对角化中纳入新展开矢量的阈值。其范数必须高于该阈值。 / 建议： / 除非遇到收敛问题，否则使用默认值。通常应设为与 CC_DCONVERGENCE 相同的值；若出现收敛问题，可尝试设为小于 CC_DCONVERGENCE 的值。 |
| `CC_EA` | If TRUE, calculates EOM-EA-CCSD excitation energies and properties using the diffuse orbital trick. A very diffuse orbital is added to the basis set, excitations from which correspond to electron attachment. | 若为 TRUE，则使用弥散轨道技巧计算 EOM-EA-CCSD 的激发能与性质。基组中会加入一条极弥散的轨道，从该轨道出发的激发对应于电子附着。 |
| `CC_EOM_AMPLITUDE_RESPONSE` | If set to TRUE, adds amplitude response terms to one-particle and two-particle EOM-CCSD density matrices before calculation of properties. CC_EXSTATES_PROP must be set to TRUE. / Recommendation: / The cost is always about the cost of an analytic gradient calculation for each state, independent of wh | 若设为 TRUE，则在计算性质之前把振幅响应项加入 EOM-CCSD 的单粒子和双粒子密度矩阵。CC_EXSTATES_PROP 必须设为 TRUE。 / 建议： / 其开销始终约为每个态一次解析梯度计算的开销，与是否请求双粒子性质无关。此外，只加入振幅响应项而不加入轨道响应项，几乎不可能改善性质的质量。不过它可用于调试目的。 |
| `CC_EOM_FULL_RESPONSE` | If set to TRUE, adds both amplitude and orbital response terms to one- and two-particle EOM-CCSD density matrices before calculation of the properties. CC_EXSTATES_PROP must be set to TRUE. If both CC_EOM_AMPL_RESP=TRUE and CC_EOM_FULL_RESP=TRUE, the CC_EOM_AMPL_RESP=TRUE will be ignored. / Recommen | 若设为 TRUE，则在计算性质之前同时把振幅响应项和轨道响应项加入 EOM-CCSD 的单粒子与双粒子密度矩阵。CC_EXSTATES_PROP 必须设为 TRUE。若同时设置 CC_EOM_AMPL_RESP=TRUE 和 CC_EOM_FULL_RESP=TRUE，则 CC_EOM_AMPL_RESP=TRUE 将被忽略。 / 建议： / 完全响应性质计算的开销约为每个态一次解析梯度计算的开销。加入完全响应项可提高所计算性质的质量，但这通常是一个很小却昂贵的修正。仅在确实需要高精度性质时使用。 |
| `CC_EOM_PROP` | (in atomic units). Incompatible with JOBTYPE=FORCE, OPT, FREQ. / Recommendation: / Additional equations (EOM-CCSD equations for the left eigenvectors) need to be solved for properties, approximately doubling the cost of calculation for each irrep. Sometimes the equations for left and right eigenvect | （以原子单位表示）。与 JOBTYPE=FORCE、OPT、FREQ 不兼容。 / 建议： / 计算性质需要额外求解方程（EOM-CCSD 左本征矢量方程），这会使每个不可约表示的计算开销大致翻倍。有时左、右本征矢量的方程会收敛到不同的目标态集合。此时左、右矢量的同时迭代将发散，部分甚至全部目标态的性质可能不正确！该问题可通过改变所请求的态数目（由 CC_NLOWSPIN 和 CC_NHIGHSPIN 指定）或猜测矢量数目（CC_NGUESS_SINGLES）来解决。单粒子性质计算本身的开销很低。可用 CC_REFSYM 和 CC_STATE_DERIV 指定态，并请求 NBO=TRUE 与  |
| `CC_EOM_PROPERTIES` | (in atomic units). Incompatible with JOBTYPE=FORCE, OPT, FREQ. / Recommendation: / Additional equations (EOM-CCSD equations for the left eigenvectors) need to be solved for properties, approximately doubling the cost of calculation for each irrep. Sometimes the equations for left and right eigenvect | （以原子单位表示）。与 JOBTYPE=FORCE、OPT、FREQ 不兼容。 / 建议： / 计算性质需要额外求解方程（EOM-CCSD 左本征矢量方程），这会使每个不可约表示的计算开销大致翻倍。有时左、右本征矢量的方程会收敛到不同的目标态集合。此时左、右矢量的同时迭代将发散，部分甚至全部目标态的性质可能不正确！该问题可通过改变所请求的态数目（由 CC_NLOWSPIN 和 CC_NHIGHSPIN 指定）或猜测矢量数目（CC_NGUESS_SINGLES）来解决。单粒子性质计算本身的开销很低。可用 CC_REFSYM 和 CC_STATE_DERIV 指定态，并请求 NBO=TRUE 与  |
| `CC_EOM_PROP_TE` | Request for calculation of non-relaxed two-particle EOM-CC properties. The two-particle properties currently include / . The one-particle properties also will be calculated, since the additional cost of the one-particle properties calculation / is inferior compared to the cost of / . The variable CC | 请求计算非弛豫的双粒子 EOM-CC 性质。目前双粒子性质包括 / 。单粒子性质也会被计算，因为单粒子性质计算的额外开销 / 相比要小得多。 / 变量 CC_EOM_PROP 也必须设为 TRUE。此外，也可用 CC_CALC_SSQ 请求 / 计算。 |
| `CC_EOM_STATE_TO_OPT` | Specifies which state to optimize. / [i,j] optimize the jth state of the ith irrep. | 指定要优化的态。 / [i,j] 优化第 i 个不可约表示中的第 j 个态。 |
| `CC_EOM_TRANSITION_PROPERTIES` | Whether or not the transition dipole moment (in atomic units) and oscillator strength for the EOM-CCSD target states will be calculated. By default, the transition dipole moment is calculated between the CCSD reference and the EOM-CCSD target states. In order to calculate transition dipole moment be | 是否计算 EOM-CCSD 目标态的跃迁偶极矩（以原子单位表示）和振子强度。默认情况下，跃迁偶极矩在 CCSD 参考态与 EOM-CCSD 目标态之间计算。若要计算一组 EOM-CCSD 态与另一个 EOM-CCSD 态之间的跃迁偶极矩，必须为该态指定 CC_REFSYM 和 CC_STATE_DERIV。 / 建议： / 计算跃迁性质需要额外求解方程（左 EOM-CCSD 本征矢量方程；若请求 CCSD 参考态与 EOM-CCSD 目标态之间的跃迁性质，还需 lambda CCSD 方程），这会使计算开销大致翻倍。跃迁性质计算本身的开销很低。 |
| `CC_EOM_TRANS_PROP` | Whether or not the transition dipole moment (in atomic units) and oscillator strength for the EOM-CCSD target states will be calculated. By default, the transition dipole moment is calculated between the CCSD reference and the EOM-CCSD target states. In order to calculate transition dipole moment be | 是否计算 EOM-CCSD 目标态的跃迁偶极矩（以原子单位表示）和振子强度。默认情况下，跃迁偶极矩在 CCSD 参考态与 EOM-CCSD 目标态之间计算。若要计算一组 EOM-CCSD 态与另一个 EOM-CCSD 态之间的跃迁偶极矩，必须为该态指定 CC_REFSYM 和 CC_STATE_DERIV。 / 建议： / 计算跃迁性质需要额外求解方程（左 EOM-CCSD 本征矢量方程；若请求 CCSD 参考态与 EOM-CCSD 目标态之间的跃迁性质，还需 lambda CCSD 方程），这会使计算开销大致翻倍。跃迁性质计算本身的开销很低。 |
| `CC_EOM_TWO_PARTICLE_PROPERTIES` | Request for calculation of non-relaxed two-particle EOM-CCSD target state properties.  The two-part properties currently include / . The one-particle properties will also be calculated since the additional cost of these is small in comparison.  The vairable CC_EXSTATES_PROP must also be set. / Recom | 请求计算非弛豫的 EOM-CCSD 目标态双粒子性质。目前双粒子性质包括 / 。单粒子性质也会被计算，因为其额外开销相比之下很小。变量 CC_EXSTATES_PROP 也必须设置。 / 建议 / 双粒子性质的计算开销极高，因为需要计算并使用双粒子密度矩阵（其开销约为每个态一次解析梯度计算的开销）。 |
| `CC_FULLRESPONSE` | Fully relaxed properties (including orbital relaxation terms) will be computed. The variable CC EOM PROP must be also set to TRUE. / Recommendation: / Not available for non-UHF/RHF references. Only available for EOM/CI methods for which analytic gradients are available. | 将计算完全弛豫的性质（包含轨道弛豫项）。变量 CC EOM PROP 也必须设为 TRUE。 / 建议： / 不适用于非 UHF/RHF 参考态。仅适用于具有解析梯度的 EOM/CI 方法。 |
| `CC_FULL_RESPONSE` | If set to TRUE, adds both amplitude and orbital response terms to one- and two-particle CCSD density matrices before calculation of the properties. CC_PROP must be set to TRUE. If both CC_AMPL_RESP=TRUE and CC_FULL_RESP=TRUE, the CC_AMPL_RESP=TRUE will be ignored. / Recommendation: / The cost for th | 若设为 TRUE，则在计算性质之前同时把振幅响应项和轨道响应项加入 CCSD 的单粒子与双粒子密度矩阵。CC_PROP 必须设为 TRUE。若同时设置 CC_AMPL_RESP=TRUE 和 CC_FULL_RESP=TRUE，则 CC_AMPL_RESP=TRUE 将被忽略。 / 建议： / 完全响应性质计算的开销约为一次解析梯度计算的开销。加入完全响应项可提高所计算性质的质量，但这通常是一个很小却昂贵的修正。仅在确实需要高精度性质时使用。 |
| `CC_HESSIAN_THRESH` | Minimum alloed value for the orbital Hessian.  Smaller values are replaced with this constant. | 轨道海森矩阵允许取的最小值。更小的数值将被替换为此常数。 |
| `CC_HIGH_SPIN` | Sets the number of high-spin excited state roots to find.  Works only for singlet reference states and triplet excited states.  The default is to not look for any excited states.  Setting this to [ / i,j,k / ...] looks for / i / excited states in the first irrep, / j / states in the second irrep and | 设置要求解的高自旋激发态根数。仅适用于单重态参考态与三重态激发态。默认情况下不搜寻任何激发态。将其设为 [ / i,j,k / ...] 表示寻找 / i / 个第一个不可约表示中的激发态， / j / 个第二个不可约表示中的态，依此类推。 |
| `CC_INCL_CORE_CORR` | Whether to include the correlation contribution from frozen core orbitals in non iterative (2) corrections, such as OD(2) and CCSD(2). / Recommendation: / Use default unless no core-valence or core correlation is desired ( / e.g. / , for comparison with other methods or because the basis used cannot | 是否在非迭代 (2) 校正（如 OD(2) 与 CCSD(2)）中包含冻结芯轨道的相关贡献。 / 建议： / 使用默认值，除非不需要芯-价相关或芯相关（ / 例如 / ，即为了与其他方法比较，或所用基组无法描述芯相关）。 |
| `CC_IP` | If TRUE, calculates EOM-IP-CCSD excitation energies and properties using the diffuse orbital trick. A very diffuse orbital is added to the basis set, excitations to which correspond to ionization. CC_IP and CC_IP_PROPER keywords are mutually exclusive. | 若为真，使用弥散轨道技巧计算 EOM-IP-CCSD 激发能与性质。向基组中加入一条非常弥散的轨道，向其的激发即对应于电离。CC_IP 与 CC_IP_PROPER 关键词互斥。 |
| `CC_IP_FILTER` | If TRUE, filters the EOM-IP-CCSD amplitudes obtained using the diffuse orbital implementation, to be used in conjunction with CC_IP keyword. Helps with convergence. | 若为真，对用弥散轨道实现得到的 EOM-IP-CCSD 振幅进行过滤，需与 CC_IP 关键词配合使用。有助于收敛。 |
| `CC_IP_PROPER` | If TRUE, calculates proper EOM-IP-CCSD excitation energies and properties. This implementation does not use the diffuse orbital and is the recommended method of doing EOM-IP-CCSD calculations. CC_IP and CC_IP_PROPER keywords are mutually exclusive. | 若为真，计算正规的 EOM-IP-CCSD 激发能与性质。该实现不使用弥散轨道，是进行 EOM-IP-CCSD 计算的推荐方法。CC_IP 与 CC_IP_PROPER 关键词互斥。 |
| `CC_ITERATE_ON` | In active space calculations, use a mixed iteration procedure if the value is greater than 0.  Then if the RMS orbital gradient is larger than the value of CC_THETA_GRAD_THRESH, micro-iterations will be performed to converge the occupied-virtual mixing angles for the current active-space. The maximu | 在活性空间计算中，若取值大于 0 则使用混合迭代过程。此时若轨道梯度的均方根大于 CC_THETA_GRAD_THRESH 的取值，将执行微迭代以收敛当前活性空间的占据-虚轨道混合角。空间迭代的最大次数由本选项给出。<br><b>建议：</b> 对不收敛的活性空间计算可能有帮助 |
| `CC_ITERATE_OV` | In active space calculations, use a mixed iteration procedure if the value is greater than 0. Then, if the RMS orbital gradient is larger than the value of CC_THETA_GRAD_THRESH, micro-iterations will be performed to converge the occupied-virtual mixing angles for the current active space. The maximu | 在活性空间计算中，若取值大于 0 则使用混合迭代过程。此时若轨道梯度的均方根大于 CC_THETA_GRAD_THRESH 的取值，将执行微迭代以收敛当前活性空间的占据-虚轨道混合角。此类迭代的最大次数由本选项给出。 / 建议： / 对不收敛的活性空间计算可能有帮助。 |
| `CC_LOW_SPIN` | Sets the number of low-spin excited state roots to find.  In the cas of closed-shell reference states, excited singlet states will be found.  For any other reference state all states (e.g. both singlet and triplet) will be calculated.  The default is to not look for any excited states.  Setting this | 设置要求解的低自旋激发态根数。对于闭壳层参考态，将得到激发单重态。对于其他任何参考态，将计算所有态（例如单重态与三重态）。默认情况下不搜寻任何激发态。将其设为 [ / i,j,k / ...] 表示寻找 / i / 个第一个不可约表示中的激发态， / j / 个第二个不可约表示中的态，依此类推。 |
| `CC_MAX_ITER` | Maximum number of iterations to optimize the coupled-cluster energy. | 优化耦合簇能量的最大迭代次数。 |
| `CC_MEMORY` | Specifies the maximum size, in Mb, of the buffers for in-core storage of block-tensors in CCMAN and CCMAN2. | 指定 CCMAN 与 CCMAN2 中用于块张量内存存储的缓冲区最大大小，单位为 Mb。 |
| `CC_MP2NO_GRAD` | If CC_MP2NO_GUESS is TRUE, what kind of one-particle density matrix is used to make the guess orbitals? / Recommendation: / The two definitions give generally similar performance. | 若 CC_MP2NO_GUESS 为真，用哪一种单粒子密度矩阵来构造猜测轨道？ / 建议： / 两种定义的性能大体相近。 |
| `CC_MP2NO_GUESS` | Will guess orbitals be natural orbitals of the MP1 wavefunction? Alternatively, it is possible to use an effective one-particle density matrix to define the natural orbitals. | 是否用 MP1 波函数的自然轨道作为猜测轨道？另一种做法是用有效单粒子密度矩阵来定义自然轨道。 |
| `CC_NGUESS_DOUBLES` | Specifies number of excited state guess vectors which are double excitations. / Recommendation: / This should be set to the expected number of doubly excited states (see also EOM_PRECONV_DOUBLES), otherwise they may not be found. | 指定为双激发的激发态猜测向量数目。 / 建议： / 应设为预期的双激发态数目（另见 EOM_PRECONV_DOUBLES），否则可能无法找到这些态。 |
| `CC_NGUESS_SINGLES` | Specifies number of excited state guess vectors which are single excitations. / Recommendation: / Should be greater or equal than the number of excited states requested. | 指定为单激发的激发态猜测向量数目。 / 建议： / 应大于或等于所请求的激发态数目。 |
| `CC_ORBS_PER_BLOCK` | Specifies target (and maximum) size of blocks in orbital space. | 指定轨道空间中块的目标（及最大）大小。 |
| `CC_PRECONVERGE_DOUBLES` | When TRUE, doubly-excited vectors are converged prior to a full excited states calculation. / Recommendation: / Occasionally necessary to ensure a doubly excited state is found. | 若为真，在进行完整的激发态计算之前先收敛双激发向量。 / 建议： / 有时为确保找到双激发态而必需。 |
| `CC_PRECONVERGE_FZ` | In active space methods, whether to preconverge other wavefunction variables for fixed initial guess of active space. / Recommendation: | 在活性空间方法中，是否在固定活性空间初始猜测的情况下预收敛其他波函数变量。 / 建议： |
| `CC_PRECONVERGE_SD` | Solves the EOM-CCSD equations, prints energies, then uses EOM-CCSD vectors as initial vectors in EOM-CC(2,3). Very convenient for calculations using energy additivity schemes. / Recommendation: / Turning this option on is recommended | 求解 EOM-CCSD 方程，输出能量，然后将 EOM-CCSD 向量用作 EOM-CC(2,3) 的初始向量。对采用能量加和方案的计算非常方便。 / 建议： / 建议开启此选项 |
| `CC_PRECONVERGE_SINGLES` | When TRUE, singly-excited vectors are converged prior to a full excited states calculation. | 若为真，在进行完整的激发态计算之前先收敛单激发向量。 |
| `CC_PRECONVERGE_T2Z` | Whether to pre-converge the cluster amplitudes before beginning orbital optimization in optimized orbital cluster methods. / Recommendation: / Experiment with this option in cases of convergence failure. | 在优化轨道簇方法中，是否在开始轨道优化之前预收敛簇振幅。 / 建议： / 收敛失败时可试用此选项。 |
| `CC_PRECONVERGE_T2Z_EACH` | Whether to pre-converge the cluster amplitudes before each change of the orbitals in optimized orbital coupled-cluster methods. The maximum number of iterations in this pre-convergence procedure is given by the value of this parameter. / Recommendation: / A very slow last resort option for jobs that | 在优化轨道耦合簇方法中，是否在每次改变轨道之前预收敛簇振幅。该预收敛过程的最大迭代次数由本参数的取值给出。 / 建议： / 一种非常慢的权宜选项，适用于无法收敛的任务。 |
| `CC_PRECONV_DOUBLES` | When TRUE, doubly-excited vectors are converged prior to a full excited states calculation. / Recommendation: / Occasionally necessary to ensure a doubly excited state is found. | 若为真，在进行完整的激发态计算之前先收敛双激发向量。 / 建议： / 有时为确保找到双激发态而必需。 |
| `CC_PRECONV_SD` | Solves the EOM-CCSD equations, prints energies, then uses EOM-CCSD vectors  as initial vectors in EOM-CC(2,3). Very convenient for calculations using energy additivity schemes.<br><b>Recommendation:</b> Turning this option on is recommended | 求解 EOM-CCSD 方程，输出能量，然后将 EOM-CCSD 向量用作 EOM-CC(2,3) 的初始向量。对采用能量加和方案的计算非常方便。<br><b>建议：</b> 建议开启此选项 |
| `CC_PRECONV_SINGLES` | When TRUE, singly-excited vectors are converged prior to a full excited states calculation. | 若为真，在进行完整的激发态计算之前先收敛单激发向量。 |
| `CC_PRECONV_T2Z` | Whether to pre-converge the cluster amplitudes before beginning orbital optimization in optimized orbital cluster methods. / Recommendation: / Experiment with this option in cases of convergence failure. | 在优化轨道簇方法中，是否在开始轨道优化之前预收敛簇振幅。 / 建议： / 收敛失败时可试用此选项。 |
| `CC_PRECONV_T2Z_EACH` | Whether to pre-converge the cluster amplitudes before each change of the orbitals in optimized orbital coupled-cluster methods. The maximum number of iterations in this pre-convergence procedure is given by the value of this parameter. / Recommendation: / A very slow last resort option for jobs that | 在优化轨道耦合簇方法中，是否在每次改变轨道之前预收敛簇振幅。该预收敛过程的最大迭代次数由本参数的取值给出。 / 建议： / 一种非常慢的权宜选项，适用于无法收敛的任务。 |
| `CC_PRINT` | Controls the output from post-MP2 coupled-cluster module of QChem / Recommendation: / Increase if you need more output and don't like trees | 控制 QChem 中后 MP2 耦合簇模块的输出 / 建议： / 若需要更多输出且不喜欢树木，可增大此值 |
| `CC_PROP` | Whether or not the non-relaxed (expectation value) one-particle CCSD properties will be calculated. The properties currently include permanent dipole moment, the second moments / 2 / , / 2 / , and / 2 / of electron density, and the total / 2 / = / 2 / + / 2 / + / 2 / (in atomic units). Incompatible  | 是否计算非弛豫（期望值）单粒子 CCSD 性质。目前这些性质包括永久偶极矩、二阶矩 / 2 / ， / 2 / ，以及 / 2 / 的电子密度，以及总的 / 2 / = / 2 / + / 2 / + / 2 / （以原子单位表示）。与 JOBTYPE=FORCE、OPT、FREQ 不兼容。 / 建议： / 计算性质需额外求解方程（lambda CCSD 方程），其开销与 CCSD 方程大致相当。若不需要性质，使用默认值。性质计算本身的开销很低。通过指定 NBO=TRUE、CC_PROP=TRUE 与 JOBTYPE=FORCE，可用 NBO 程序包分析 CCSD 单粒子密度。 |
| `CC_PROPERTIES` | Whether or not the non-relaxed (expectation value) one-particle CCSD properties will be calculated. The properties currently include permanent dipole moment, the second moments / 2 / , / 2 / , and / 2 / of electron density, and the total / 2 / = / 2 / + / 2 / + / 2 / (in atomic units). Incompatible  | 是否计算非弛豫（期望值）单粒子 CCSD 性质。目前这些性质包括永久偶极矩、二阶矩 / 2 / ， / 2 / ，以及 / 2 / 的电子密度，以及总的 / 2 / = / 2 / + / 2 / + / 2 / （以原子单位表示）。与 JOBTYPE=FORCE、OPT、FREQ 不兼容。 / 建议： / 计算性质需额外求解方程（lambda CCSD 方程），其开销与 CCSD 方程大致相当。若不需要性质，使用默认值。性质计算本身的开销很低。通过指定 NBO=TRUE、CC_PROP=TRUE 与 JOBTYPE=FORCE，可用 NBO 程序包分析 CCSD 单粒子密度。 |
| `CC_QCCD_THETA_SWITCH` | QCCD calculations switch from OD to QCCD when the rotation gradient is below this threshold [10 / -n / ] | 当旋转梯度低于该阈值时，QCCD 计算由 OD 切换到 QCCD [10 / -n / ] |
| `CC_REFERENCE_SYMMETRY` | Together with CC_STATE_DERIV, selects which EOM state is to be considered for optimization or property calculations. When transition properties are requested, the transition properties will be calculated between this state and all other EOM states. | 与 CC_STATE_DERIV 配合，选择将哪个 EOM 态用于优化或性质计算。若请求跃迁性质，则会在该态与所有其他 EOM 态之间计算跃迁性质。 |
| `CC_REFSYM` | Together with CC_STATE_DERIV, selects which EOM state is to be considered for optimization or property calculations. When transition properties are requested, the transition properties will be calculated between this state and all other EOM states. | 与 CC_STATE_DERIV 配合，选择将哪个 EOM 态用于优化或性质计算。若请求跃迁性质，则会在该态与所有其他 EOM 态之间计算跃迁性质。 |
| `CC_REF_PROP` | (in atomic units). Incompatible with JOBTYPE=FORCE, OPT, FREQ. / Recommendation: / Additional equations need to be solved (lambda CCSD equations) for properties with the cost approximately the same as CCSD equations. Use default if you do not need properties. The cost of the properties calculation i | （以原子单位表示）。与 JOBTYPE=FORCE、OPT、FREQ 不兼容。 / 建议： / 计算性质需额外求解方程（lambda CCSD 方程），其开销与 CCSD 方程大致相当。若不需要性质，使用默认值。性质计算本身的开销很低。通过指定 NBO=TRUE、CC_PROP=TRUE 与 JOBTYPE=FORCE，可用 NBO 程序包分析 CCSD 单粒子密度。 |
| `CC_REF_PROP_TE` | Request for calculation of non-relaxed two-particle CCSD properties. The two-particle properties currently include . The one-particle properties also will be calculated, since the additional cost of the one-particle properties calculation is / inferior compared to the cost of . The variable CC_REF_P | 请求计算非弛豫双粒子 CCSD 性质。目前双粒子性质包括 。由于在这里进行比较时，单粒子性质计算的额外开销远小于 / 的计算开销，因此单粒子性质也会一并计算。变量 CC_REF_PROP 也必须设为 TRUE。 |
| `CC_RESET_THETA` | The reference MO coefficient matrix is reset every n iterations to help   overcome problems associated with the theta metric as theta becomes large.  | 参考分子轨道系数矩阵每 n 次迭代重置一次，以帮助克服 theta 变大时与 theta 度量相关的问题。 |
| `CC_RESTART` | Allows an optimized orbital coupled cluster calculation to begin with an initial guess for the orbital transformation matrix U other than the unit vector. The scratch file from a previous run must be available for the U matrix to be read successfully. / Recommendation: / Useful for restarting a job  | 允许优化轨道耦合簇计算以非单位向量的轨道变换矩阵 U 作为初始猜测开始。前一运行的临时文件必须存在，才能成功读入 U 矩阵。 / 建议： / 若已保存文件，对重启未收敛的任务很有用。 |
| `CC_RESTART_NO_SCF` | Should an optimized orbital coupled cluster calculation begin with optimized orbitals from a previous calculation? When TRUE, molecular orbitals are initially orthogonalized, and CC_PRECONV_T2Z and CC_CANONIZE are set to TRUE while other guess options are set to FALSE. | 优化轨道耦合簇计算是否以先前计算得到的优化轨道开始？若为真，分子轨道首先进行正交化，且 CC_PRECONV_T2Z 与 CC_CANONIZE 设为 TRUE，其他猜测选项则设为 FALSE。 |
| `CC_RESTR_AMPL` | Controls the restriction on amplitudes is there are restricted orbitals | 控制存在限制性轨道时对振幅施加的限制 |
| `CC_RESTR_TRIPLES` | Controls which space the triples correction is computed in | 控制在哪个空间中计算三重激发校正 |
| `CC_REST_OCC` | Sets the number of restricted occupied orbitals including frozen occupied  orbitals. | 设置限制性占据轨道（含冻结占据轨道）的数目。 |
| `CC_REST_VIR` | Sets the number of restricted virtual orbitals including frozen virtual  orbitals. | 设置限制性虚轨道（含冻结虚轨道）的数目。 |
| `CC_SD_3` | This keyword initializes calculation of non-iterative triples corrections (fT) and (dT) for EE or SF after the EOM-CCSD is done. | 该关键词在 EOM-CCSD 完成后启动 EE 或 SF 的非迭代三重校正 (fT) 与 (dT) 计算。 |
| `CC_SPIN_FLIP` | Selects whether do perform a standard excited state calculation, or a spin-flip calculation. Spin multiplicity should be set to 3 for systems with an even number of electrons, and 4 for systems with an odd number of electrons. | 选择是进行标准的激发态计算，还是自旋翻转计算。对于电子数为偶数的体系，自旋多重度应设为 3；对于电子数为奇数的体系，应设为 4。 |
| `CC_SPIN_FLIP_MS` | This option is only used in EOM-SF using quintet references and including triple excitations. By default, SF flips the spin of one α electron. One can ask to flip the spins of two α electrons by specifying CC_SPIN_FLIP_MS = 1 / Recommendation: / This option can be useful when starting from quintet r | 该选项仅用于以五重态为参考态且包含三重激发的 EOM-SF 中。默认情况下，SF 翻转一个 α 电子的自旋。可通过指定 CC_SPIN_FLIP_MS = 1 翻转两个 α 电子的自旋 / 建议： / 从五重态参考出发时该选项可能有用，尽管这在 EOM-SF 中并不常见。 |
| `CC_STATE_DERIV` | Selects which EOM or CIS(D) state is to be considered for optimization or property calculations. | 选择将哪个 EOM 或 CIS(D) 态用于优化或性质计算。 |
| `CC_STATE_DERIVATIVE` | Selects which EOM or CIS(D) state is to be considered for optimization or property calculations. | 选择将哪个 EOM 或 CIS(D) 态用于优化或性质计算。 |
| `CC_STATE_TO_OPT` | Species which state to optimize. / [i,j] optimize the jth state of the ith irrep. | 指定要优化哪个态。 / [i,j] 优化第 i 个不可约表示中的第 j 个态。 |
| `CC_SYMMETRY` | Controls the use of symmetry in coupled-cluster calculations / Recommendation: / It is automatically turned off for any finite difference calculations, / e.g. / second derivatives. | 控制在耦合簇计算中使用对称性 / 建议： / 对任何有限差分计算会自动关闭， / 例如 / 二阶导数。 |
| `CC_THETA_CONV` | Convergence criterion on the RMS difference between successive sets of orbital rotation angles [10 / -n / ]. / Recommendation: / Use default | 相邻两组轨道旋转角之间均方根差的收敛判据 [10 / -n / ]。 / 建议： / 使用默认值 |
| `CC_THETA_GRAD_CONV` | Convergence desired on the RMS gradient of the energy with respect to orbital rotation angles [10 / -n / ]. / Recommendation: / Use default | 能量对轨道旋转角的均方根梯度所需的收敛限 [10 / -n / ]。 / 建议： / 使用默认值 |
| `CC_THETA_GRAD_THRESH` | RMS orbital gradient threshold [10<sup>-n</sup>] above which mixed iterations  are performed in active space calculations if CC_ITERATE_OV is  TRUE.<br><b>Recommendation:</b> Can be made smaller if convergence difficulties are encountered. | 轨道梯度均方根阈值 [10<sup>-n</sup>]，在活性空间计算中若 CC_ITERATE_OV 为真且梯度高于该阈值，则执行混合迭代。<br><b>建议：</b> 若遇到收敛困难可将其调小。 |
| `CC_THETA_STEPSIZE` | Scale factor for the orbital rotation step size. The optimal rotation steps should be approximately equal to the gradient vector. / Recommendation: / Try a smaller value in cases of poor convergence and very large orbital gradients. For example, a value of 01001 translates to 0.1 | 轨道旋转步长的比例因子。最优旋转步长应大致等于梯度向量。 / 建议： / 收敛不佳且轨道梯度极大时，可尝试取更小的值。例如，取值 01001 相当于 0.1 |
| `CC_TMPBUFFSIZE` | Maximum size, in Mb, of additional buffers for temporary arrays used to work with individual blocks or matrices.<br><b>Recommendation:</b> Should not be smaller than the size of the largest possible block. | 用于处理单个块或矩阵的临时数组所用附加缓冲区的最大大小，单位为 Mb。<br><b>建议：</b> 不应小于可能的最大块的大小。 |
| `CC_TRANS_PROP` | Whether or not the transition dipole moment (in atomic units) and oscillator strength for the EOM-CCSD target states will be calculated. By default, the transition dipole moment is calculated between the CCSD reference and the EOM-CCSD target states. In order to calculate transition dipole moment be | 是否计算 EOM-CCSD 目标态的跃迁偶极矩（以原子单位表示）与振子强度。默认情况下，跃迁偶极矩在 CCSD 参考态与 EOM-CCSD 目标态之间计算。若要计算一组 EOM-CCSD 态与另一个 EOM-CCSD 态之间的跃迁偶极矩，必须为该态指定 CC_STATE_TO_OPT。 |
| `CC_TWO_PARTICLE_PROPERTIES` | Request for calculation of non-relaxed two-particle CCSD properties. The two-particle properties currently include / . The variable CC_PROPERTIES must be also set to TRUE. / Recommendation: / The two-particle properties are extremely computationally expensive, since they require calculation and use  | 请求计算非弛豫双粒子 CCSD 性质。目前双粒子性质包括 / 。变量 CC_PROPERTIES 也必须设为 TRUE。 / 建议： / 双粒子性质的计算开销极大，因为需要计算并使用双粒子密度矩阵（其开销与解析梯度计算大致相当）。除非确实需要，否则不要请求双粒子性质。 |
| `CC_T_CONV` |  |  |
| `CDFT` | Initiates a constrained DFT calculation / Recommendation: / Set to TRUE if a Constrained DFT calculation is desired. | 启动约束性 DFT 计算 / 建议： / 若需要进行约束性 DFT 计算，设为 TRUE。 |
| `CDFT_POSTDIIS` | Controls whether the connstraint is enforced after DIIS extrapolation. / Recommentation: / Use default unless convergence problems arise, in which case it may be benificial to turn this option off.  If selected, energies should be variational after the first iteration. | 控制是否在 DIIS 外推之后强制施加约束。 / 建议： / 使用默认值，除非出现收敛问题；此时关闭此选项可能有帮助。若启用，第一次迭代之后能量应是变分的。 |
| `CDFT_PREDIIS` | Controls wheter the constraint is enforced before DIIS extrapolation. / Recommendation: / Use default unless problems arise, in which case it might be beneficial to turn this option on. | 控制是否在 DIIS 外推之前强制施加约束。 / 建议： / 使用默认值，除非出现问题；此时开启此选项可能有帮助。 |
| `CDFT_THRESH` | Determines how tightly the constraint must be satisfied. / Recommendation: / Use default unless problems occur. | 决定约束必须满足的严格程度。 / 建议： / 使用默认值，除非出现问题。 |
| `CD_ALGORITHM` | Determines the algorithm for MP2 integral transformations. / Recommendation: / Semi-direct is usually most efficient, and will normally be chosen by default. | 决定 MP2 积分变换所用的算法。 / 建议： / 半直接算法通常效率最高，一般会被默认选用。 |
| `CFMM_GRAIN` | Controls the number of lowest-level boxes in one dimension for CFMM. / Recommendation: / This is an expert option; either use the default, or use a value of 1 if CFMM is not desired. | 控制 CFMM 中一个维度上最低层盒子的数目。 / 建议： / 这是专家选项；要么使用默认值，要么在不需要 CFMM 时取值为 1。 |
| `CFMM_ORDER` | Controls the order of the multipole expansions in CFMM calculation. | 控制 CFMM 计算中多极展开的阶数。 |
| `CHELPG` | Controls the calculation of CHELPG charges. / Recommendation: / For large molecules there is some overhead associated with computing CHELPG charges, especially if the number of grid points is large. | 控制 CHELPG 电荷的计算。 / 建议： / 对于大分子，计算 CHELPG 电荷会带来一定额外开销，尤其当格点数目很多时。 |
| `CHEMSOL_READ_VDW` | Controls the input of user-defined atomic radii for a ChemSol calculation. | 控制 ChemSol 计算中用户自定义原子半径的输入。 |
| `CHOLESKY_TOL` | Tolerance for the Cholesky decomposition of two-electron integrals. / Recommendation: / 2 - qualitative calculations / 3 - appropriate for most cases / 4 - quantatative (error / -6 / E / h / ) | 双电子积分 Cholesky 分解的容差。 / 建议： / 2 - 定性计算 / 3 - 适用于大多数情况 / 4 - 定量（误差 / -6 / E / h / ） |
| `CIS_AMPL_ANAL` | Perform additional analysis of CIS and TDDFT excitation amplitudes, including generation of natural transition orbitals, excited-state multipole moments, and Mulliken analysis of the excited state densities and particle/hole density matrices. | 对 CIS 与 TDDFT 激发振幅进行额外分析，包括生成自然跃迁轨道、激发态多极矩，以及对激发态密度与粒子/空穴密度矩阵做 Mulliken 分析。 |
| `CIS_CONVERGENCE` | CIS is considered converged when error is less than 10 / -CIS_CONVERGENCE | 当误差小于 10 时认为 CIS 已收敛 / -CIS_CONVERGENCE |
| `CIS_DYNAMIC_MEM` | Controls whether to use static or dynamic memory in CIS and TDDFT calculations. / Recommendation: / The default control requires static memory (MEM_STATIC) to hold a temporary array whose minimum size is / OV / × CIS_N_ROOTS. For a large calculation, one has to specify a large value for MEM_STATIC,  | 控制 CIS 与 TDDFT 计算中使用静态内存还是动态内存。 / 建议： / 默认方式要求静态内存（MEM_STATIC）容纳一个临时数组，其最小大小为 / OV / × CIS_N_ROOTS。对于大型计算，必须为 MEM_STATIC 指定很大的值，这并不推荐。因此，建议大型计算使用动态内存。 |
| `CIS_GUESS_DISK` | Read the CIS guess from disk (previous calculation) / Recommendation: / Requires a guess from previous calculation. | 从磁盘读取 CIS 初始猜测（先前计算） / 建议： / 需要有先前计算留下的猜测。 |
| `CIS_GUESS_DISK_TYPE` | Determines the type of guesses to be read from disk / Recommendation: / Must be specified if a CIS guess in to be read from disk. | 决定从磁盘读取的猜测类型 / 建议： / 若要从磁盘读取 CIS 猜测，必须指定此项。 |
| `CIS_MAX_CYCLES` | Maximum number of CIS iterative cycles allowed / Recommendation: / Default is usually sufficient. | 允许的最大 CIS 迭代轮数 / 建议： / 默认值通常已足够。 |
| `CIS_MOMENTS` | Controls the calculation of excited-date (CIS or TDDFT) multipole moments. | 控制激发态（CIS 或 TDDFT）多极矩的计算。 |
| `CIS_MULLIKEN` | Controls Mulliken and Löwdin population analyses for excited-state particle and hole density matrices. | 控制对激发态粒子与空穴密度矩阵做 Mulliken 和 Löwdin 布居分析。 |
| `CIS_N_ROOTS` | Sets the number of CI-Singles (CIS) excited state roots to find | 设置要求解的 CI-Singles（CIS）激发态根数 |
| `CIS_RAS` | Controls whether reduced single excitation space is used | 控制是否使用缩减的单激发空间 |
| `CIS_RAS_CUTOFF_OCCUPIED` | Specifies the occupied orbital cutoff | 指定占据轨道的截断值 |
| `CIS_RAS_CUTOFF_VIRTUAL` | Specifies the virtual orbital cutoff. | 指定虚轨道的截断值。 |
| `CIS_RAS_N_SOLUTE_ATOMS` | Specifies number of atoms or orbitals in / s / olute | 指定溶 / 质 / 中的原子或轨道数目 |
| `CIS_RAS_PRINT` | Selects whether or not to print additional output. | 选择是否打印额外输出。 |
| `CIS_RAS_TYPE` | Controls how reduced subspace is specified | 控制如何指定缩减的子空间 |
| `CIS_RELAXED_DENSITY` | Determines whether or not to use the relaxed CIS density for attachment/detachment density analysis | 决定是否使用弛豫的 CIS 密度进行附着/脱附密度分析 |
| `CIS_SINGLETS` | Solve for singlet excited states in RCIS calculations (ignored for UCIS) | 在 RCIS 计算中求解单重激发态（对 UCIS 忽略此项） |
| `CIS_STATE_DERIVATIVE` | Sets which CIS state to use for excited state optimizations and vibrational analysis. / Recommendation: / Check to see that the states do no change order during an optimization. | 设置激发态优化与振动分析所用的 CIS 态。 / 建议： / 检查在优化过程中各激发态的次序是否发生变化。 |
| `CIS_TRIPLETS` | Solve for triplet excited states in RCIS calculations (ignored for UCIS) | 在 RCIS 计算中求解三重态激发态（对 UCIS 则忽略此设置） |
| `COMPLEX` | Run an SCF calculation with complex MOs. / Recommendation: / GEN_SCFMAN must be active for this to take effect. | 使用复数分子轨道进行 SCF 计算。 / 建议： / 必须启用 GEN_SCFMAN 才能生效。 |
| `COMPLEX_MIX` | Mix a percentage of the real part of the HOMO to the imaginary part of the LUMO.  The mixing angle = π x COMPLEX_MIX/100.  This is analogous to SCF_GUESS_MIX for finding unrestricted solutions. | 将 HOMO 实部的一定比例混入 LUMO 的虚部。混合角 = π x COMPLEX_MIX/100。这类似于 SCF_GUESS_MIX 用于寻找非限制性解的做法。 |
| `CORE_CHARACTER` | Selects how the core orbitals are determined in the frozen-core approximation. / Recommendation: / Use default, unless performing calculations on molecules with heavy elements. | 选择在冻芯近似下如何确定芯轨道。 / 建议： / 使用默认值，除非对含重元素的分子进行计算。 |
| `CORE_CHARACTER_PRINT` | Determines the print level for the CORE_CHARACTER option. / Recommendation: / Use default, unless you are uncertain about what the core character is. | 确定 CORE_CHARACTER 选项的输出级别。 / 建议： / 使用默认值，除非你不确定芯轨道成分是什么。 |
| `CORRELATION` | Specifies the correlation level of theory, either DFT or wavefunction-based. / Recommendation: / Consult the literature and reviews for guidence | 指定相关能的理论级别，可为 DFT 或基于波函数的方法。 / 建议： / 查阅文献与综述以获得指导 |
| `CPSCF_NSEG` | Controls the number of segments used to calculate the CPSCF equations. / Recommendation: / Use default unless too much memory is requested.  Increasing this option reduces memory requirements. | 控制用于求解 CPSCF 方程的分段数目。 / 建议： / 除非申请的内存过多，否则使用默认值。增大该选项可降低内存需求。 |
| `DCPSCF_PERTNUM` | Specifies whether to do the perturbations all together. | 指定是否将所有微扰一并处理。 |
| `DCPSCF_PERTURBATIONS` | Specifies whether to do the perturbations all together. | 指定是否将所有微扰一并处理。 |
| `DEUTERATE` | Requests that all hydrogen atoms be replaces with deuterium. / Recommendation: / Replacing hydrogen atoms reduces the fastest vibrational frequencies by a factor of 1.4, which allow for a larger fictitious mass and time step in ELMD calculations. There is no reason to replace hydrogens in BOMD calcu | 要求将所有氢原子替换为氘。 / 建议： / 替换氢原子可使最快的振动频率降低 1.4 倍，从而在 ELMD 计算中允许采用更大的虚拟质量和时间步长。在 BOMD 计算中没有理由替换氢原子。 |
| `DFPT_EXCHANGE` | Specifies the secondary functional in a density functional perturbation theory (DFPT) calculation. | 在密度泛函微扰理论（DFPT）计算中指定次级泛函。 |
| `DFPT_XC_GRID` | Specifies the secondary grid in a density functional perturbation theory (DFPT) calculation. / Recommendation: / This should be at least as large as the XC_GRID | 在密度泛函微扰理论（DFPT）计算中指定次级网格。 / 建议： / 该网格至少应与 XC_GRID 一样大 |
| `DFTVDW_ALPHA1` | Parameter in XDM calculations with higher-order terms | 含高阶项的 XDM 计算中的参数 |
| `DFTVDW_ALPHA2` | Parameter in XDM calculations with higher-order terms. | 含高阶项的 XDM 计算中的参数。 |
| `DFTVDW_JOBNUMBER` | Basic vdW job control | vdW 任务的基本控制 |
| `DFTVDW_KAI` | Damping factor K for C6 only damping functions | 仅含 C6 的阻尼函数的阻尼因子 K |
| `DFTVDW_METHOD` | Selects the damping function used in XDM | 选择 XDM 中使用的阻尼函数 |
| `DFTVDW_MOL1NATOMS` | The number of atoms in the first monomer in a dimer calculation. | 二聚体计算中第一个单体所含的原子数。 |
| `DFTVDW_PRINT` | Controls the amount of output from the VDW code. | 控制 VDW 代码的输出量。 |
| `DFT_C` | Controls whether the DFT-C empirical BSSE correction should be added. | 控制是否添加 DFT-C 经验 BSSE 校正。 |
| `DFT_D` | Specifies what dispersion correction to use within a DFT calculation. / Recommendation: / Use the D3 option, which selects the empirical potential based on the density functional specified by the user. | 指定在 DFT 计算中使用何种色散校正。 / 建议： / 使用 D3 选项，它会根据用户指定的密度泛函选择经验势。 |
| `DFT_D3_S6` | Controls the strength of dispersion corrections, s / 6 / , in Grimme’s DFT-D3 method | 控制色散校正的强度 s / 6 / ，在 Grimme 的 DFT-D3 方法中 |
| `DFT_D3_S8` | Controls the strength of dispersion corrections, s / 8 / , in Grimme’s DFT-D3 method. | 控制色散校正的强度 s / 8 / ，在 Grimme 的 DFT-D3 方法中。 |
| `DFT_D3_SR6` | Controls the strength of dispersion corrections, s / r6 / , in the Grimme’s DFT-D3 method. | 控制色散校正的强度 s / r6 / ，在 Grimme 的 DFT-D3 方法中。 |
| `DFT_D_A` | Controls the strength of the dispersion corrections in the Chai-Head-Gordon scheme.   The default value should be apprpriate for most systems. | 控制 Chai-Head-Gordon 方案中色散校正的强度。默认值对大多数体系应该是合适的。 |
| `DH` | Controls the applicaiton of the double hybrid DFT scheme | 控制双杂化 DFT 方案的应用 |
| `DIIS_ERROR_METRIC` | Changes the DIIS convergence metric from the maximum to the RMS error. / Recommendation: / Use default, the maximum error provides a more reliable criterion. | 将 DIIS 收敛判据由最大误差改为均方根误差。 / 建议： / 使用默认值，最大误差可提供更可靠的判据。 |
| `DIIS_MAX_CYCLES` | The maximum number of DIIS iterations before switching to (geometric) direct minimization when SCF_ALGORITHM is DIIS_GDM or DIIS_DM. See also THRESH_DIIS_SWITCH. | 当 SCF_ALGORITHM 为 DIIS_GDM 或 DIIS_DM 时，切换到（几何）直接最小化之前所允许的最大 DIIS 迭代次数。另见 THRESH_DIIS_SWITCH。 |
| `DIIS_PRINT` | Controls the output from DIIS SCF optimization: / 1: Chosen method and DIIS coefficients / 2: Level 1 + print changes in multipole moments / 3: Level 2 + multipole moments / 4: Level 3 + extrapolated Fock matrices | 控制 DIIS SCF 优化的输出： / 1：所选方法与 DIIS 系数 / 2：级别 1 + 输出多极矩的变化 / 3：级别 2 + 多极矩 / 4：级别 3 + 外推的 Fock 矩阵 |
| `DIIS_SEPARATE_ERRVEC` | Controls the optimization of DIIS error vectors in unrestricted caclulations.  When using DIIS in QChem a convenient optimization for unrestricted calculations is to sume the alpha and beta error vectors into a single vector which is used for extrapolation.  This is often extremely effective, but in | 控制非限制性计算中 DIIS 误差向量的优化。在 QChem 中使用 DIIS 时，对非限制性计算的一种便捷优化是把 alpha 与 beta 误差向量相加成一个向量，用于外推。这通常非常有效，但在某些存在对称性破缺的病态体系中，可能导致检出假解，即误差向量的 alpha 与 beta 分量恰好相消，使 DIIS 误差为零。 / 建议： / 虽然这种情况极为罕见， / 但若怀疑发生相消，可设为 TRUE 进行检查。 |
| `DIIS_SUBSPACE_SIZE` | Controls the size of the DIIS and/or RCA subspace during the SCF. | 控制 SCF 过程中 DIIS 和/或 RCA 子空间的大小。 |
| `DIIS_SWITCH_THRESH` | The threshold for switching between DIIS extrapolation and direct minimization of the SCF energy is 10 / -THRESH_DIIS_SWITCH / when SCF_ALGORITHM is DIIS_GDM or DIIS_DM. See also MAX_DIIS_MAX_CYCLES | 当 SCF_ALGORITHM 为 DIIS_GDM 或 DIIS_DM 时，在 DIIS 外推与 SCF 能量直接最小化之间切换的阈值为 10 / -THRESH_DIIS_SWITCH / 。另见 MAX_DIIS_MAX_CYCLES |
| `DIP_SINGLETS` | Sets the number of singlet DIP roots to find. Works only for closed-shell references. / [i, j, k...] Find i DIP singlet states in the first irrep, j states in the second irrep etc. | 设置要查找的单重态 DIP 根数。仅适用于闭壳层参考态。 / [i, j, k...] 在第一个不可约表示中查找 i 个 DIP 单重态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `DIP_STATES` | Sets the number of DIP roots to find. For closed-shell reference, defaults into EOM_DIP_SINGLETS. For open-shell references, speccies all low-lying states. / [i, j, k...] Find i DIP states in the first irrep, j states in the second irrep etc. | 设置要查找的 DIP 根数。对于闭壳层参考态，默认归入 EOM_DIP_SINGLETS。对于开壳层参考态，指定所有低能态。 / [i, j, k...] 在第一个不可约表示中查找 i 个 DIP 态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `DIP_TRIPLETS` | Sets the number of triplet DIP roots to find. Works only for closed-shell references. / [i, j, k...] Find i DIP triplet states in the first irrep, j states in the second irrep etc. | 设置要查找的三重态 DIP 根数。仅适用于闭壳层参考态。 / [i, j, k...] 在第一个不可约表示中查找 i 个 DIP 三重态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `DIRECT_RI` | Controls the use of RI and Cholesky integrals in conventional (undecomposed) form. / Recommendation: / By default, all integrals are used in decomposed format allowing significant reduction of memory use.  If all integrals are transformed back (TRUE option) no memory reduction is achieved and decomp | 控制以常规（未分解）形式使用 RI 与 Cholesky 积分。 / 建议： / 默认情况下，所有积分均以分解格式使用，可显著减少内存占用。若把所有积分都变换回去（TRUE 选项），则无法减少内存占用，还会引入分解误差。不过积分变换的速度会明显加快，且可使用常规的 CC/EOM 算法。 |
| `DIRECT_SCF` | Controls direct SCF, the default is determined by the program. / Recommendation: / Use default; direct SCF switches off in-core integrals. | 控制直接 SCF，默认值由程序确定。 / 建议： / 使用默认值；直接 SCF 会关闭内存常驻积分。 |
| `DMA` | Specifies whether to perform Distributed Multipole Analysis. | 指定是否执行分布多极分析。 |
| `DMA_MIDPOINTS` | Specifies whether to include bond midpoints in the DMA expansion. | 指定是否在 DMA 展开中包含键的中点。 |
| `DSCF_CONVERGENCE_LEVEL_1` | Sets the convergence criterion for the level-1 iterations. This preconditions the density for the level-2 calculation, and does not include any two-electron integrals. / Recommendation: / The criterion for level-1 convergence must be less than or equal to the level-2 criterion, otherwise the D-CPSCF | 设置第 1 级迭代的收敛判据。它为第 2 级计算预处理密度，且不包含任何双电子积分。 / 建议： / 第 1 级收敛判据必须小于或等于第 2 级判据，否则 D-CPSCF 将不收敛。 |
| `DSCF_CONVERGENCE_LEVEL_2` | Sets the convergence criterion for the level-2 iterations. | 设置第 2 级迭代的收敛判据。 |
| `DSCF_DIIS_SUBSPACE` | Specifies the number of matrices to use in the DIIS extrapolation in the D-CPSCF. / Recommendation: / Use the default. | 指定 D-CPSCF 中 DIIS 外推所用的矩阵数目。 / 建议： / 使用默认值。 |
| `DSCF_MAX_CYCLES_LEVEL_1` | Sets the maximum number of level-1 iterations. / Recommendation: / Use default. | 设置第 1 级迭代的最大次数。 / 建议： / 使用默认值。 |
| `DSCF_MAX_CYCLES_LEVEL_2` | Sets the maximum number of level-2 iterations. / Recommendation: / Use default. | 设置第 2 级迭代的最大次数。 / 建议： / 使用默认值。 |
| `DSF_STATES` | Sets the number of double spin-ﬂip target states roots to ﬁnd. / [ / i, j, k . . . / ] Find / i / SF states in the ﬁrst irrep, / j / states in the second irrep etc. | 设置要查找的双自旋翻转目标态根数。 / [ / i, j, k . . . / ] 查找 / i / 个 SF 态于第一个不可约表示， / j / 个态于第二个不可约表示，依此类推。 |
| `DUAL_BASIS_ENERGY` | Activates dual-basis SCF (HF or DFT) energy correction. / Recommendation: / Use Dual-Basis to capture large-basis effects at smaller basis cost. Particularly useful with RI-MP2, in which HF often dominates. Use only proper subsets for small-basis calculation. | 激活双基组 SCF（HF 或 DFT）能量校正。 / 建议： / 使用双基组，以较小的基组代价获得大基组效应。与 RI-MP2 结合使用时尤其有用，此时 HF 往往占主导。小基组计算只能使用真正的子集。 |
| `ECP` | Defines the effective core potential and associated basis set to be used / Recommendation: / Pseudopotentials are recommended for first row transition metals and heavier elements. Consult the reviews for more details. | 定义所使用的有效芯势及相应基组 / 建议： / 建议对第一过渡系金属及更重的元素使用赝势。更多细节请查阅综述。 |
| `EDA_BSSE` | Calculates the BSSE correction when performing the energy decomposition analysis. / Recommendation: / Set to TRUE unless a very large basis set is used. | 在进行能量分解分析时计算 BSSE 校正。 / 建议： / 除非使用非常大的基组，否则设为 TRUE。 |
| `EDA_COVP` | Perform COVP analysis when evaluating the RS or ARS charge-transfer correction. COVP analysis is currently implemented only for systems of two fragments. / Recommendation: / Set to TRUE to perform COVP analysis of the CT term in an EDA or SCF MI(RS) job. | 在评估 RS 或 ARS 电荷转移校正时进行 COVP 分析。COVP 分析目前仅适用于二片段体系。 / 建议： / 设为 TRUE 可对 EDA 或 SCF MI(RS) 任务中的 CT 项进行 COVP 分析。 |
| `EDA_PRINT_COVP` | Replace the final MOs with the CVOP orbitals in the end of the run. / Recommendation: / Set to TRUE to print COVP orbitals instead of conventional MOs. | 在计算结束时用 CVOP 轨道替换最终的分子轨道。 / 建议： / 设为 TRUE 可输出 COVP 轨道而非常规分子轨道。 |
| `EE_SINGLETS` |  |  |
| `EE_STATES` |  |  |
| `EE_TRIPLETS` |  |  |
| `EFP` | The keyword should be present if excited state calculation is requested. | 若要求进行激发态计算，应包含此关键词。 |
| `EFP_FRAGMENTS_ONLY` | Set to true if there is no QM part to the calculation. | 若计算中不含 QM 部分，则设为 true。 |
| `EFP_INPUT` | True indicates the new format without a dummy atom in the $molecule section.  False indicates the old format which requires a dummy atom (e.g. He) in the $molecule section for an EFP-only calculation. | True 表示新格式，$molecule 段中不含哑原子。False 表示旧格式，对于纯 EFP 计算，$molecule 段中需要有哑原子（如 He）。 |
| `EOM_CORR` | Specifies the correlation level | 指定相关级别 |
| `EOM_DAVIDSON_CONVERGENCE` | Convergence criterion for the RMS residuals of excited state vectors. / n Corresponding to 10^-n convergence criterion / Use default. Should normally be set to the same value as / EOM DAVIDSON THRESHOLD. | 激发态向量均方根残差的收敛判据。 / n 对应于 10^-n 的收敛判据 / 使用默认值。通常应设为与 / EOM DAVIDSON THRESHOLD 相同的值。 |
| `EOM_DAVIDSON_MAXVECTORS` | Species maximum number of vectors in the subspace for the Davidson diagonalization. / n Up to n vectors per root before the subspace is reset / RECOMMENDATION: / Larger values increase disk storage but accelerate and stabilize convergence. | 指定 Davidson 对角化中子空间的最大向量数。 / n 在子空间重置前每个根最多 n 个向量 / 建议： / 较大的值会增加磁盘存储量，但能加快并稳定收敛。 |
| `EOM_DAVIDSON_MAX_ITER` | Maximum number of iteration allowed for Davidson diagonalization procedure. / n User-defined number of iterations | Davidson 对角化过程允许的最大迭代次数。 / n 用户定义的迭代次数 |
| `EOM_DAVIDSON_THRESHOLD` | Species threshold for including a new expansion vector in the iterative Davidson diagonalization. Their norm must be above this threshold. / abcde Integer code is mapped to abc x 10^-de | 指定在迭代 Davidson 对角化中加入新展开向量的阈值。其范数必须高于该阈值。 / abcde 整数码映射为 abc x 10^-de |
| `EOM_EA_ALPHA` | Sets the number of attached target states derived by attaching alpha electron (Ms = 1/2). / [i, j, k...] Find i EA states in the first irrep, j states in the second irrep etc. | 设置通过附加 alpha 电子（Ms = 1/2）得到的附着目标态数目。 / [i, j, k...] 在第一个不可约表示中查找 i 个 EA 态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `EOM_EA_BETA` | Sets the number of attached target states derived by attaching beta electron (Ms = -1/2). / [i, j, k...] Find i EA states in the first irrep, j states in the second irrep etc. | 设置通过附加 beta 电子（Ms = -1/2）得到的附着目标态数目。 / [i, j, k...] 在第一个不可约表示中查找 i 个 EA 态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `EOM_EA_STATES` | Sets the number of attached target states roots to find. By default, alpha electron will be attached (see EOM_EA_ALPHA). / [i; j; k...] Find i EA states in the first irrep, j states in the second irrep etc. | 设置要查找的附着目标态根数。默认附加 alpha 电子（参见 EOM_EA_ALPHA）。 / [i; j; k...] 在第一个不可约表示中查找 i 个 EA 态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `EOM_IP_ALPHA` | Sets the number of ionized target states derived by removing alpha electron (Ms = -1/2). / [i, j, k...] Find i inonized states in the first irrep, j states in the second irrep etc. | 设置通过移除 alpha 电子（Ms = -1/2）得到的电离目标态数目。 / [i, j, k...] 在第一个不可约表示中查找 i 个电离态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `EOM_IP_BETA` | Sets the number of ionized target states derived by removing beta electron (Ms = 1/2, default for EOM-IP). / [i, j, k...] Find i inonized states in the first irrep, j states in the second irrep etc. | 设置通过移除 beta 电子（Ms = 1/2，EOM-IP 的默认值）得到的电离目标态数目。 / [i, j, k...] 在第一个不可约表示中查找 i 个电离态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `EOM_IP_STATES` | Sets the number of ionized target states roots to find. By default, B electron will be removed (see EOM_IP_BETA). / [i, j, k...] Find i inonized states in the first irrep, j states in the second irrep etc. | 设置要查找的电离目标态根数。默认移除 B 电子（参见 EOM_IP_BETA）。 / [i, j, k...] 在第一个不可约表示中查找 i 个电离态，在第二个不可约表示中查找 j 个态，依此类推。 |
| `EOM_NGUESS_DOUBLES` | Specifies number of excited state guess vectors which are double excitations. / Options: n Include n guess vectors that are double excitations / Recommendation: / This should be set to the expected number of doubly excited states (see also CC_PRECONV_DOUBLES), otherwise they may not be found. | 指定作为双激发的激发态初始猜测向量的数目。 / 选项：n 包含 n 个双激发的猜测向量 / 建议： / 该值应设为预期的双激发态数目（另见 CC_PRECONV_DOUBLES），否则可能无法找到这些态。 |
| `EOM_NGUESS_SINGLES` | Specifies number of excited state guess vectors which are single excitations. / Options: n Include n guess vectors that are single excitations / Recommendation: / Should be greater or equal than the number of excited states requested. | 指定作为单激发的激发态初始猜测向量的数目。 / 选项：n 包含 n 个单激发的猜测向量 / 建议： / 应大于或等于所请求的激发态数目。 |
| `EOM_NGUES_DOUBLES` | Specifies number of excited state guess vectors which are double excitations. / Options: n Include n guess vectors that are double excitations / Recommendation: / This should be set to the expected number of doubly excited states (see also CC_PRECONV_DOUBLES), otherwise they may not be found. | 指定作为双激发的激发态初始猜测向量的数目。 / 选项：n 包含 n 个双激发的猜测向量 / 建议： / 该值应设为预期的双激发态数目（另见 CC_PRECONV_DOUBLES），否则可能无法找到这些态。 |
| `EOM_REF_PROP_TE` | Request for calculation of non-relaxed two-particle EOM-CC properties. The two-particle properties currently include / ⟨ / S / 2 / ⟩. The one-particle properties also will be calculated, since their cost is much less than the two-particle properties. The variable CC_EOM_PROP must be also set to TRUE | 请求计算非弛豫的二粒子 EOM-CC 性质。目前二粒子性质包括 / ⟨ / S / 2 / ⟩。单粒子性质也会被计算，因为其计算量远小于二粒子性质。变量 CC_EOM_PROP 也必须设为 TRUE。或者，可使用 CC_CALC_SSQ 请求 ⟨ / S / 2 / ⟩ 的计算。 / 建议： / 二粒子性质的计算代价很高，因为它们需要计算 / 并使用二粒子密度矩阵（其代价大致与解析梯度计算相当）。除非确实需要，否则不要请求二粒子性质。 |
| `EPAO_ITERATE` | Controls iterations for EPAO calculations (see PAO_METHOD). / Recommendation: / Use default. For molecules that are not too large, one can test the sensitivity of the results to the type of minimal functions by the use of optimized EPAOs in which case a value of 500 is reasonable. | 控制 EPAO 计算的迭代（参见 PAO_METHOD）。 / 建议： / 使用默认值。对于不太大的分子，可用优化的 EPAO 来检验结果对极小基函数类型的敏感性，此时取 500 是合理的。 |
| `EPAO_WEIGHTS` | Controls algorithm and weights for EPAO calculations (see PAO_METHOD). / Recommendation: / Use default, unless convergence failure is encountered. | 控制 EPAO 计算的算法与权重（参见 PAO_METHOD）。 / 建议： / 使用默认值，除非遇到收敛失败。 |
| `EXCHANGE` | Specifies the exchange level of theory. / Recommendation: / Consult the literature and reviews for guidence | 指定交换的理论级别。 / 建议： / 查阅文献与综述以获得指导 |
| `FAST_XC` | Controls direct variable thresholds to accelerate the calculation of exchange and correlation (XC) in DFT. / Recommendation: / This option improves the speed of a DFT calculation, but may occasionally cause the SCF calculation to diverge. | 控制直接变量阈值，以加速 DFT 中交换相关（XC）的计算。 / 建议： / 该选项可提高 DFT 计算的速度，但偶尔可能导致 SCF 计算发散。 |
| `FD_DERIVATIVE_TYPE` | Controls what types of gradient information are used to compute higher derivatives. The default uses a combination of energy, gradient and Hessian information, which makes the force field calculation faster. / Recommendation: / When the molecule is larger than benzene with small basis set, using onl | 控制使用何种梯度信息来计算高阶导数。默认组合使用能量、梯度与海森矩阵信息，这使力场计算更快。 / 建议： / 当分子比苯更大且使用小基组时，仅使用海森矩阵信息可能更快。注意，若所请求阶数的解析导数不可用，该选项会被自动下调。 |
| `FD_STEP_SIZE` | Displacement used for calculating derivatives by finite difference. / Recommendation: / Use default, unless on a very flat potential, in which case a larger value may be required. | 用有限差分计算导数时所用的位移。 / 建议： / 使用默认值，除非势能面非常平缓，此时可能需要更大的值。 |
| `FRGM_LPCORR` | Specifies a correction method performed after the locally-projected equations are converged. / Recommendation: / For large basis sets use ARS, use RS if ARS fails. | 指定局域投影方程收敛后所执行的校正方法。 / 建议： / 对于大基组使用 ARS，若 ARS 失败则使用 RS。 |
| `FRGM_METHOD` | Specifies the locally-projected method. / Recommendation: / STOLL and GIA - variational optimization of the ALMOs. NOSCF options are for computationally fast corrections of the FRAGMO initial guess. | 指定局域投影方法。 / 建议： / STOLL 与 GIA —— 对 ALMO 做变分优化。NOSCF 选项用于对 FRAGMO 初始猜测作快速的计算校正。 |
| `FSM_MODE` | Specifies the method of interpolation. | 指定插值的方法。 |
| `FSM_NGRAD` | Specifies the number of perpendicular gradients used to optimise each node. | 指定优化每个节点所用的垂直梯度数目。 |
| `FSM_NNODE` | Specifies the number of nodes along the string. | 指定弦上节点的数目。 |
| `FSM_OPT_MODE` | Specifies the method of optimization. | 指定优化的方法。 |
| `FTC` | Controls the overall use of the FTC. / Recommendation: / Use FTC when bigger and/or diffuse basis sets are used. | 控制 FTC 的整体使用。 / 建议： / 当使用较大和/或弥散基组时使用 FTC。 |
| `FTC_CLASS_THRESH_MULT` | Together with FTC_CLASS_THRESH_ORDER, determines the cutoff threshold for included a shell-pair in the / dd / class, i.e. the class that is expanded in terms of plane waves. | 与 FTC_CLASS_THRESH_ORDER 共同决定将壳层对纳入 / dd / 类的截断阈值，即以平面波展开的那一类。 |
| `FTC_CLASS_THRESH_ORDER` | Together with FTC_CLASS_THRESH_MULT, determines the cutoff threshold for included a shell-pair in the / dd / class, i.e. the class that is expanded in terms of plane waves. | 与 FTC_CLASS_THRESH_MULT 共同决定将壳层对纳入 / dd / 类的截断阈值，即以平面波展开的那一类。 |
| `FTC_FAST` | Controls whether or not the operator is evaluated on a large grid and stored in memory to speed up the calculation. / Recommendation: / Use the default if possible.  Turning this option off conserves some memory, but causes a slow down in speed. | 控制是否在较大的网格上计算算符并将其存入内存以加快计算速度。 / 建议： / 尽量使用默认值。关闭该选项可节省部分内存，但会导致速度变慢。 |
| `GAUSSIAN_BLUR` | Enables the use of Gaussian-delocalized external charges in a QM/MM calculation.  If set to FALSE, then regular point charges are used. | 启用在 QM/MM 计算中使用高斯离域化的外部电荷。若设为 FALSE，则改用常规点电荷。 |
| `GEN_SCFMAN` | Unselect this option for features not yet supported by GEN_SCFMAN. | 对于 GEN_SCFMAN 尚不支持的功能，请取消选择该选项。 |
| `GEOM_OPT_COORDINATES` | Controls the type of optimization coordinates. / Recommendation: / Use the default; delocalized internals are more efficient. | 控制优化坐标的类型。 / 建议： / 使用默认值；离域内坐标效率更高。 |
| `GEOM_OPT_DIIS_SUBSPACE` | Controls maximum size of subspace for GDIIS. 0 turns off GDIIS and -1 causes the program to select the default size. | 控制 GDIIS 子空间的最大大小。0 关闭 GDIIS，-1 使程序选择默认大小。 |
| `GEOM_OPT_DRIVER` | Controls the optimization driver. | 控制几何优化的驱动方式。 |
| `GEOM_OPT_HESSIAN` | Determines the initial Hessian status. / Recommendation: / An accurate initial Hessian will improve the performance of the optimizer, but is expensive to compute. | 确定初始海森矩阵的状态。 / 建议： / 精确的初始海森矩阵可改善优化器的表现，但计算代价较高。 |
| `GEOM_OPT_HESSIAN_UPDATE` | Controls the Hessian update algorithm.  The default dpends on the type of job. | 控制海森矩阵的更新算法，默认值取决于任务类型。 |
| `GEOM_OPT_IPROJ` | Allows the molecule to reorient during a geometry optimization.  Turn this option off if using external charges. | 允许分子在几何优化过程中重新取向。如果使用外部电荷，请关闭该选项。 |
| `GEOM_OPT_LINEAR_ANGLE` | Threshold for near linear bond angles (in degrees). / Recommendation: / Use default. | 近线性键角的阈值（单位为度）。 / 建议： / 使用默认值。 |
| `GEOM_OPT_MAX_CYCLES` | Maximum number of optimization cycles. / Recommendation: / The default should be sufficient for most cases. Increase if the initial guess geometry is poor, or for systems with shallow potential wells. | 最大优化循环次数。 / 建议： / 默认值对大多数情况应已足够。若初始猜测几何构型较差，或体系势能阱较浅，可适当增大。 |
| `GEOM_OPT_MAX_STEP_SIZE` | Maximum allowed step size in the geometry optimization. | 几何优化中允许的最大步长。 |
| `GEOM_OPT_MODE` | Determines which Hessian mode is followed during a transition state search. | 确定在过渡态搜索中沿哪个海森矩阵模式进行。 |
| `GEOM_OPT_PRINT` | Controls the amount of optimization print output. / Recommendation: / Use the default. | 控制优化输出信息的详略程度。 / 建议： / 使用默认值。 |
| `GEOM_OPT_SCF_GUESS_ALWAYS` | Switch to force the regeneration of a new initial guess for each series of SCF iterations (for use in geometry optimization). / Recommendation: / Use default unless SCF convergence issues arise | 强制为每一轮 SCF 迭代重新生成新的初始猜测的开关（用于几何优化）。 / 建议： / 除非出现 SCF 收敛问题，否则使用默认值。 |
| `GEOM_OPT_SYMMETRY` | Controls the use of point-group symmetry in the optimization. | 控制优化中点群对称性的使用。 |
| `GEOM_OPT_TOL_DISPLACEMENT` | Convergence on maximum atomic displacement (in micro-angstroms). / Recommendation: / Use the default. To converge the gradient and either one of the energy or displacement tolerances must be satisfied. | 最大原子位移的收敛判据（单位为微埃）。 / 建议： / 使用默认值。要达到收敛，梯度以及能量或位移容差其中之一必须得到满足。 |
| `GEOM_OPT_TOL_ENERGY` | Convergence on energy change of successive optimization cycles (x 10 / -8 / ). / Recommendation: / Use the default. To converge the gradient and either one of the energy or displacement tolerances must be satisfied. | 相继优化周期能量变化的收敛判据（× 10 / -8 / ）。 / 建议： / 使用默认值。要达到收敛，梯度以及能量或位移容差其中之一必须得到满足。 |
| `GEOM_OPT_TOL_GRADIENT` | Convergence on maximum gradient component. / Recommendation: / Use the default. To converge the gradient and either one of the energy and displacement tolerances must be satisfied. | 最大梯度分量的收敛判据。 / 建议： / 使用默认值。要达到收敛，梯度以及能量和位移容差其中之一必须得到满足。 |
| `GEOM_PRINT` | Controls the printing of additional geometric information at each step. / Recommendation: / Use if you want to be able to quickly examine geometric parameters at the beginning and end of optimizations. Only prints in the beginning of single point energy calculations. | 控制每一步是否打印额外的几何信息。 / 建议： / 若希望在优化开始和结束时快速查看几何参数，可使用该选项。在单点能量计算中仅在开始时打印。 |
| `GHF` | Run a generalized Hatree-Fock calculation. / Recommendation: / GEN_SCFMAN must be active for this to take effect. | 运行广义 Hartree-Fock 计算。 / 建议： / 必须启用 GEN_SCFMAN 才能生效。 |
| `GUI` | Controls the output of auxiliary information for third party packages. / Recommendation: / Use default unless the additional information is required. Please note that any existing / Test.FChk / file will be overwritten. | 控制是否输出供第三方程序使用的辅助信息。 / 建议： / 除非需要这些附加信息，否则使用默认值。请注意，任何已存在的 / Test.FChk / 文件都将被覆盖。 |
| `GVB_AMP_SCALE` | Scales the default orbital amplitude iteration step size for IP/RCC. PP amplitude equations are solved analytically, so this parameter does not affect PP. / Recommendation: / Default is usually fine, but in some highly-correlated systems it can help with convergence to use smaller values. | 缩放 IP/RCC 的默认轨道振幅迭代步长。PP 振幅方程是解析求解的，因此该参数不影响 PP。 / 建议： / 默认值通常即可，但在某些强关联体系中使用较小的值有助于收敛。 |
| `GVB_GUESS_MIX` | Similar to SCF_GUESS_MIX, it breaks alpha-beta symmetry for UPP by mixing the alpha HOMO and LUMO orbitals according to the user-defined fraction of LUMO to add the HOMO. 100 corresponds to a 1:1 ratio of HOMO and LUMO in the mixed orbitals. / Recommendation: / 25 often works well to break symmetry  | 与 SCF_GUESS_MIX 类似，它按用户定义的掺入 HOMO 的 LUMO 比例混合 alpha HOMO 与 LUMO 轨道，从而打破 UPP 的 alpha-beta 对称性。100 对应混合轨道中 HOMO 与 LUMO 的 1:1 比例。 / 建议： / 25 通常能很好地打破对称性，同时不会过度妨碍收敛。 |
| `GVB_LOCAL` | Sets the localization scheme used in the initial guess wave function. / Recommendation: / Different initial guesses can sometimes lead to different solutions. It can be helpful to try both to ensure the global minimum has been found. | 设置初始猜测波函数中所用的定域化方案。 / 建议： / 不同的初始猜测有时会导致不同的解。尝试两种方案有助于确认已找到全局极小值。 |
| `GVB_N_PAIRS` | Alternative to CC_REST_OCC and CC_REST_VIR for setting active space size in GVB and valence coupled cluster methods. / Recommendation: / Use default unless one wants to study a special active space. When using small active spaces, it is important to ensure that the proper orbitals are incorporated i | 在 GVB 和价层耦合簇方法中，替代 CC_REST_OCC 和 CC_REST_VIR 来设置活性空间大小。 / 建议： / 除非要研究特殊的活性空间，否则使用默认值。使用较小的活性空间时，务必确保正确的轨道被纳入活性空间。若未能纳入，可使用 / $reordermo / 功能适当调整 SCF 轨道。 |
| `GVB_ORB_CONV` | The GVB-CC wave function is considered converged when the root-mean-square  orbital gradient and orbital step sizes are less than  10<sup>-GVB_ORB_CONV</sup>. Adjust THRESH simultaneously.<br><b>Recommendation:</b> Use 6 for PP(2) jobs or geometry optimizations. Tighter convergence (<i>i.e.</i> 7 or | 当轨道梯度和轨道步长的均方根小于 10<sup>-GVB_ORB_CONV</sup> 时，GVB-CC 波函数即视为收敛。同时调整 THRESH。<br><b>建议：</b> PP(2) 任务或几何优化使用 6。更严格的收敛（<i>即</i> 7 或更高）并非总能可靠地达到。 |
| `GVB_ORB_MAX_ITER` | Controls the number of orbital iterations allowed in GVB-CC calculations. Some jobs, particularly unrestricted PP jobs can require 500-1000 iterations.<br><b>Recommendation:</b> Default is typically adequate, but some jobs, particularly UPP jobs, can  require 500-1000 iterations if converged tightly | 控制 GVB-CC 计算中允许的轨道迭代次数。某些任务，特别是非限制性 PP 任务，可能需要 500-1000 次迭代。<br><b>建议：</b> 默认值通常足够，但某些任务，特别是 UPP 任务，若要求严格收敛可能需要 500-1000 次迭代。 |
| `GVB_ORB_SCALE` | Scales the default orbital step size by <i>n</i>/1000.<br><b>Recommendation:</b> Default is usually fine, but for some stretched geometries it can help with convergence to use smaller values. | 按 <i>n</i>/1000 缩放默认轨道步长。<br><b>建议：</b> 默认值通常即可，但对于某些被拉伸的几何构型，使用较小的值有助于收敛。 |
| `GVB_PRINT` | Controls the amount of information printed during a GVB-CC job.<br><b>Recommendation:</b> Should never need to go above 0 or 1. | 控制 GVB-CC 任务中打印信息的详略程度。<br><b>建议：</b> 通常无需超过 0 或 1。 |
| `GVB_RESTART` | Restart a job from previously-converged GVB-CC orbitals.<br><b>Recommendation:</b> Useful when trying to converge to the same GVB solution at slightly different geometries, for example. | 从先前已收敛的 GVB-CC 轨道重启任务。<br><b>建议：</b> 例如，在略有差异的几何构型下要收敛到同一 GVB 解时非常有用。 |
| `GVB_UNRESTRICTED` | Controls restricted versus unrestricted PP jobs. Usually handled  automatically.<br><b>Recommendation:</b> Set this variable explicitly only to do a UPP job from an RHF or ROHF initial guess. | 控制限制性 PP 与非限制性 PP 任务，通常由程序自动处理。<br><b>建议：</b> 仅在需要从 RHF 或 ROHF 初始猜测出发做 UPP 任务时才显式设置该变量。 |
| `HESS_PROJ_TRM` | Selects whether or not to project out the rotational and translational degrees of freedom in a frequency calculation. | 选择在频率计算中是否投影掉转动和平动自由度。 |
| `HFPT` | This is set implicitly by setting HFPT_BASIS | 该变量通过设置 HFPT_BASIS 隐式设定。 |
| `HFPT_BASIS` | Specifies the secondary basis in a HFPC/DFPC calculation | 指定 HFPC/DFPC 计算中的次级基组。 |
| `HIRSHFELD` | Compute Hirshfeld populations | 计算 Hirshfeld 布居数。 |
| `INCDFT` | Toggles the use of the IncDFT procedure for DFT energy calculations. / Recommendation: / Turning this option on can lead to faster SCF calculations, particularly towards the end of the SCF. Please note that for some systems use of this option may lead to convergence problems. | 切换是否在 DFT 能量计算中使用 IncDFT 流程。 / 建议： / 启用该选项可加快 SCF 计算，特别是在 SCF 接近收敛时。请注意，对某些体系使用该选项可能导致收敛问题。 |
| `INCDFT_DENDIFF_THRESH` | Sets the threshold for screening density matrix values in the IncDFT procedure. / Recommendation: / If the default value causes convergence problems, set this value higher to tighten the threshold. | 设置 IncDFT 流程中筛选密度矩阵数值的阈值。 / 建议： / 若默认值引起收敛问题，可将该值调高以收紧阈值。 |
| `INCDFT_DENDIFF_VARTHRESH` | Sets the lower bound for the variable threshold for screening density matrix values in the IncDFT procedure. The threshold will begin at this value and then vary depending on the error in the current SCF iteration until the value specified by INCDFT_DENDIFF_THRESH is reached. This means this value m | 设置 IncDFT 流程中筛选密度矩阵数值的可变阈值的下限。阈值将从该值开始，随后根据当前 SCF 迭代的误差而变化，直至达到 INCDFT_DENDIFF_THRESH 所指定的值。这意味着该值必须设得低于 INCDFT_DENDIFF_THRESH。 / 建议： / 若默认值引起收敛问题，可将该值调高以提高精度。若仍无效，则设为 0 并使用静态阈值。 |
| `INCDFT_GRIDDIFF_THRESH` | Sets the threshold for screening functional values in the IncDFT procedure / Recommendation: / If the default value causes convergence problems, set this value higher to tighten the threshold. | 设置 IncDFT 流程中筛选泛函数值的阈值。 / 建议： / 若默认值引起收敛问题，可将该值调高以收紧阈值。 |
| `INCDFT_GRIDDIFF_VARTHRESH` | Sets the lower bound for the variable threshold for screening the functional values in the IncDFT procedure. The threshold will begin at this value and then vary depending on the error in the current SCF iteration until the value specified by INCDFT_GRIDDIFF_THRESH is reached. This means that this v | 设置 IncDFT 流程中筛选泛函数值的可变阈值的下限。阈值将从该值开始，随后根据当前 SCF 迭代的误差而变化，直至达到 INCDFT_GRIDDIFF_THRESH 所指定的值。这意味着该值必须设得低于 INCDFT_GRIDDIFF_THRESH。 / 建议： / 若默认值引起收敛问题，可将该值调高以提高精度。若仍无效，则设为 0 并使用静态阈值。 |
| `INCFOCK` | Iteration number after which the incremental Fock matrix algorithm is initiated.  Setting this to 0 turns INCFOCK off. / Recommendation: / May be necessary to allow several iterations before switching on INCFOCK. | 启动增量 Fock 矩阵算法的迭代序号。设为 0 则关闭 INCFOCK。 / 建议： / 可能需要先进行数次迭代再开启 INCFOCK。 |
| `INTEGRALS_BUFFER` | Controls the size of in-core integral storage buffer (in megabytes). / Recommendation: / Use the default, or consult your systems administrator for hardware limits. | 控制内核积分存储缓冲区的大小（单位为兆字节）。 / 建议： / 使用默认值，或就硬件限制咨询系统管理员。 |
| `INTERNAL_STABILITY` | Perform internal stability analysis in GEN_SCFMAN / Recommendation: / Turn on when the SCF solution is prone to unstable solutions, especially for open-shell species. | 在 GEN_SCFMAN 中执行内禀稳定性分析。 / 建议： / 当 SCF 解容易出现不稳定解时启用，尤其是开壳层物种。 |
| `INTRACULE` | Controls whether intracule properties are calculated.  Setting this option causes the data in $intracule to be activated. | 控制是否计算电子对相关性质。设置该选项会使 $intracule 中的数据被激活。 |
| `INTRACULE_CONSERVE_MEMORY` | Reduce memory required in the evaluation of W(u,v). / Recommendation: / The low memory option is slower, use default unless memory is limited. | 减少计算 W(u,v) 所需的内存。 / 建议： / 低内存选项速度较慢，除非内存受限，否则使用默认值。 |
| `INTRACULE_GRID` | Specify angular Lebedev grid for Wigner intracule calculations. / Recommendation: / Larger grids if high accuracy required. | 为 Wigner 电子对相关计算指定 Lebedev 角度网格。 / 建议： / 若需要高精度，使用更大的网格。 |
| `INTRACULE_I_SERIES_LIMIT` | Sets summation limit for series expansion evaluation of i_n(x). / Recommendation: / Lower values speed up the calculation, but may affect accuracy. | 设置 i_n(x) 级数展开计算的求和上限。 / 建议： / 较小的值可加快计算，但可能影响精度。 |
| `INTRACULE_J_SERIES_LIMIT` | Sets summation limit for series expansion evaluation of j_n(x). / Recommendation: / Lower values speed up the calculation, but may affect accuracy. | 设置 j_n(x) 级数展开计算的求和上限。 / 建议： / 较小的值可加快计算，但可能影响精度。 |
| `INTRACULE_METHOD` | Use Lebedev quadrature to evaluate Wigner integrals. | 使用 Lebedev 求积计算 Wigner 积分。 |
| `INTRACULE_WIGNER_SERIES_LIMIT` | Sets summation limit for Wigner integrals. / Recommendation: / Increase / n / for greater accuracy. | 设置 Wigner 积分的求和上限。 / 建议： / 增大 / n / 以获得更高的精度。 |
| `ISOTOPES` | Specifies if non-default masses are to be used in the frequency calculation.  If this option is selected the $isotopes section is read. | 指定在频率计算中是否使用非默认质量。若选择该选项，将读取 $isotopes 段。 |
| `JOB_TYPE` | Specifies the type of calculation to run. | 指定要运行的计算类型。 |
| `LINK_ATOM_PROJECTION` | Controls whether to perform a link-atom projection, which is necessary in a full QM/MM hessian evaluation on a system with link atoms. | 控制是否执行连接原子投影，这在含连接原子的体系上进行完整 QM/MM 海森矩阵计算时是必需的。 |
| `LIN_K` | Controls whether linear scaling evaluation of exact exchange (LinK) is used. / Recommendation: / Use for HF and hybrid DFT calculations with large numbers of atoms. | 控制是否使用精确交换的线性标度算法（LinK）。 / 建议： / 用于原子数较多的 HF 和杂化 DFT 计算。 |
| `LOCAL_INTERP_ORDER` | Controls the order of the B-spline. | 控制 B 样条的阶数。 |
| `LRC_DFT` | Controls the application of long-range-corrected DFT / Recommendation: / Long-range correction is available for any combination of HF, B88 and PBE exchange. | 控制长程校正 DFT 的应用。 / 建议： / 长程校正适用于 HF、B88 和 PBE 交换的任意组合。 |
| `MAX_CIS_SUBSPACE` | Maximum number of subspace vectors allowed in the CIS iterations: / Recommendation: / The default is usually appropriate, unless a large number of states are requested for a large molecule. The total memory required to store the subspace vectors is bounded above by 2 / nOV / , where / O / and / V /  | CIS 迭代中允许的最大子空间向量数： / 建议： / 默认值通常合适，除非对大分子请求大量的态。存储子空间向量所需的总内存上限为 2 / nOV / ，其中 / O / 和 / V / 分别表示占据轨道和虚轨道的数目。 / n / 可减小以节省内存，代价是 CIS 迭代次数增多。若 / n / 并未显著大于 CIS_N_ROOTS，收敛可能受到影响。 |
| `MAX_SUB_FILE_NUM` | Sets the maximum number of sub files allowed. / Recommendation: / Leave as default, or adjust according to your system limits. | 设置允许的子文件最大数量。 / 建议： / 保持默认值，或根据系统限制调整。 |
| `MEMORY_STATIC` | Sets the memory (in megabytes) for individual program modules. / Recommendation: / For direct and semi-direct MP2 calculations, this must exceed OVN + requirements for AO integral evaluation (32-160 Mb). | 设置各程序模块的内存（单位为兆字节）。 / 建议： / 对于直接和半直接 MP2 计算，该值必须超过 OVN 加上 AO 积分计算的需求（32-160 Mb）。 |
| `MEMORY_TOTAL` | Sets the total memory available to Q-Chem, in megabytes. / Recommendation: / Use default, or set to the physical memory of your machine. | 设置 Q-Chem 可用的总内存，单位为兆字节。 / 建议： / 使用默认值，或设为您机器的物理内存大小。 |
| `MEM_STATIC` | Sets the memory (in megabytes) for individual fortran program modules. / Recommendation: / For direct and semi-direct MP2 calculations, this must exceed OVN + requirements for AO integral evaluation (32-160 Mb). | 设置各 fortran 程序模块的内存（单位为兆字节）。 / 建议： / 对于直接和半直接 MP2 计算，该值必须超过 OVN 加上 AO 积分计算的需求（32-160 Mb）。 |
| `MEM_TOTAL` | Sets the total memory available to Q-Chem, in megabytes. | 设置 Q-Chem 可用的总内存，单位为兆字节。 |
| `METECO` | Sets the threshold criteria for discarding shell-pairs. / Recommendation: / Use default. | 设置丢弃壳层对的阈值判据。 / 建议： / 使用默认值。 |
| `METHOD` | The level of theory used in the calculation. | 计算中所用的理论级别。 |
| `MM_CHARGES` | Requests the calculation of multipole-derived charges (MDCs). / Recommendation: / Set to TRUE if MDCs or the traceless form of the multipole moments are desired. The calculation does not take long. | 请求计算多极衍生电荷（MDC）。 / 建议： / 若需要 MDC 或多极矩的无迹形式，设为 TRUE。该计算耗时不长。 |
| `MOLDEN_FORMAT` | Requests a / $molden / -formatted input file containing information from a Q-Chem job. | 请求生成 / $molden / 格式的输入文件，其中包含来自 Q-Chem 任务的信息。 |
| `MOM_METHOD` | Selects between the original (MOM) and initial (IMOM) algorithms. / Recommendation: / If appropriate guess orbitals can be obtained, then IMOM can provide more reliable convergence to the desired solution. | 在原始（MOM）与初始（IMOM）算法之间选择。 / 建议： / 若能获得合适的猜测轨道，IMOM 可更可靠地收敛到所需解。 |
| `MOM_PRINT` | Switches printing on within the MOM procedure. | 开启 MOM 过程中的打印输出。 |
| `MOM_START` | Determines when MOM is switched on to stabilize DIIS iterations. / Recommendation: / Set to 1 if preservation of initial orbitals is desired. If MOM is to be used to aid convergence, an SCF without MOM should be run to determine when the SCF starts oscillating. MOM should be set to start just before | 确定何时开启 MOM 以稳定 DIIS 迭代。 / 建议： / 若希望保持初始轨道，设为 1。若要借助 MOM 改善收敛，应先运行一次不使用 MOM 的 SCF，以确定 SCF 何时开始振荡。MOM 应设为在振荡开始之前启动。 |
| `MOPROP` | Specifies the job for mopropman.  Note that for hyperpolarizabilities, the Wigner option uses the (2n+1) rule, and the Read option takes first order results from disk. | 指定 mopropman 的任务。注意，对于超极化率，Wigner 选项使用 (2n+1) 规则，Read 选项从磁盘读取一阶结果。 |
| `MOPROP_CONVERGENCE_LEVEL_1` | Sets the convergence criteria for CPSCF and 1st order TDSCF. | 设置 CPSCF 和一阶 TDSCF 的收敛判据。 |
| `MOPROP_CONVERGENCE_LEVEL_2` | Sets the convergence criterium for second-order TDSCF. | 设置二阶 TDSCF 的收敛判据。 |
| `MOPROP_DIIS` | Controls the use of Pulays DIIS. | 控制 Pulay DIIS 的使用。 |
| `MOPROP_DIIS_SUBSPACE` | Specified the DIIS subspace dimension. | 指定 DIIS 子空间的维数。 |
| `MOPROP_MAX_CYCLES_LEVEL_1` | The maximal number of iterations for CPSCF and first-order TDSCF. / Recommendation: / Use default. | CPSCF 和一阶 TDSCF 的最大迭代次数。 / 建议： / 使用默认值。 |
| `MOPROP_MAX_CYCLES_LEVEL_2` | The maximal number of iterations for second-order TDSCF. / Recommendation: / Use default. | 二阶 TDSCF 的最大迭代次数。 / 建议： / 使用默认值。 |
| `MOPROP_PERTURBATIONS` | Set the number of perturbed densities that will to be treated together. / Recommendation: / Use default | 设置一起处理的受扰密度数目。 / 建议： / 使用默认值。 |
| `MOPROP_SAVE_LAST_GPX` | Save last G[P] / x / when calculating dynamic polarizabilities in order to call mopropman in a second run with MOPROP = 102. | 保存最后的 G[P] / x / 在计算动态极化率时保存，以便在第二次运行中以 MOPROP = 102 调用 mopropman。 |
| `MP2V` |  |  |
| `MRXC` | Controls the use of multi-resolution XC. / Recommendation: / MRXC is very efﬁcient for medium and large molecules, especially when medium and / large basis sets are used. | 控制多分辨率交换相关（XC）的使用。 / 建议： / MRXC 对中大型分子非常高效，特别是在使用中等和 / 大基组时。 |
| `MRXC_CLASS_THRESH_MULT` | Controls the prefactor of the smoothness precision. | 控制平滑精度中前置因子的取值。 |
| `MRXC_CLASS_THRESH_ORDER` | Controls the exponent of the smoothness precision. | 控制平滑精度的指数。 |
| `MULLIKEN` |  |  |
| `MULTIPOLE_ORDER` | Determines the order to which the multipole expansion of the solute charge density is carried out. / Recommendation: / Use default unless higher (or lower) precision is desired. | 决定溶质电荷密度的多极展开所进行的阶数。 / 建议： / 使用默认值，除非需要更高（或更低）的精度。 |
| `NBO` | Controls the use of the NBO package. | 控制是否使用 NBO 程序包。 |
| `NL_CORRELATION` | Speciﬁes a non-local correlation functional that includes non-empirical dispersion. / Recommendation: / Do not forget to add the LSDA correlation (PW92 is recommended) when using vdW-DF-04, vdW-DF-10, or VV09. VV10 should be used with PBE correlation. Choose exchange functionals carefully: HF, rPW86 | 指定包含非经验色散作用的非局域相关泛函。 / 建议： / 使用 vdW-DF-04、vdW-DF-10 或 VV09 时，别忘了加上 LSDA 相关（推荐 PW92）。VV10 应与 PBE 相关配合使用。交换泛函需谨慎选择：HF、rPW86、revPBE 以及部分长程校正（LRC）交换泛函都是推荐之选。 |
| `NL_GRID` | Speciﬁes the grid to use for non-local correlation. / Recommendation: / Use default unless computational cost becomes prohibitive, in which case SG-0 may be / used.  XC_GRID should generally be ﬁner than NL_GRID. | 指定用于非局域相关的网格。 / 建议： / 使用默认值，除非计算开销过大，此时可使用 SG-0。 / 一般而言，XC_GRID 应比 NL_GRID 更精细。 |
| `NL_VV_B` | Sets the parameter / b / in VV10. This parameter controls the short range behavior of the nonlocal correlation energy. / Recommendation: / The optimal value depends strongly on the exchange functional used. / b / = 5.9 is recommended for rPW86. | 设置 / b / 参数。该参数控制 VV10 中非局域相关能的短程行为。 / 建议： / 最优值在很大程度上取决于所用的交换泛函。 / b / = 5.9 是 rPW86 的推荐取值。 |
| `NL_VV_C` | Sets the parameter / C / in VV09 and VV10.  This parameter is fitted to asymptotic van der Waals / C / 6 / coefficients. / Recommendation: / C / = 0.0093 is recommended when a semilocal exchange functional is used. / C / = 0.0089 / is recommended when a long-range corrected (LRC) hybrid functional i | 设置 / C / 参数（VV09 与 VV10 中）。该参数由渐近范德华 / C / 6 / 系数拟合得到。 / 建议： / C / = 0.0093 是使用半局域交换泛函时的推荐取值。 / C / = 0.0089 / 是使用长程校正（LRC）杂化泛函时的推荐取值。 |
| `NTO_PAIRS` | Sets the number of HONTO/LUNTO pairs for each transition. | 设置每个跃迁的 HONTO/LUNTO 自然跃迁轨道对数。 |
| `NVO_LIN_CONVERGENCE` | Target error factor in the preconditioned conjugate gradient solver of the single-excitation amplitude equations.<br><b>Recommendation:</b> Solution of the single-excitation amplitude equations is considered converged if the maximum residual is less than 10<sup>-n</sup> multiplied by the current DII | 单激发振幅方程的预处理共轭梯度求解器中的目标误差因子。<br><b>建议：</b>若最大残差小于当前 DIIS 误差的 10<sup>-n</sup> 倍，则认为单激发振幅方程的解已收敛。对于 ARS 校正，<i>n</i> 自动设为 1，因为局域投影的 DIIS 误差通常比完整的 DIIS 误差小数个数量级。 |
| `NVO_LIN_MAX_ITE` | Maximum number of iterations in the preconditioned conjugate gradient solver of the single-excitation amplitude equations. | 单激发振幅方程的预处理共轭梯度求解器中的最大迭代次数。 |
| `NVO_METHOD` | Sets method to be used to converge solution of the single-excitation amplitude equations. / Recommendation: / Experimental option. Use default. | 设置用于收敛单激发振幅方程解的方法。 / 建议： / 实验性选项。使用默认值。 |
| `NVO_TRUNCATE_DIST` | Specifies which atomic blocks of the Fock matrix are used to construct the preconditioner. / Recommendation: / This option does not affect the final result. However, it affects the rate of the PCG algorithm convergence. For small systems use default. | 指定用 Fock 矩阵的哪些原子块来构造预条件子。 / 建议： / 该选项不影响最终结果，但会影响 PCG 算法的收敛速度。对于小体系，使用默认值。 |
| `NVO_TRUNCATE_PRECOND` | Specifies which atomic blocks of the Fock matrix are used to construct the preconditioner. This variable is used only if NVO_TRUNCATE_DIST is set to -2.<br><b>Recommendation:</b> Use default. Increasing <i>n</i> improves convergence of the PCG algorithm but overall may slow down the calculations. | 指定用 Fock 矩阵的哪些原子块来构造预条件子。该变量仅在 NVO_TRUNCATE_DIST 设为 -2 时使用。<br><b>建议：</b>使用默认值。增大 <i>n</i> 可改善 PCG 算法的收敛性，但总体上可能使计算变慢。 |
| `NVO_UVV_MAXPWR` | Controls convergence of the Taylor series when calculating the U<sub>vv</sub> block from the single-excitation amplitudes. If the series is not converged at the <i>n</i>th term, more expensive direct inversion is used to calculate the U<sub>vv</sub> block. | 控制由单激发振幅计算 U<sub>vv</sub> 块时 Taylor 级数的收敛性。若级数在第 <i>n</i> 项仍未收敛，则改用开销更大的直接求逆来计算 U<sub>vv</sub> 块。 |
| `NVO_UVV_PRECISION` | Controls convergence of the Taylor series when calculating the U<sub>vv</sub> block from the single-excitation amplitudes. Series is considered converged when the maximum element of the term is less than 10<sup>-n</sup>.<br><b>Recommendation:</b> NVO_UVV_PRECISION must be the same as or larger than  | 控制由单激发振幅计算 U<sub>vv</sub> 块时 Taylor 级数的收敛性。当该项的最大元素小于 10<sup>-n</sup> 时，即认为级数已收敛。<br><b>建议：</b>NVO_UVV_PRECISION 必须与 THRESH 相同或不小于 THRESH。 |
| `N_FROZEN_CORE` | Sets the number of frozen core orbitals in a post-Hartree-Fock calculation. / Recommendation: / The default is to freeze orbitals making MP2 calculations more efficient. Use FC if possible. | 设置后 Hartree-Fock 计算中冻结的芯轨道数目。 / 建议： / 默认会冻结轨道，从而使 MP2 计算更高效。尽可能使用 FC。 |
| `N_FROZEN_VIRTUAL` | Sets the number of frozen virtual orbitals in a post-Hartree-Fock calculation. | 设置后 Hartree-Fock 计算中冻结的虚轨道数目。 |
| `OMEGA` | Controls the degree of attenuation of the Coulomb operator. | 控制 Coulomb 算符的衰减程度。 |
| `OS_ROSCF` | Run an open-shell ROSCF calculation. / Recommendation: / GEN_SCFMAN must be turned on for this to have effect. | 运行开壳层 ROSCF 计算。 / 建议： / 必须启用 GEN_SCFMAN 才能生效。 |
| `PAO_ALGORITHM` | Algorithm used to optimize polarized atomic orbitals (see PAO_METHOD) | 用于优化极化原子轨道的算法（参见 PAO_METHOD） |
| `PAO_METHOD` | Controls evaluation of polarized atomic orbitals (PAOs). | 控制极化原子轨道（PAO）的计算。 |
| `PDB_PRINT` | Prints final coordinates at the end of the output file using the PDB format. | 在输出文件末尾以 PDB 格式打印最终坐标。 |
| `PLOTS_GRID` | Controls evaluation of the electrostatic potential on a grid of points. If enabled, the output is in an ACSII file, / plot.esp / , in the format x, y, z, esp for each point. | 控制在网格点上计算静电势。若启用，结果输出到 ASCII 文件 / plot.esp / 中，每个点的格式为 x, y, z, esp。 |
| `PLOTS_PROPERTY` | Triggers the calculation of the electrostatic potential and/or the electric field at the points given in the file ESPGrid. / Recommendation: / Must use this option when IGDESP is specified. | 触发对 ESPGrid 文件中给定点处静电势和／或电场的计算。 / 建议： / 指定 IGDESP 时必须使用此选项。 |
| `PRINT_DISTANCE_MATRIX` | Controls the printing of the inter-atomic distance matrix / Recommendation: / Use default unless distances are required for large systems | 控制是否打印原子间距离矩阵 / 建议： / 使用默认值，除非大体系需要距离信息 |
| `PRINT_DIST_MATRIX` | Controls the printing of the inter-atomic distance matrix<br><b>Recommendation:</b> Use default unless distances are required for large systems | 控制是否打印原子间距离矩阵<br><b>建议：</b>使用默认值，除非大体系需要距离信息 |
| `PRINT_GENERAL_BASIS` | Controls print out of built in basis sets in input format / Recommendation: / Useful for modification of standard basis sets. | 控制是否以输入格式打印内置基组 / 建议： / 便于修改标准基组。 |
| `PRINT_ORBITALS` | Prints orbitals coefficients with atom labels in the analysis part of the output / Recommendation: / Usually not required as the orbitals are more easily examined visually via the formatted checkpoint file. | 在输出文件的分析部分打印带原子标签的轨道系数 / 建议： / 通常不需要，因为通过格式化的 checkpoint 文件可以更直观地查看轨道。 |
| `PSEUDO_CANONICAL` | When SCF_ALGORITHM = DM, this controls the way the initial step, and steps after subspace resets are taken. / Recommendation: / The default is usually more efficient, but choosing TRUE sometimes avoids problems with orbital reordering. | 当 SCF_ALGORITHM = DM 时，控制初始步以及子空间重置后各步的取法。 / 建议： / 默认值通常效率更高，但选择 TRUE 有时可避免轨道重排序的问题。 |
| `PURECART` | Controls the use of pure (spherical harmonic) or Cartesian angular forms. | 控制使用纯（球谐）角函数形式还是笛卡尔角函数形式。 |
| `QMMM` | Turns on the Q-Chem/CHARMM interface. / Recommendation: / Use default unless running calculations with / $charmm / . | 开启 Q-Chem/CHARMM 接口。 / 建议： / 使用默认值，除非使用 / $charmm / 。 |
| `QMMM_CHARGES` | Controls the printing of QM charges to file. / Recommendation: / Use default unless running calculations with / $charmm / where charges on the QM region need to be saved. | 控制是否将 QM 电荷输出到文件。 / 建议： / 使用默认值，除非使用 / $charmm / 且需要保存 QM 区域的电荷。 |
| `QMMM_FULL_HESSIAN` |  |  |
| `QMMM_PRINT` | Controls the amount of output printed from a QM/MM job. / Recommendation: / Use default unless running calculations with / $charmm / . | 控制 QM/MM 任务输出信息的多少。 / 建议： / 使用默认值，除非使用 / $charmm / 。 |
| `QUI_ADC_CORE` | Set the number of core orbitals in an CVS-ADC calculation. | 设置 CVS-ADC 计算中的芯轨道数目。 |
| `QUI_ADC_STATES1` |  |  |
| `QUI_ADC_STATES2` |  |  |
| `QUI_ANGULAR_GRID` | Specifies the quadrature grid to be used for evaluating the exchange-correlation component of the energy.  Either a standard grid should be selected, or a Lebedev grid with the corresponding number of points. / Recommendation: / Use the default unless convergence difficulties arise.  Larger grids ar | 指定用于计算能量中交换相关部分的求积网格。可选择标准网格，或指定点数的 Lebedev 网格。 / 建议： / 使用默认值，除非出现收敛困难。涉及导数与激发态的计算需要更大的网格。 |
| `QUI_AVOGADRO_VISUALIZE_FILE` | Determines which file is passed as an argument to Avogadro.  If true, then the .out file is passed, if false the .Fchk file is passed. | 决定向 Avogadro 传递哪个文件作为参数。若为 true，则传递 .out 文件；若为 false，则传递 .Fchk 文件。 |
| `QUI_CARTESIAN_FALLBACK` | Sets whether or not to fall back to cartesian coordinates if the optimization in internal or Z-matrix coordinates fails. | 设置当内坐标或 Z 矩阵坐标下的优化失败时，是否回退到笛卡尔坐标。 |
| `QUI_CFMM` |  |  |
| `QUI_CHARGE` | Sets the total charge of the system. | 设置体系的总电荷。 |
| `QUI_CHEMSOL_EFIELD` | Determines how the solute charge distribution is approximated in evaluating the electrostatic field of the solute.  Either the exact solute charge distribution is used, or the charge distribution is approximated by Mulliken atomic charges. / Recommentation: / Mulliken charges are faster, but less ri | 决定在计算溶质静电场时如何近似溶质的电荷分布。可使用精确的溶质电荷分布，也可用 Mulliken 原子电荷来近似。 / 建议： / Mulliken 电荷速度更快，但严谨性较差。 |
| `QUI_CHEMSOL_NGRIDS` | Sets the number of grids used to calculate the average hydration free energy. | 设置计算平均水合自由能所用的网格数目。 |
| `QUI_CHEMSOL_PRINT` | Increases the amount of ChemSol output. | 增加 ChemSol 的输出量。 |
| `QUI_CIS_GUESS` | Determines what guess should be used for a CIS calculation.  Note that the read options require a guess from a previous calculation. | 决定 CIS 计算使用何种初始猜测。注意，读取类选项需要有前一次计算得到的猜测。 |
| `QUI_COORDINATES` | Controls the format of the geometry in the output file. | 控制输出文件中几何结构的格式。 |
| `QUI_EOM_DIP` |  |  |
| `QUI_EOM_EA` |  |  |
| `QUI_EOM_EE` |  |  |
| `QUI_EOM_IP` |  |  |
| `QUI_EOM_METHOD` | Specifies the type of EOM calculation to perform | 指定要执行的 EOM 计算类型 |
| `QUI_EOM_SF` |  |  |
| `QUI_EOM_STATES1` |  |  |
| `QUI_EOM_STATES2` |  |  |
| `QUI_FROZEN_CORE` |  |  |
| `QUI_GEOM_OPT_ALGORITHM` | Specifies which type of minimization algorithm to use. / Recommendation: / Steepest descent and conjugate gradient methods are slow to converge in general but are useful when the near the minimum. Best to start with other algorithms and finalize with these two methods if a tighter converged minimum  | 指定使用何种最小化算法。 / 建议： / 最速下降法和共轭梯度法通常收敛较慢，但在接近极小点时较为有用。最好先用其他算法，若需要收敛更严格的极小点，最后再用这两种方法收尾。牛顿法效率较高，但每一步都需要计算海森矩阵，因此使用精确牛顿法时必须把海森矩阵的计算开销考虑在内。BFGS 在寻找极小点时速度快、效率高，是 jobtype = OPT 的默认算法。处理内存受限的超大体系时，推荐使用 L-BFGS。jobtype = TS 的默认算法是 Bofill。 |
| `QUI_GEOM_OPT_CONVERGENCE_CHECK` | Specifies the convergence criterion. | 指定收敛判据。 |
| `QUI_GEOM_OPT_COORDINATES` | Specifies which type of coordinate sytem to use for optimizations. / Recommendation: / Cartesian can be more stable than internal coordinates, but are generally slower. | 指定优化时使用何种坐标系。 / 建议： / 笛卡尔坐标可能比内坐标更稳定，但通常速度较慢。 |
| `QUI_GEOM_OPT_DISPLACEMENT_CONVERGENCE` | The value of maximum absolute displacement for convergence check. | 收敛判据中最大绝对位移的取值。 |
| `QUI_GEOM_OPT_EIGENVECTOR_ALGORITHM` | Specifies the type of Eigenvector algorithm. | 指定特征向量算法的类型。 |
| `QUI_GEOM_OPT_ENERGY_CONVERGENCE` | The value of maximum absolute energy difference for convergence check. | 收敛判据中最大绝对能量差的取值。 |
| `QUI_GEOM_OPT_FINAL_VIBRATIONAL_ANALYSIS` | Run a vibrational analysis after the geometry optimization. | 在几何优化之后运行振动分析。 |
| `QUI_GEOM_OPT_FINAL_ZMAT_PRINT` | Controls if a Z-matrix is printed at the end of the optimization. | 控制是否在优化结束时打印 Z 矩阵。 |
| `QUI_GEOM_OPT_GRADIENT_CONVERGENCE` | The value of maximum absolute gradient or norm of gradient for convergence check. | 收敛判据中最大绝对梯度或梯度模的取值。 |
| `QUI_GEOM_OPT_HESSIAN_VERIFY` | Specifies the type of verification with the Hessian after the geometry optimization has converged. | 指定几何优化收敛后利用海森矩阵进行检验的方式。 |
| `QUI_GEOM_OPT_INITIAL_HESSIAN` | Specifies the type of initial Hessian to use. / Recommendation: / The initial guess Hessian for Cartesian coordinates is a unit matrix where for internal coordinates it is an approximate Hessian based on the internal coordinates. The model approximate Hessian is based on a force constant matrix. The | 指定使用的初始海森矩阵类型。 / 建议： / 笛卡尔坐标下的初始猜测海森矩阵为单位矩阵，而内坐标下则是基于内坐标的近似海森矩阵。模型近似海森矩阵基于力常数矩阵。精确海森矩阵始终是高质量的初始海森矩阵，但开销可能较大，可在优化开始时计算，或从 scratch 目录读入。OPT 的默认设置是简单的近似猜测海森矩阵，而 TS 则使用精确解析海森矩阵。 |
| `QUI_GEOM_OPT_MAXITER` | Specifies the maximum number of geometry optimization cycles. | 指定几何优化的最大循环次数。 |
| `QUI_GEOM_OPT_MAX_DISPLACEMENT` | The maximum value for the STEP_LIMITER of the Eigenvector following algorithm. | 特征向量跟随算法中 STEP_LIMITER 的最大取值。 |
| `QUI_GEOM_OPT_PRINT_LEVEL` | Specifies the printing level of the optimizer. | 指定优化器的打印级别。 |
| `QUI_GEOM_OPT_PRINT_TOPOLOGY` | Print the topology used for the optimization. | 打印优化所用的拓扑结构。 |
| `QUI_GEOM_OPT_RECOMPUTE_HESSIAN` | Recompute the exact Hessian during optimization. | 在优化过程中重新计算精确海森矩阵。 |
| `QUI_GEOM_OPT_RECOMPUTE_HESSIAN_CYCLES` | The number of cycles before recomputing the Hessian during optimization. | 优化过程中重新计算海森矩阵前所经过的循环数。 |
| `QUI_GEOM_OPT_RESTART` | Specifies if tthe optimization should restart in Cartesian coordinates after back-transformation failure with internal coordinates. | 指定在内坐标反向变换失败后，是否以笛卡尔坐标重新开始优化。 |
| `QUI_GEOM_OPT_STEP_LIMITER` | Specifies the type of limiter to use for adjustmet of the step during geometry optimization. | 指定几何优化中用于调整步长的限制器类型。 |
| `QUI_GEOM_OPT_STEP_SEARCH_ALGORITHM` | Specifies the type of algorithm for geometry step generation. | 指定几何步长生成的算法类型。 |
| `QUI_GEOM_OPT_USER_TOPOLOGY` | Specifies whether to use a genereated or user-specified topology. | 指定使用自动生成的拓扑还是用户指定的拓扑。 |
| `QUI_INTEGRAL_DECOMPOSITION_NONE` | No integral decomposition | 不进行积分分解 |
| `QUI_LARGEMOL_NONE` |  |  |
| `QUI_LIBOPT3` | Selects the new LibOpt3 geometry optimization driver | 选择新的 LibOpt3 几何优化驱动 |
| `QUI_MULTIPLICITY` | Sets the multiplicity of the system | 设置体系的多重度 |
| `QUI_OPTIMIZE` | Selects the old OPTIMIZE geometry optimization driver. | 选择旧的 OPTIMIZE 几何优化驱动。 |
| `QUI_PCM_METHOD` | Use of SWIG is recommended only because it is slightly more efficient than the switching functions of ISWIG. | 推荐使用 SWIG，仅因为它比 ISWIG 的切换函数效率略高。 |
| `QUI_PCM_PRINTLEVEL` | Controls the print level during PCM calculations. | 控制 PCM 计算过程中的输出级别。 |
| `QUI_PCM_RADII` | Bondi radii are widely used. | Bondi 半径被广泛使用。 |
| `QUI_PCM_THEORY` | Specifies the polarizable continuum model. / Recommendation: / The IEF-PCM/SS(V)PE mode is more sophisticated than either C-PCM or COSMO and probably more appropriate for low-dielectric solvents, but it is alao more computationally demanding. | 指定可极化连续介质模型。 / 建议： / IEF-PCM/SS(V)PE 模式比 C-PCM 或 COSMO 更为精细，可能更适用于低介电常数的溶剂，但计算代价也更高。 |
| `QUI_PLOTS_POINTS` |  |  |
| `QUI_PRIMARY_BASIS` | Specifies the basis sets to be used for the inital SCF to determine the occupied orbitals. / Recommendation: / The primary basis should be smaller than the target basis. | 指定用于初始 SCF 以确定占据轨道的基组。 / 建议： / 主基组应小于目标基组。 |
| `QUI_PRINT_ORBITALS` |  |  |
| `QUI_QCHEM_EXECUTABLE` |  |  |
| `QUI_RADIAL_GRID` | Specifies the number of radial point for the exchange-correlation quadrature. | 指定交换相关积分所用径向点的数目。 |
| `QUI_SECTION_OPT` | Adds the $opt section for specifying constraints in the geometry optimization | 添加 $opt 段，用于指定几何优化中的约束 |
| `QUI_SECTION_SWAP_OCCUPIED_VIRTUAL` | Change the occupancies of the guess orbitals (not compatible with the SAD guess) | 改变初始猜测轨道的占据数（与 SAD 猜测不兼容） |
| `QUI_SMX_CHARGES` | Sets the type of atomic charges for the SM12 electrostatic terms. | 设置 SM12 静电项所用的原子电荷类型。 |
| `QUI_SMX_SOLVENT` | Specifies which solvent to use in the SMx models. | 指定 SMx 模型中使用的溶剂。 |
| `QUI_SOLVENT_CAVITYRADIUS` | Sets the radius of the spherical solute cavity, in angstroms | 设置球形溶质空腔的半径，单位为 angstrom |
| `QUI_SOLVENT_DIELECTRIC` |  |  |
| `QUI_SOLVENT_DIELECTRIC_COSMO` | Sets the dielectric constant of the solvent | 设置溶剂的介电常数 |
| `QUI_SOLVENT_MULTIPOLEORDER` | Determines the order to which the multipole expansion of the solute charge density is carried out. / Recommendation: / Use the default.  The expansion is usually converged by order 15. | 确定溶质电荷密度多极展开所进行到的阶数。 / 建议： / 使用默认值。该展开通常在 15 阶即已收敛。 |
| `QUI_SOLVENT_OPTICALDIELECTRIC` | The optical dielectric constant of th PCM solvent. | PCM 溶剂的光学介电常数。 |
| `QUI_SOLVENT_SMX_MODEL` |  |  |
| `QUI_TITLE` | Sets the lable for this section of the input file. | 设置输入文件该段的标签。 |
| `QUI_USE_CASE` | Selects the atenuated coulomb operator (CASE approximation). | 选择衰减库仑算符（CASE 近似）。 |
| `QUI_WINDOWS_DIRECTORY` | Sets the directory for tskill or taskkill | 设置 tskill 或 taskkill 所在的目录 |
| `QUI_WINDOWS_KILL_COMMAND` | The command required to kill qchem jobs, only used on Windows | 终止 qchem 任务所需的命令，仅在 Windows 上使用 |
| `RAMAN` | Controls calculation of Raman intensities. Only relevant for a frequency calculation. | 控制拉曼强度的计算。仅在频率计算中有意义。 |
| `RCA_MAX_CYCLES` | The maximum number of RCA iterations before switching to DIIS when the SCF algorithm is RCA_DIIS | 当 SCF 算法为 RCA_DIIS 时，切换到 DIIS 之前 RCA 迭代的最大次数 |
| `RCA_PRINT` | Controls the amount of output from a RCA SCF optimization. | 控制 RCA SCF 优化的输出量。 |
| `RCA_SWITCH_THRESH` | The threshold for switching between RCA and DIIS when the SCF algorithm is set to RCA_DIIS | 当 SCF 算法设置为 RCA_DIIS 时，在 RCA 与 DIIS 之间切换的阈值 |
| `RC_R0` | Determines the parameter in the Gaussian weight function used to smooth the  density at the nuclei.<br><b>Recommendation:</b> We recommend value of 250 for a typical spit valence basis.  For basis sets  with increased flexibility in the nuclear vicinity the smaller values of r_0  also yield adequate | 确定用于在原子核处平滑密度的高斯权重函数中的参数。<br><b>建议：</b>对于典型的劈裂价基组，我们推荐取值 250。对于在原子核附近具有更高灵活性的基组，较小的 r_0 值也能给出足够的自旋密度。 |
| `ROKS` | Controls whether a restricted open-shell Kohn-Sham calculation will be performed. | 控制是否执行限制性开壳层 Kohn-Sham 计算。 |
| `ROKS_LEVEL_SHIFT` | Introduce a level-shift (in Hartree) to aid convergence. | 引入能级移动（单位为 Hartree）以帮助收敛。 |
| `RPA` | Do an RPA calculation in addition to a CIS calculation | 除 CIS 计算外还进行 RPA 计算 |
| `RPATH_COORDINATES` | Determines which coordinate system to use in the IRC search. / Recommendation: / Mass weighted coordinates are usually more effective. | 确定 IRC 搜索中使用的坐标系。 / 建议： / 质量加权坐标通常更有效。 |
| `RPATH_DIRECTION` | Determines the direction of the eigen mode to follow. This will not usually be known prior to the Hessian diagonalization and thereforr both directions will have to be considered. | 确定所跟随的本征模式的方向。在 Hessian 对角化之前通常无法知道该方向，因此需要考虑两个方向。 |
| `RPATH_MAX_CYCLES` | Specifies the maximum number of points to find on the reaction path. / Recommendation: / Use more points if the minimum is desired, but not reached using the default. | 指定反应路径上要寻找的最大点数。 / 建议： / 如果需要得到极小点而使用默认值尚未达到，则使用更多点。 |
| `RPATH_MAX_STEPSIZE` | Specifies the maximum step size to be taken (in a.u.). | 指定所取的最大步长（单位为 a.u.）。 |
| `RPATH_PRINT` | Controls the level of output for a reaction coordinate calculation. / Recommendation: / Use default, little additional information is printed at higher levels. Most of the output arises from the multiple single point calculations that are performed along the reaction pathway. | 控制反应坐标计算的输出级别。 / 建议： / 使用默认值，更高级别只输出很少的附加信息。大部分输出来自沿反应路径执行的多单点计算。 |
| `RPATH_TOL_DISPLACEMENT` | Specifies the convergence threshold (in a.u.) for the step. If a step size is chosen by the algorithm that is smaller than this, the path is deemed to have reached the minimum. | 指定步长的收敛阈值（单位为 a.u.）。如果算法选取的步长小于该值，则认为路径已达到极小点。 |
| `SCF_ALGORITHM` | Selects the algorithm to use for converging the SCF. / Recommendation: / Use DIIS unless performing a restricted open-shell calculation, in which case GDM is recommended. If DIIS fails to find a reasonable approximate solution in the initial iterations, RCA_DIIS is the recommended fallback option. I | 选择用于 SCF 收敛的算法。 / 建议： / 除限制性开壳层计算外均使用 DIIS，此时推荐使用 GDM。如果 DIIS 在初始迭代中未能找到合理的近似解，推荐的后备选项是 RCA_DIIS。如果 DIIS 已接近正确解但最终未能收敛，推荐的后备选项是 DIIS_GDM。 |
| `SCF_CONVERGENCE` | SCF is considered converged when the wavefunction error is less that 10 / -SCF_CONVERGENCE / . Adjust the value of THRESH at the same time. Note that from Q-Chem 3.0 the DIIS error is measured by the maximum error rather than the RMS error as in previous versions. / Recommendation: / Tighter criteri | 当波函数误差小于 10 / -SCF_CONVERGENCE / 。同时调整 THRESH 的值。注意从 Q-Chem 3.0 起，DIIS 误差以最大误差衡量，而不是以前版本中的 RMS 误差。 / 建议： / 几何优化和振动分析采用更严格的判据。更大的数值可提供更多有效数字，但计算代价更高。 |
| `SCF_FINAL_PRINT` | Controls level of output from SCF procedure to Q-Chem output file at the end of the SCF. / Recommendation: / The break-down of energies is often useful (level 1). | 控制 SCF 结束时从 SCF 过程输出到 Q-Chem 输出文件的级别。 / 建议： / 能量分解通常很有用（级别 1）。 |
| `SCF_GUESS` | Specifies the initial guess procedure to use for the SCF. / Recommendation: / Use SAD, AUTOSAD, or SADMO guess for standard basis sets. For general basis sets, it is best to use the BASIS2 / $rem / . For mixed (standard) basis sets, use AUTOSAD.  Alternatively, try the GWH or core Hamiltonian guess. | 指定 SCF 使用的初始猜测方法。 / 建议： / 对于标准基组，使用 SAD、AUTOSAD 或 SADMO 猜测。对于一般基组，最好使用 BASIS2 / $rem / 。对于混合（标准）基组，使用 AUTOSAD。或者尝试 GWH 或核心哈密顿量猜测。对于 ROHF，从相应阳离子或阴离子的 SCF 计算中 READ 猜测可能很有用。注意，由于密度被做成球形，这可能使原子体系（尤其是过渡金属）倾向于不想要的状态。 |
| `SCF_GUESS_ALWAYS` | Switch to force the regeneration of a new inital guess for each SCF in, for example, a geometry optimization. / Recommendation: / Use the default, unless convergence issues arise. | 切换开关，强制为几何优化等过程中的每次 SCF 重新生成新的初始猜测。 / 建议： / 除非出现收敛问题，否则使用默认值。 |
| `SCF_GUESS_MIX` | Controls mixing of LUMO and HOMO to break symmetry in the initial guess. For unrestricted jobs, the mixing is performed only for the alpha orbitals. / Recommendation: / When performing unrestricted calculations on molecules with an even number of electrons, it is often necessary to break alpha-beta  | 控制初始猜测中 LUMO 与 HOMO 的混合以打破对称性。对于非限制性任务，只对 alpha 轨道进行混合。 / 建议： / 对电子数为偶数的分子进行非限制性计算时，通常需要用此选项或通过在 / $occupied / 。 |
| `SCF_GUESS_PRINT` | Controls printing of guess MOs, Fock and density matrices. | 控制猜测分子轨道、Fock 矩阵和密度矩阵的打印输出。 |
| `SCF_MAX_CYCLES` | Controls the maximum number of SCF iterations permitted. / Recommendation: / Increase for slowly converging systems such as those containing transition metals. | 控制允许的最大 SCF 迭代次数。 / 建议： / 对于含过渡金属等收敛缓慢的体系，可增大该值。 |
| `SCF_PRINT` | Controls level of output from SCF procedure to Q-Chem output file. / Recommendation: / Proceed with care; can result in / extremely / large output files at level 2 or higher. These levels are primarily for program debugging. | 控制 SCF 过程输出到 Q-Chem 输出文件的级别。 / 建议： / 谨慎使用；在级别 2 或更高时可能产生 / 极其 / 大的输出文件。这些级别主要用于程序调试。 |
| `SCF_PRINT_FRGM` | Controls the output of Q-Chem jobs on isolated fragments. / Recommendation: / Use TRUE if details about isolated fragments are important. | 控制孤立片段上 Q-Chem 任务的输出。 / 建议： / 如果孤立片段的详细信息很重要，则设为 TRUE。 |
| `SF_STATES` | Sets the number of spin-ﬂip target states roots to ﬁnd. / [ / i, j, k . . . / ] Find / i / SF states in the ﬁrst irrep, / j / states in the second irrep etc. | 设置要寻找的自旋翻转目标态根的数目。 / [ / i, j, k . . . / ] 寻找 / i / 第一个不可约表示中的 SF 态， / j / 第二个不可约表示中的态，依此类推。 |
| `SKIP_CIS_RPA` | Skips the solution of the CIS, RPA, TDA or TDDFT equations for wavefunction analysis. / Recommendation: / Set to true to speed up the generation of plot data if the same calculation has ben run previously. | 跳过 CIS、RPA、TDA 或 TDDFT 方程的求解，用于波函数分析。 / 建议： / 如果先前已运行过相同的计算，设为 TRUE 可加快绘图数据的生成。 |
| `SOLUTE_RADIUS` | Sets the Onsager solvent model cavity radius. / Recommendation: / Use equation (\ref{eq1000}). | 设置 Onsager 溶剂模型的空腔半径。 / 建议： / 使用公式（\ref{eq1000}）。 |
| `SOLVENT_DIELECTRIC` | Sets the dielectric constant of the Onsager solvent continuum. / Recommendation: / As per required solvent. | 设置 Onsager 溶剂连续介质的介电常数。 / 建议： / 按所需溶剂设定。 |
| `SOLVENT_METHOD` | Sets the preferred solvent model. | 设置首选的溶剂模型。 |
| `SOL_ORDER` | Determines the order to which the multipole expansion of the solute charge density is carried out. | 确定溶质电荷密度多极展开所进行到的阶数。 |
| `SOS_FACTOR` | Controls the strength of the opposite-spin component of the PT2 correlation energy. | 控制 PT2 相关能中相反自旋分量的强度。 |
| `SPIN_FLIP` | Selects whether to perform a spin-ﬂip calculation.  Spin multiplicity should be set to 3 for systems with an even number of electrons, and 4 for systems with an odd number of electrons. | 选择是否执行自旋翻转计算。对于电子数为偶数的体系，自旋多重度应设为 3；对于电子数为奇数的体系，应设为 4。 |
| `SPIN_FLIP_XCIS` | Do a SF-XCIS | 进行 SF-XCIS 计算 |
| `SSG` | Controls the calculation of the SSG wavefunction. / Recommendation: / See also the UNRESTRICTED and DIIS_SUBSPACE_SIZE / $rem / variables. | 控制 SSG 波函数的计算。 / 建议： / 另见 UNRESTRICTED 和 DIIS_SUBSPACE_SIZE / $rem / 变量。 |
| `SSS_FACTOR` | Controls the strength of the same-spin component of the PT2 correlation energy. | 控制 PT2 相关能中相同自旋分量的强度。 |
| `STABILITY_ANALYSIS` | Performs stability analysis for a HF or DFT solution. / Recommendation: / Set to TRUE when a HF or DFT solution is suspected to be unstable. | 对 HF 或 DFT 解进行稳定性分析。 / 建议： / 当怀疑 HF 或 DFT 解不稳定时设为 TRUE。 |
| `STATE_ANALYSIS` | Performs certain excited state analyses for CIS/TD-DFT, ADC, and CC excited states. [EXPERIMENTAL] | 对 CIS/TD-DFT、ADC 和 CC 激发态执行某些激发态分析。[实验性] |
| `STS_MOM` | Compute transition moments between excited states in the CIS and TD-DFT calculations. | 计算 CIS 和 TD-DFT 计算中激发态之间的跃迁矩。 |
| `SVP_CAVITY_CONV` | Determines the convergence value of the iterative isodensity cavity procedure.<br><b>Recommendation:</b> The default value unless convergence problems arise. | 确定迭代等密度空腔过程的收敛值。<br><b>建议：</b>除非出现收敛问题，否则使用默认值。 |
| `SVP_CHARGE_CONV` | Determines the convergence value for the charges on the cavity. When the change in charges fall below this value, if the electron density is converged, then the calculation is considered converged. / Recommendation: / The default value unless convergence problems arise. | 确定空腔上电荷的收敛值。当电荷变化降到该值以下且电子密度已收敛时，即认为计算已收敛。 / 建议： / 除非出现收敛问题，否则使用默认值。 |
| `SVP_GUESS` | Specifies how and if the solvation module will use a given guess for the charges and cavity points. / Recommendation: / It is helpful to also set SCF_GUESS to READ when using a guess from a previous Q-Chem run. | 指定溶剂化模块是否以及如何使用给定的电荷和空腔点猜测。 / 建议： / 使用先前 Q-Chem 运行的猜测时，同时设置 SCF_GUESS 为 READ 会很有帮助。 |
| `SVP_MEMORY` | Specifies the amount of memory for use by the solvation module. / Recommendation: | 指定溶剂化模块使用的内存量。 / 建议： |
| `SVP_PATH` | Specifies whether to run a gas phase computation prior to performing the solvation procedure. / Recommendation: / Running the gas-phase calculation provides a good guess to start the solvation stage and provides a more complete set of solvated properties. | 指定在执行溶剂化过程之前是否先进行气相计算。 / 建议： / 先运行气相计算可为溶剂化阶段提供良好的初始猜测，并提供更完整的溶剂化性质集合。 |
| `SYMMETRY_DECOMPOSITION` | Determines symmetry decompositions to calculate. | 确定要计算的对称性分解。 |
| `SYMMETRY_IGNORE` | Controls whether or not Q-Chem determines the point group of the molecule and reorients the molecule to the standard orientation. / Recommendation: / Use default unless you do not want the molecule to be reoriented. Note that symmetry usage is disabled for RIMP2 jobs. | 控制 Q-Chem 是否确定分子的点群并将分子重定向到标准取向。 / 建议： / 除非不希望分子被重定向，否则使用默认值。注意 RIMP2 任务会禁用对称性使用。 |
| `SYMMETRY_INTEGRAL` | Controls the efficiency through the use of point group symmetry for calculating integrals. / Recommendation: / Use default unless benchmarking. Note that symmetry usage is disabled for RIMP2, FFT and QM/MM jobs. | 通过利用点群对称性计算积分来控制效率。 / 建议： / 除非做基准测试，否则使用默认值。注意 RIMP2、FFT 和 QM/MM 任务会禁用对称性使用。 |
| `SYMMETRY_TOLERANCE` | Controls the tolerance for determining point group symmetry. Differences in atom locations less than 10 / -SYM_TOL / are treated as zero. / Recommendation: / Use the default unless the molecule has high symmetry which is not being correctly identified. Note that relaxing this tolerance too much may  | 控制确定点群对称性的容差。原子位置差异小于 10 / -SYM_TOL / 者视为零。 / 建议： / 除非分子具有高对称性但未被正确识别，否则使用默认值。注意过度放宽该容差可能给计算引入误差。 |
| `THREADS` | Number of threads in shared memory parallel calculations. | 共享内存并行计算中的线程数。 |
| `THRESH` | Cutoff for neglect of two electron integrals. 10 / -THRESH / (THRESH ≤ 14). / Recommendation: / Should be at least three greater than the SCF convergence setting. Increase for more significant figures, at greater computational cost. | 忽略双电子积分的截断。10 / -THRESH / （THRESH ≤ 14）。 / 建议： / 应至少比 SCF 收敛设置大 3。为获得更多有效数字可增大该值，但计算代价更高。 |
| `UNRESTRICTED` | Controls the use of restricted or unrestricted orbitals. / Recommendation: / Use default unless ROHF is desired. Note that for unrestricted calculations on systems with an even number of electrons it is usually necessary to break alpha-beta symmetry in the initial guess, by using SCF_GUESS_MIX or pr | 控制使用限制性还是非限制性轨道。 / 建议： / 除非需要 ROHF，否则使用默认值。注意，对电子数为偶数的体系进行非限制性计算时，通常需要在初始猜测中打破 alpha-beta 对称性，方法是使用 SCF_GUESS_MIX 或提供 / $occupied / 信息。 |
| `VARTHRESH` | Controls the temporary integral cut-off threshold. The variable threshold is set to 10 / -VARTHRESH / ∗ DIIS_error / Recommendation: / 3 has been found to be a practical level, and can slightly speed up SCF evaluation. | 控制临时积分截断阈值。可变阈值设为 10 / -VARTHRESH / ∗ DIIS_error / 建议： / 3 已被证明是实用的级别，并可使 SCF 计算略微加速。 |
| `VCI` | Specifies the number of quanta involved in the VCI calculation. / Recommendation: / The availability depends on the memory of the machine.  For example, a machine with 1.5 GB memory and for molecules with fewer than 4 atoms, VCI(10) can be carried out, for molecule containing fewer than 5 atoms, VCI | 指定 VCI 计算中所涉及的量子数。 / 建议： / 可行性取决于机器的内存。例如，在具有 1.5 GB 内存的机器上，对于少于 4 个原子的分子可进行 VCI(10)，对于少于 5 个原子的分子可进行 VCI(6)，对于少于 6 个原子的分子可进行 VCI(5)。对于少于 50 个原子的分子，可使用 VCI(2)。VCI(1) 和 VCI(3) 通常会高估真实能量，而 VCI(4) 通常给出接近收敛能量的结果。 |
| `VIBMAN_PRINT` | Controls level of extra print out for vibrational analysis. / Recommendation: / Use default. | 控制振动分析的额外输出级别。 / 建议： / 使用默认值。 |
| `WAVEFUNCTION_ANALYSIS` | Controls the running of the default wavefunction analysis tasks. | 控制默认波函数分析任务的执行。 |
| `WRITE_WFN` | Specifies whether or not a wfn file is created, which is suitable for use with AIMPAC. Note that the output to this file is currently limited to / f / orbitals, which is the highest angular momentum implemented in AIMPAC. | 指定是否创建适用于 AIMPAC 的 wfn 文件。注意，输出到该文件的目前仅限于 / f / 轨道，这是 AIMPAC 中实现的最高角动量。 |
| `XCIS` | Do an XCIS calculation in addition to a CIS calculation | 除 CIS 计算外还进行 XCIS 计算 |
| `XC_GRID` | Specifies the quadrature grid to be used for evaluating the exchange-correlation component of the energy.  Either a standard grid should be selected, or a Lebedev grid with the corresponding number of points. / Recommendation: / Use the default unless convergence difficulties arise.  Larger grids ar | 指定用于计算能量交换相关部分的积分网格。可选择标准网格，或具有相应点数的 Lebedev 网格。 / 建议： / 除非出现收敛困难，否则使用默认值。涉及导数和激发态的计算需要更大的网格。 |
| `XC_SMART_GRID` | Uses SG-0 (where available) for early SCF cycles, and switches to the (larger) grid specified by XC_GRID (which defaults to SG-1, if not otherwise specified) for final cycles of the SCF. / Recommendation: / The use of the smart grid can save some time on initial SCF cycles. | 在早期 SCF 周期使用 SG-0（若可用），并在 SCF 的最后周期切换到由 XC_GRID 指定的（更大的）网格（若未指定则默认为 SG-1）。 / 建议： / 使用智能网格可在初始 SCF 周期上节省一些时间。 |
| `XOPT_SEAM_ONLY` | Orders an intersection seam search only, no minimization is to perform. / Recommendation: / In systems with a large number of degrees of freedom it might be useful to locate the seam first setting this option to TRUE and use that geometry as a starting point for the minimization. | 仅执行交叉缝搜索，不进行最小化。 / 建议： / 对于自由度很多的体系，先将此选项设为 TRUE 定位交叉缝，再以该几何结构作为最小化的起点，可能会很有用。 |
| `XOPT_STATE_1` | Specify two electronic states the intersection of which will be searched. / [spin, irrep, state] / spin = 0 Addresses states with low spin, see also EOM EE SINGLETS. / spin = 1 Addresses states with high spin, see also EOM EE TRIPLETS. / irrep Species the irreducible representation to which the sta | 指定将搜索其交叉点的两个电子态。 / [spin, irrep, state] / spin = 0 寻址低自旋态，另见 EOM EE SINGLETS。 / spin = 1 寻址高自旋态，另见 EOM EE TRIPLETS。 / irrep 指定该态所属的不可约表示，针对 C2v 点群对称性 / irrep = 1 表示 A1，irrep = 2 表示 A2， / irrep = 3 表示 B1，irrep = 4 表示 B2。 / state 指定不可约 / 表示内的态编号，state = 1 表示最低激发 / 态，state = 2 表示第二激发态，依此类推。 / 0, 0, -1  |
| `XOPT_STATE_2` | Specify two electronic states the intersection of which will be searched. / [spin, irrep, state] / spin = 0 Addresses states with low spin, see also EOM EE SINGLETS. / spin = 1 Addresses states with high spin, see also EOM EE TRIPLETS. / irrep Species the irreducible representation to which the sta | 指定将搜索其交叉点的两个电子态。 / [spin, irrep, state] / spin = 0 寻址低自旋态，另见 EOM EE SINGLETS。 / spin = 1 寻址高自旋态，另见 EOM EE TRIPLETS。 / irrep 指定该态所属的不可约表示，针对 C2v 点群对称性 / irrep = 1 表示 A1，irrep = 2 表示 A2， / irrep = 3 表示 B1，irrep = 4 表示 B2。 / state 指定不可约 / 表示内的态编号，state = 1 表示最低激发 / 态，state = 2 表示第二激发态，依此类推。 / 0, 0, -1  |
