# 仓库结构说明

本文档说明 `IQmol-for-Chinese` 仓库的目录结构与各文件的用途，方便新成员快速了解代码组织。

> 项目：IQmol 分子编辑与可视化软件的简体中文本地化。
> 上游：<https://github.com/nutjunkie/IQmol3>（GPLv3）　本仓库：<https://github.com/stone-Glitch/IQmol-for-Chinese>

**本文档只讲「东西在哪、是什么」。要编译请看
[doc/构建说明-中文版.md](doc/构建说明-中文版.md)，要用软件请看
[doc/IQmolUserGuide.pdf](doc/IQmolUserGuide.pdf)。**

---

## 一、顶层结构

```
IQmol-for-Chinese/
├── CMakeLists.txt          # 主构建脚本（含中文本地化集成 + 平台修复）
├── configure               # 三平台构建入口（Linux/macOS/Windows）
├── LICENSE                 # GPLv3 许可证
├── README.md               # 项目说明（含汉化成果概述）
├── 仓库结构说明.md          # ★ 本文档（东西在哪、是什么）
├── src/                    # 源码（C++ / Qt5）
├── modules/                # 第三方库（Git 子模块）
├── resources/              # 资源（图标/光标/帮助/QSS 等）
├── translations/           # ★ 中文本地化（本项目核心产出）
├── doc/                    # 用户手册（中文版）与构建文档
├── docs/验证记录/           # 编译与运行验证记录（含诊断报告）
├── dialog_screenshots/     # 中文界面运行时截图
├── scripts/                # 构建/翻译/拉取等辅助脚本
├── samples/                # 示例分子文件（.xyz 等）
├── share/                  # OpenBabel 等运行时数据
├── installer/              # 安装器配置（Linux/Windows）
└── build/                  # 本地构建产物（未纳入版本管理）
```

---

## 二、核心目录说明

### 1. `src/` — 源码

按功能拆分的 18 个子模块，主要部分：

| 子目录 | 说明 | 规模（约） |
|---|---|---|
| `Viewer/` | 3D 视图与渲染 | 21k 行 |
| `Layer/` | 图层模型（分子/表面/轨道等） | 18k 行 |
| `Util/` | 工具类（颜色/文件/字符串等） | 16k 行 |
| `Data/` | 数据模型（原子/键/分子等） | 15k 行 |
| `Process/` | 计算任务管理（Q-Chem 作业） | 10k 行 |
| `Grid/` | 网格数据（分子轨道/密度等） | 9.6k 行 |
| `Parser/` | 文件解析器 | 9.3k 行 |
| `Qui/` | Q-Chem 输入界面（QUI） | 8.9k 行 |
| `Plot/` | 绘图 | 7.9k 行 |
| `Network/` | 网络（SSH 提交作业等） | 5.2k 行 |
| `Main/` | 主窗口/入口 | 3.2k 行 |
| 其余 | Amber / Configurator / GL / Gromacs / Math / Fortran 等 | 少量 |

> 汉化工作主要在源码中补充 `tr()` 包裹，让运行期字符串可被 Qt 翻译体系识别。

### 2. `modules/` — 第三方库（Git 子模块）

| 库 | 用途 |
|---|---|
| `openbabel` | 化学文件格式转换 |
| `hdf5` / `HighFive` | HDF5 数据存储 |
| `libQGLViewer` | 3D 视图基础库 |
| `libarchive` | 压缩包处理 |
| `libssh2` | SSH 通信 |
| `libxml2` | XML 解析 |
| `openmesh` | 网格处理 |
| `yaml-cpp` | YAML 解析 |

> 克隆仓库时必须加 `--recursive`，否则这些子模块为空、无法编译。

> **国内网络克隆子模块频繁失败？** 本仓库另提供打包好的完整子模块源码
> （49,335 个文件，单文件 73 MB，无需合并），位于 **`submodules-package` 分支**，
> 下载后解压到仓库根目录即可，无需联网克隆：
> ```
> https://github.com/stone-Glitch/IQmol-for-Chinese/raw/refs/heads/submodules-package/submodules-package/IQmol-submodules.tar.gz
> https://github.com/stone-Glitch/IQmol-for-Chinese/raw/refs/heads/submodules-package/submodules-package/extract_submodules.bat
> ```
> 两个文件放同一目录，在 cmd 里跑 `extract_submodules.bat "D:\IQmol"` 自动校验并解压。
>
> 下载慢时把 `https://github.com/` 换成 `https://ghfast.top/https://github.com/` 可加速。
> 详细步骤与校验值见该分支下的 `submodules-package/使用说明.md`。

### 3. `translations/` — ★ 中文本地化（核心产出）

| 文件 | 说明 |
|---|---|
| `zh_CN.ts` | 翻译源文件（1519 条，全部完成） |
| `zh_CN.qm` | 编译后的二进制翻译（运行时加载） |

构建时由 CMake 调用 `lrelease` 从 `.ts` 自动生成 `.qm` 到可执行文件同级。

### 4. `doc/` — 文档

| 文件/目录 | 说明 |
|---|---|
| `IQmolUserGuide.tex` | 用户手册（已全文汉化，34 页） |
| `IQmolUserGuide.pdf` | 编译后的中文手册 |
| `IQmolUserGuide.bib` | 参考文献 |
| `figures/` | 手册配图（含中文界面截图） |
| `构建说明-中文版.md` | 三平台编译指南（含环境准备与离线包） |
| `编译-照着敲.md` | ★ Windows 逐步编译指南（从编译到双击运行的完整流程） |
| `界面英文残留清单.md` | 汉化完整性核查：约 71 处未包 `tr()` 的英文残留及处理建议 |
| `GitHub上传方案.md` | 仓库发布与大文件处理方案 |

### 5. `docs/验证记录/` — 验证与诊断记录

按日期归档的编译/运行验证记录与问题诊断报告，用于追溯结论依据。

| 文件 | 说明 |
|---|---|
| `2026-09-13-编译与运行验证.md` | 沙箱完整编译 + 运行验证（含中文界面截图） |
| `2026-09-13-OpenBabel插件加载失败诊断与修复.md` | 插件加载失败的根因分析与修复全过程 |
| `01_主界面.png` 等 | 中文界面运行时截图 |

### 6. `dialog_screenshots/` — 中文界面截图

各对话框在中文界面下的运行时截图，用于文档配图与翻译效果验证。

---

## 三、`scripts/` — 辅助脚本

### 构建与部署

| 脚本 | 说明 |
|---|---|
| `configure`（顶层） | 三平台构建入口 |
| `build_windows.sh` | ★ **推荐**：MINGW64 一键构建。自动下载 CMake 3.31、补全缺失文件、检查依赖、配置指纹自检（配置变更自动重配） |
| `build_windows.bat` | Windows 一键构建（cmd 备选），产出 `IQmol.exe` |
| `deploy_windows.sh` | ★ **编译后必跑**：复制 Qt 插件 / MinGW 运行库 / 资源 `share/` / OpenBabel 数据，使 `build/bin/IQmol.exe` 可直接运行；`--stage DIR` 可整理分发包 |
| `build_docs.sh` | 编译中文用户手册 PDF |
| `update_translations.sh` | 更新翻译（lupdate + 防退化 + lrelease） |
| `fix_gcc15.sh` | GCC 15+ 兼容性修复（老代码缺 `<cstdint>` 等） |

### 子模块修复文件

以下文件供构建脚本自动修复上游子模块的问题，**不要删除**：

| 文件 | 说明 |
|---|---|
| `openbabel_CMakeLists.txt` | 修复 OpenBabel `uninstall` 目标与 yaml-cpp 冲突（CMP0002） |
| `openbabel_data_CMakeLists.txt` | 修复 OpenBabel 数据目录的 Windows 头文件生成 |

### 拉取与环境

| 脚本 | 说明 |
|---|---|
| `clone_repo.sh` / `clone_repo.ps1` | 跨平台一键克隆（含子模块） |
| `拉取加速.bat` | 国内镜像加速拉取 |
| `安装依赖.bat` | 一键安装 MSYS2（至 D 盘）及编译依赖 |
| `install_packages.sh` | Linux 依赖安装 |
| `extract_submodules.bat` | 校验（大小+MD5）并解压子模块离线包 |

### 打包发布

| 脚本 | 说明 |
|---|---|
| `make_deb.sh` | 生成 Linux .deb 包 |
| `mac_deploy.sh` | macOS 打包 |
| `make_ffmpeg.sh` | 编译 ffmpeg 依赖 |
| `github_upload.sh` | 推送构建产物到仓库 |
| `aws-setup.sh` | AWS 相关配置 |

---

## 四、汉化项目关键文件索引

以下是本项目汉化工作最常改动的文件：

| 类别 | 文件 |
|---|---|
| 界面翻译 | `translations/zh_CN.ts` |
| 源码国际化（tr 包裹） | `src/**/*.C`、`src/**/*.h`（约 20+ 文件） |
| 对话框界面文本 | `src/**/*.ui` |
| 用户手册 | `doc/IQmolUserGuide.tex` |
| 手册配图 | `doc/figures/`、`dialog_screenshots/` |
| 构建集成 | `CMakeLists.txt`（lrelease 集成段） |
| 翻译流程 | `scripts/update_translations.sh` |

---

## 五、术语与规范

- 翻译风格：专业、准确、简洁，遵循全国科学技术名词审定委员会官方译法。
- 术语统一：如 "Job" 统一译为「任务」，"Surface" 统一译为「表面」。
- 沟通与文档（除代码注释外）使用简体中文。
