cmake_minimum_required(VERSION 3.14.4)

set(targetName IQmol)
project(${targetName} VERSION 3.1.1 LANGUAGES CXX C Fortran)

include(FortranCInterface)
#FortranCInterface_VERIFY(CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

if(NOT CMAKE_BUILD_TYPE)
   set(CMAKE_BUILD_TYPE Debug CACHE STRING "Build type" FORCE)
endif()

set(CMAKE_CXX_FLAGS  "${CMAKE_CXX_FLAGS} -Wno-deprecated")
set(BUILD_SHARED_LIBS OFF)

if (UNIX AND NOT APPLE)
   set(LINUX TRUE)
endif()

if (APPLE OR WIN32)
   set(CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
endif()

# We need these to be able to include e.g. "Data/Bank.h" in the sources
# and to access autogenerated files 
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/src)
include_directories(${CMAKE_CURRENT_BINARY_DIR}/src)


find_package(Qt5 COMPONENTS Core Gui Xml PrintSupport Widgets OpenGL Sql REQUIRED)
find_package(Qt5LinguistTools REQUIRED)
find_package(Threads REQUIRED)
find_package(OpenGL  REQUIRED)
find_package(OpenMP QUIET)
find_package(OpenSSL REQUIRED)

find_package(ZLIB REQUIRED)
   include_directories(${ZLIB_INCLUDE_DIRS})


if (LINUX)
   set(QGLVIEWER_LIBRARY QGLViewer-qt5 )
else ()
   include_directories(modules/libQGLViewer)
   add_subdirectory(modules  EXCLUDE_FROM_ALL)
   set(QGLVIEWER_LIBRARY QGLViewer)
endif()


if (LINUX)
   find_package(yaml-cpp REQUIRED)
   include_directories(${YAML_INCLUDE_DIRS})
else()
   include_directories(modules/yaml-cpp/include)
   add_subdirectory(modules/yaml-cpp  EXCLUDE_FROM_ALL)
endif()


if (LINUX)
   find_package(OpenBabel3 REQUIRED)
   include_directories(/usr/include/openbabel3)
else()
   include_directories(modules/openbabel/include)
   include_directories(${CMAKE_CURRENT_BINARY_DIR}/modules/openbabel/include)
   # babelconfig.h 是 OpenBabel 在 configure 期由 src/config.h.cmake 生成的头文件，
   # 嵌入构建时偶发未生成，会导致 elements.h 等包含 <openbabel/babelconfig.h> 失败。
   # 这里先用已知 3.1.1 取值显式生成一份兜底；若 OpenBabel 自身 configure_file 随后运行，
   # 会用其正确版本覆盖本文件。
   set(_OB_DATADIR "${CMAKE_CURRENT_SOURCE_DIR}/modules/openbabel/data")
   set(BABEL_DATADIR "${_OB_DATADIR}")
   set(BABEL_MAJ_VER 3)
   set(BABEL_MIN_VER 1)
   set(BABEL_PATCH_VER 1)
   set(BABEL_VERSION "3.1.1")
   set(MODULE_EXTENSION ".obf")
   set(OB_MODULE_PATH "${_OB_DATADIR}")
   set(OB_SHARED_PTR_HEADER "memory")
   set(OB_SHARED_PTR_IMPLEMENTATION "std::shared_ptr")
   set(HAVE_SHARED_POINTER 1)
   set(HAVE_STD_SHARED_PTR 1)
   set(HAVE_GCC_VISIBILITY 1)
   set(HAVE_CONIO_H 1)
   set(HAVE_SYS_TIME_H 1)
   set(HAVE_TIME_H 1)
   set(HAVE_SSTREAM 1)
   set(HAVE_CLOCK_T 1)
   set(HAVE_RINT 1)
   set(HAVE_SNPRINTF 1)
   set(HAVE_STRCASECMP 1)
   set(HAVE_STRNCASECMP 1)
   file(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/modules/openbabel/include/openbabel)
   configure_file(
      ${CMAKE_CURRENT_SOURCE_DIR}/modules/openbabel/src/config.h.cmake
      ${CMAKE_CURRENT_BINARY_DIR}/modules/openbabel/include/openbabel/babelconfig.h)
   add_subdirectory(modules/openbabel  EXCLUDE_FROM_ALL)
endif()


#-------------------------------------------------------------------
# 子模块链接约定: 所有平台一律通过 CMake target 名链接, 由 CMake
# 管理构建顺序与产物路径; Linux 端使用系统库时才直接用库名。
# 不要写死产物文件路径——各子模块产物命名规则不一(有的重命名,
# 有的受 BUILD_SHARED_LIBS 影响), 写死路径在 Windows/MinGW 下必报
# "No rule to make target"。
#-------------------------------------------------------------------

# libssh2 (静态库 target: libssh2_static)
if (LINUX)
   set(LIBSSH2_LIBRARY ssh2)
else (WIN32)
   include_directories(modules/libssh2/include)
   add_subdirectory(modules/libssh2  EXCLUDE_FROM_ALL)
   set(LIBSSH2_LIBRARY libssh2_static)
endif()


# OpenMesh (上游三平台 target 名不同, 保持原样——Windows 侧已实测可用)
if (LINUX)
   find_package(OpenMesh REQUIRED)
   set(OPENMESH_LIBRARIES OpenMeshCore OpenMeshTools)
elseif (APPLE)
   include_directories(modules/openmesh/src)
   add_subdirectory(modules/openmesh  EXCLUDE_FROM_ALL)
   set(OPENMESH_LIBRARIES OpenMeshCoreStatic OpenMeshToolsStatic)
else (WIN32)
   include_directories(modules/openmesh/src)
   add_subdirectory(modules/openmesh  EXCLUDE_FROM_ALL)
   set(OPENMESH_LIBRARIES OpenMeshCore OpenMeshTools)
endif()


# libarchive (静态库 target: archive_static; bcrypt 为 Windows 下的 SHA 依赖。
# 上游 WIN32 分支原引用不存在的 modules/zstd 导致 configure 必崩, 已移除;
# zstd 为 libarchive 可选依赖, 缺失时自动跳过, IQmol 无需 .zst 支持;
# MinGW 下 libarchive 自带 CHECK_ICONV 探测不可靠, 生成的 config.h 把 ICONV_CONST
# 定义成空; 而 OpenBabel 自带 iconv.h 声明 iconv 形参 inbuf 为 const char**,
# 导致 archive_string.c:2092 iconv(cd, &itp,...) 实参 char** 与 const char** 冲突。
# 命令行 -DICONV_CONST=const 会被 config.h 后定义反覆盖(实测 redefined 仍报错),
# 故在 configure 阶段对子模块源做幂等修补(见 WIN32 分支), 调用处实参转型为
# (const char **)&itp, 与 OpenBabel iconv.h 的 const char** 声明对齐)
if (LINUX)
   find_package(LibArchive REQUIRED)
   set(ARCHIVE_LIBRARY ${LibArchive_LIBRARIES})
elseif (APPLE)
   find_package(ZSTD REQUIRED)
   if (EXISTS /opt/homebrew/lib/)
      link_directories(/opt/homebrew/lib/)
   endif()
   include_directories(modules/libarchive/libarchive)
   add_subdirectory(modules/libarchive EXCLUDE_FROM_ALL)
   set(ARCHIVE_LIBRARY archive_static)
else (WIN32)
   include_directories(modules/libarchive)
   # 修复 MinGW 下 libarchive 调用 libiconv 的签名冲突: 对子模块源做幂等修补。
   # OpenBabel 自带 iconv.h 声明 inbuf 为 const char**, 而 libarchive 的 config.h
   # 把 ICONV_CONST 定义成空, 使 archive_string.c:2092 的 &itp 为 char** 不匹配。
   # 此处把调用处实参转型为 (const char **)&itp (已修补则跳过, 避免重复),
   # 它在任意 ICONV_CONST 取值下都合法, 比强行覆盖宏更鲁棒。
   set(_LA_STRING "${CMAKE_SOURCE_DIR}/modules/libarchive/libarchive/archive_string.c")
   if (EXISTS "${_LA_STRING}")
      file(READ "${_LA_STRING}" _LA_SRC)
      string(FIND "${_LA_SRC}" "iconv(cd, (const char **)&itp," _LA_DONE)
      if (_LA_DONE EQUAL -1)
         string(REPLACE "iconv(cd, &itp, &remaining, &outp, &avail)"
                         "iconv(cd, (const char **)&itp, &remaining, &outp, &avail)"
                         _LA_SRC "${_LA_SRC}")
         file(WRITE "${_LA_STRING}" "${_LA_SRC}")
         message(STATUS "[IQmol] 已修补 libarchive archive_string.c: iconv 实参 &itp -> (const char**)&itp")
      endif()
   endif()
   add_subdirectory(modules/libarchive EXCLUDE_FROM_ALL)
   set(ARCHIVE_LIBRARY archive_static bcrypt)
endif()


if (WITH_GROMACS)
   add_compile_definitions(GROMACS)
   add_subdirectory(src/Gromacs)
endif()


# The following must come after the modules as it picks up the include 
# for the system libssh2.h.
#set(Boost_USE_STATIC_LIBS TRUE)
find_package(Boost REQUIRED COMPONENTS headers)
include_directories(${Boost_INCLUDE_DIRS})


set(CMAKE_AUTOUIC ON)
set(CMAKE_AUTOMOC ON)

add_subdirectory(src)

enable_testing()
if (EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/tests)
   add_subdirectory(tests)
endif()

if (${Qt5Core_VERSION_STRING} VERSION_GREATER_EQUAL 5.15 )
   qt_add_resources(IQmol_RESOURCES_RCC resources/IQmol.qrc)
else ()
   qt5_add_resources(IQmol_RESOURCES_RCC resources/IQmol.qrc)
endif ()

set(ICON_PATH resources/IQmol.icns)


if (LINUX)
   add_executable(${targetName} 
      src/main.C
      ${IQmol_RESOURCES_RCC}
   )
   target_link_libraries(${targetName} -static-libgcc -static-libstdc++)
endif (LINUX)


if (APPLE)
   add_executable(${targetName} 
      MACOSX_BUNDLE
      ${ICON_PATH}
      src/main.C
      ${IQmol_RESOURCES_RCC}
   )
   set_source_files_properties(${ICON_PATH} PROPERTIES MACOSX_PACKAGE_LOCATION Resources)
   file(COPY ${ICON_PATH} DESTINATION "bin/${targetName}.app/Contents/Resources")
   file(COPY resources/crtimgseq.py DESTINATION "bin/${targetName}.app/Contents/Resources")
   add_custom_command(TARGET ${targetName} POST_BUILD COMMAND ${CMAKE_COMMAND} -E 
      copy_directory ${CMAKE_SOURCE_DIR}/share ${CMAKE_BINARY_DIR}/bin/${targetName}.app/Contents/share/)
   add_custom_command(TARGET ${targetName} POST_BUILD COMMAND ${CMAKE_COMMAND} -E 
      copy_directory ${CMAKE_SOURCE_DIR}/modules/openbabel/data 
                     ${CMAKE_BINARY_DIR}/bin/${targetName}.app/Contents/share/openbabel
   )
endif(APPLE)


if (WIN32)
   set(WIN32_RESOURCES ${CMAKE_CURRENT_SOURCE_DIR}/resources/windows.rc)
   enable_language("RC")

   add_executable(${targetName} 
      src/main.C
      ${IQmol_RESOURCES_RCC}
      ${WIN32_RESOURCES}
   )
   set_target_properties(${targetName} PROPERTIES WIN32_EXECUTABLE ON)
endif (WIN32)


# ---- 中文本地化：构建时用 lrelease 生成 .qm 并放到可执行文件同级的 translations/ 目录 ----
find_program(LRELEASE_EXECUTABLE NAMES lrelease lrelease-qt5)
set(IQMOL_TS_FILE ${CMAKE_CURRENT_SOURCE_DIR}/translations/zh_CN.ts)
set(IQMOL_QM_DIR  $<TARGET_FILE_DIR:${targetName}>/translations)
set(IQMOL_QM_FILE ${IQMOL_QM_DIR}/zh_CN.qm)

if(LRELEASE_EXECUTABLE)
   add_custom_command(TARGET ${targetName} POST_BUILD
      COMMAND ${CMAKE_COMMAND} -E make_directory ${IQMOL_QM_DIR}
      COMMAND ${LRELEASE_EXECUTABLE} ${IQMOL_TS_FILE} -qm ${IQMOL_QM_FILE}
      COMMENT "Generating ${IQMOL_QM_FILE} from ${IQMOL_TS_FILE}"
   )
else()
   message(WARNING
      "未找到 lrelease/lrelease-qt5，跳过 .qm 生成步骤。"
      "缺少 zh_CN.qm 时界面将回退为英文；"
      "可安装 Qt5 Linguist 工具（如 qtbase5-dev-tools / qttools5-dev-tools）后重新配置。")
endif()


target_link_libraries (${targetName}
   Main
   Process
   Parser
   Network
   Qui 
   Layer
   Viewer
   Configurator
   Data
   Grid
   Util
   Math
   Plot
   Fort
   Amber 
   yaml-cpp
   openbabel
   Qt5::Core
   Qt5::Gui
   Qt5::Xml
   Qt5::PrintSupport
   Qt5::Widgets
   Qt5::OpenGL
   Qt5::Sql
   ${LIBSSH2_LIBRARY}
   ${QGLVIEWER_LIBRARY}
   ${OPENMESH_LIBRARIES}
   ${OPENGL_LIBRARIES}
   ${ZLIB_LIBRARIES}
   ${FORTRAN_LIBRARIES}
   ${ARCHIVE_LIBRARY}
)

if (WIN32)
   target_link_libraries (${targetName} ws2_32)
endif ()
